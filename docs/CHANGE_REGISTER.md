# SDI Estimating Intelligence — decision & change register

**Adding a row is part of making the change** — an estimator response quotes its rows, so
the estimators always know what changed and where the change lives.

This document has two layers, and the distinction is the point of it.

> **Why it was split (16 Sep 2026).** Read as one list, this register claimed several rules
> were finished that the day's live workbooks disproved — a title fix that never reached the
> sheet, a price check still blocking, an answers file still not landing. A change register
> that records intentions as outcomes is worse than none, because it is exactly the document
> somebody reaches for when deciding whether a number can be trusted. So **Part 1 is what the
> engine is actually doing today**, and nothing enters it until a real book shows it working.
> Part 2 keeps every finding, attempt and correction, including the ones that failed.

---

## Part 1 — Current operating guide

Only rules **proven on a live book**. A rule written and tested but not yet seen working
stays in Part 2 until a workbook shows it.

**Rule types.** `safeguard` — a universal check that refuses or flags, never prices.
`engine rule` — a costing rule that fires on stated conditions. `customer term` — commercial
terms for one customer. `shop method` — a method or rate the shop stated. `job answer` — one
job's confirmed reading, governing that job only.

**A rate taken from a single estimate is a `scoped pilot`, never `generic`**, until a second
independent job or the department itself confirms it. That distinction is what D-045 got
wrong and is why this table carries the basis and the scope in their own columns.

### Mandatory rule for changes

Before merging any pricing, routing, extraction or workbook change, record:

- the generic behaviour being introduced or corrected;
- its evidence and source authority;
- applicable scope and explicit exclusions;
- whether it is proven, provisional, job-only or superseded;
- the test/replay that protects it;
- every customer-facing explanation or workbook surface affected.

A commit is not a completed learning change until this register is updated. A rule found only
in code, a test, a chat message or an old workbook is not operational knowledge.

### Price and method separation

Methods may become reusable rules when evidenced: for example, how roll goods are consumed,
when a process applies, or how setup is allocated.

Prices are data, not hidden logic. A price must state its source, date, scope, status and
review/expiry date. A job-specific quote must never become an automatic shared rate.
Historical prices may be used only as identified, labelled historical inference; live SDI,
supplier or current-job evidence takes precedence.

### No silent gaps

The engine must attempt to price every required item. Where the drawing is incomplete, it must
use the best available evidenced basis and explain it. "Not priced" is permitted only after the
documented source hierarchy has been exhausted, and must name the missing information and the
requested owner.

### Historic estimates teach; they do not supply prices

Historic estimates may teach the engine how to identify an item, route it, choose an
appropriate source, or recognise that a fresh quote is required. They must not supply a
numeric price to a new estimate unless that number is independently revalidated from its
original current source.

A figure from an earlier job may not appear in a new estimate as a charge, a fallback, a
comparator, a workbook note or a suggested value. Where one is retained for audit it lives
outside the pricing resolver, and no estimator run may read, display or charge it.

### Non-negotiable pricing policy

**A number copied from an estimator's sheet is not a price source, even as a "reference".** It must not be retained in the current register, documentation, reports, prompts, tests, or audit payloads. What carries forward is the method, applicability, SKU/specification, route and evidence — not the number. A line the engine cannot price from SDI Live / UDEF, a supplier catalogue or API, or an identified current quote is raised awaiting price, naming the owner. (James Gray, 16 Sep 2026; D-078.)

**The engine learns methods and evidence; it does not copy an old estimate into a new one.**
Code may decide *how* to price—quantity, yield, route, setup allocation and applicability—but a
job-specific amount must not become an automatically chargeable code constant.

Prices are resolved and printed in this order, with the winning source, date and scope shown
on the line:

1. **Exact current SDI Live / UDEF purchase or SKU price.**
2. **Exact current supplier catalogue or connected supplier API price.**
3. **A current quote or confirmed price for this job.**
4. **A matched historical SDI estimate**, identified by job, date, supplier/specification and
   match basis. It may price a new job as a *historical comparator/inference*, never as an
   unlabelled standing rate.
5. **LLM market research/estimate** when no better source exists. It is still a price, but
   must be labelled indicative and retain its evidence/date so an estimator can replace it.

The drawing, DXF and SolidWorks files determine the required item, geometry and route; they
do not turn a supplier quote from another job into a current price. A named finish may identify
the process, but does not by itself inherit a previous job's amount. Where the source is old,
missing or inferred, the estimate still prices the work and explains that basis rather than
silently omitting it.

### Configuration and pricing logic

**Configuration is SDI's named knowledge register, not a second estimating engine.**

A value belongs in configuration only when it is reusable, named and evidenced: for example a
material family, stocked sheet size, supplier/SKU mapping, department rate, approved routing
method, packing rule, customer commercial term, or a dated stated figure. Each entry must carry
a meaningful variable/key name plus source, owner, date, scope and review status.

Pricing calculations and decision logic must be centralised in the engine's pricing modules.
The configuration supplies the facts and rules they consume; it must not hide bespoke
calculation code or silently embed a prior job's price.

The estimate workbook remains authoritative for its own formulas, template layout and visible
quantity-break calculations. The engine must populate it with traceable inputs and read back
the resulting figures; it must not recreate divergent spreadsheet logic in configuration or
code.

A new pricing-related setting must answer all of these:

- What does this value represent, in plain estimator language?
- Is it a reusable rule, a current price, a historic comparator, or a job-only answer?
- What source supports it, and when was that source current?
- Which materials, departments, customers, products or job families may use it?
- What supersedes it, and how is a disagreement shown to the estimator?

Job-specific confirmations and quotes remain beside that job's drawings. They must never be
promoted to shared configuration merely because they produced a good result once.

| Rule | Type | Applies to — and NOT to | Source, date, stated or derived | Quantity / setup basis | Proven on | Commit | Status |
|---|---|---|---|---|---|---|---|
| Roll goods price by the length used | engine rule | anything on a roll with a catalogued roll length. NOT bulk or sheet stock | Howard Thurley, 9 Sep 2026 — **stated** (roll length); price live from SDI Live | per unit, by length | 10975-02 16:38 book, tape £[redacted] | `5ee2da9` | verified |
| Acrylic laser 95/hr | shop method | acrylic laser cutting. NOT metal laser, NOT routing | Howard Thurley, 9 Sep 2026 — **stated** | per part | 10975-02 11:19 / 12:56 books | `bdb9356` | verified |
| Linebend 60/hr (0.5 min a bend) | shop method | acrylic line bending | Howard Thurley, 9 Sep 2026 — 60/hr **stated**, per-bend figure **derived** | per bend | 10975-02 16:38 book, row 97 | `bdb9356` | verified |
| Assemble/tape/pack 30/hr (PACP) | shop method | acrylic display assemble-and-pack. NOT joinery, NOT metal | Howard Thurley's own 0355255 sheet, 7 Sep 2026 — **stated** | per unit | 10975-02 11:19 / 12:56 books | `c9b6224` | verified |
| Packing method PACK56 + BOX481 steps | shop method | acrylic display goods ≤ 5 kg a unit. NOT joinery, NOT metal, NOT heavier units | Howard Thurley's own 0355255 sheet — **stated** method, consumables priced live | per unit (bag) + stated box steps | 10975-02 12:16 / 12:56 books | `c9b6224` | verified |
| Per-customer rebate and absorption | customer term | M&S 1.8/0.92, Tesco 2.7/0.93, TTI 6.6/0.93, Boots 2.7/0.93. NOT a customer absent from the table | SDI office table via Tony Ford's Labour tab — **stated** | per order | 11908-21 (£57.09 reproduced), 10975-02 12:56 | `—` | verified |
| A laminated core is bought pre-faced | engine rule | MDF/chipboard with laminating evidence. NOT plain board | Tony Ford, 3 Sep 2026 — **stated** (£172 sheet, Lawcris) | per sheet purchased | 11908-21 books | `—` | verified |
| Blank may be turned on the sheet | engine rule | sheet plastics. NOT directional finishes, NOT steel, NOT faced board | Howard Thurley — **stated** (yield 26) | per sheet | 10975-02 11:19 onward | `bdb9356` | verified |
| Build stamp names the engine, not the folder | safeguard | every run | — | — | 10975-02 12:56 book (`93bc6a7 · 38 loose files`) | `7965e07` | verified |
| One line, one verdict in the explanation | safeguard | every run | — | — | 10975-02 12:56 book (duplicate gone) | `0b5cd85` | verified |
| No workbook cell needs a glyph Excel lacks | safeguard | every workbook | — | — | 10975-02 12:56 book | `93bc6a7` | verified |

**Not yet in this table, and why** — each has a Part 2 row: the answers-file lookup (no book
has shown it landing), the Description title (fix made after the 12:56 book), the displaced
price (still blocking), the phantom DXF extent (still reported), and the joinery rates
(scoped pilot, setup not separated, awaiting Tony).

---

## Part 2 — Change ledger

Every finding, attempt, replacement and proof, newest first — including what did not work.
Status: proposed → implemented → verified (seen on a real book) → **reopened** (a live book
disproved it) → superseded.

| ID | Date | Estimator / source | Job | Finding | Decision / rule | Scope | Implemented in | Commit | Proven by | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| D-210 | 24 Sep 2026 | 04:22 11650-06: packaging £55 and delivery £85 for the order, written as £27.50 and £42.50 a unit at 2 off. The 18-off unit (£393.42) carried the same figures, although the Material Price Break tab held £3.06 and £4.72 at 18: about £62 a unit too much. | per-order lines priced by a house figure or researched | **THE LOOKUP WAS WRITTEN FOR ONE KIND OF PER-ORDER LINE.** The Estimate price cell took the break-table LOOKUP only when the packing method priced the line (`order_gbp_at_breaks`). A researched or house per-order figure carries `order_gbp` alone, so it got a literal and the sheet ignored the table beside it. | Any commercial line with an order figure gets the same `=IF(break row empty, literal, LOOKUP($D$6, …))` formula. The break table already fills that row for any order figure, so D6 now drives packaging and delivery like every break-priced line. | generic | `wb_populate` (BOM price cell) | `this commit` | `test_freight_follows_the_order_quantity` (3, one recalculated in LibreOffice) | implemented |
| D-211 | 24 Sep 2026 | 04:22 11650-06: the handed extender (its DXF 420 × 133) and the plain one (the PANEL default envelope, 400 × 300) were nested at different sizes, £2.43 against £4.51 a part. | handed pairs where only the twin was measured | **THE MIRROR RULE RAN ONE WAY.** It fills a twin from a measured base and refused when the base was unmeasured, even when the twin carried the pair's only measurement. It also treated the base's inferred blank as a reading that could "disagree". | Where the base is unmeasured and the twin measured (rank ≥ DXF), `apply_mirror_geometry` fills the base from the twin at the same rank and with the same guards. Only a MEASURED blank can raise HANDED PAIR DISAGREES; an inferred envelope is a gap to fill. Two unmeasured hands still inherit nothing. | generic | `drawing_job_merge.apply_mirror_geometry` | `this commit` | `test_an_unmeasured_base_takes_the_measured_hands_flat`; `test_two_unmeasured_hands_still_inherit_nothing` | implemented |
| D-213 | 24 Sep 2026 | 04:22 11650-06: the kit BOM lists HANDED ARM (Mirror11650-03-GA) ×3 and HANDED ARM BRACKET (Mirror11650-03-02M) ×3; the sheet charged the plain arm ×3 only. The run's JSON: "MIRROR11650-03-GA is a character interleave of 11650-03-GA and MIRROR11650-03-02M … Dropped from the graph", and the reverse for the bracket. | every pack that names an opposite hand "Mirror<code>" | **TWO RULES EACH READ THE HANDED SET AS SOMETHING ELSE.** (1) The zipped-row check: a mirror code is its base's characters in order, so it scans as an interleave of the base and any other Mirror… code, and both were dropped with their records. (2) The SolidWorks tree: Mirror11650-03-GA shares the slider with 11650-03-GA, so every member the census could see was already claimed, and the tree was filed as a "model variant … never priced". D-206 had only stopped the naming alias; these two ran regardless. | (1) A code the naming convention marks as the mirror of a drawing number is never an interleave suspect. The 10975 chimera is still dropped. (2) A tree whose parent is a mirror code is minted as its own assembly, not filed as a variant; a plain second tree over the same members is still a variant (7332). With the records kept, `apply_mirror_geometry` gives Mirror11650-03-02M the plain arm's measured flat. | generic | `route_compiler.build_part_graph`, `source_connectors/solidworks.apply_native_hierarchy_to_parts` | `this commit` | `test_a_mirror_code_is_never_read_as_two_rows_zipped`; `test_an_opposite_hand_tree_is_minted_not_filed_as_a_variant`; `test_a_plain_second_tree_over_the_same_members_is_still_a_variant` | implemented — verify on run |
| D-214 | 24 Sep 2026 | 04:22 11650-06: the kit prints M4x12mm THREADED PEM STUD ×18 and no M6. The sheet had no M4 line and charged FIXING632 PEM STUD M6 x 12mm ×30; its own record read "'4' from bom_tree was kept, '2' … not applied". | every bought-in matched by description | **THE THREAD SIZE WAS TOKENISED AWAY.** `_bought_in_token_set` split "M4x12mm" into M, 4, X, 12, MM and dropped single letters, so an M4 stud and an M6 stud shared PEM, STUD and 12. That passed "same item", and file_scan's dual-path pass (and `_reconcile_bought_in`) poured the M4 rows into FIXING632. This is the pending "stem code" fault, and it was a description match, not a code match. | The thread designation is its own token (`THREAD:M4`), and two different threads are never the same item. The same thread written two ways (M6x12 / M6 x 12; M5-2 / M5 clinch) still matches. The M4 rows are then added as their own bought-in (`BI-PEMSTUD`, the description rule for an uncoded stud). Whether the kit's printed ×18 lands on that line, and whether FIXING632 M6 still reaches the kit through the sub-assembly BOMs, is confirmed on the next run. | generic | `estimator._bought_in_token_set` / `_bought_in_same_item` | `this commit` | `test_an_m4_stud_is_not_an_m6_stud`; `test_the_same_thread_written_two_ways_is_still_one_item` | implemented — verify on run |
| D-216 | 24 Sep 2026 | 05:40 11650-06: the M4 knob (£0.85 researched last run), the M4 PEM stud and the Yiree key all came back unpriced, "no catalogue, price file or quote". | every researched bought-in line | **D-215 PROBABLY REFUSED TOO MUCH** (to confirm from the run log). It searched the whole answer, including the item's own description and price basis, for SHEET METAL, CNC, MACHINING…, and a PEM stud is rightly "a self-clinching fastener for sheet metal". | `answers_a_purchase` matches only who the answer says to buy from (`verify_against`, `supplier_name`), and only supplier TYPES that make things: FABRICATOR, FABRICATION, MACHINE SHOP, MACHINIST, LASER CUTTING SERVICE, CNC SERVICE, SUBCONTRACT (`config.RESEARCH_FABRICATION_SUPPLIER_TERMS`). The key's "local sheet metal fabricator quotes" is still refused. | generic | `pricing_service.answers_a_purchase` | `this commit` | `test_words_describing_a_bought_item_do_not_refuse_it`; `test_a_purchase_answered_as_a_made_part_is_refused` | implemented — verify on run |
| D-219 | 24 Sep 2026 | 06:24 11650-06: "3 prices missing: FIXINGTBC, BI-PEMSTUD, YIREE KEY". FIXINGTBC's own row read "SAME ARTICLE AS BI-KNURLEDKNOB: costed there, not here", and the knob was charged 32 × £0.85. The row's trail also said ×26 against the charged ×32. Review: "the output still overstates the number of missing prices." | every same-article line | **ONE MARKER, TWO READERS.** `wb_populate` writes the marker and the workbook read-back classifies it as not applicable, but `costed_facts._price_origin` only knew "costed in …" as a cross-reference. So the line was UNPRICED and reached the headline tally, the report's missing-price list and the covering email. | A row saying "SAME ARTICLE AS <kept>" with no money is NIL by design (`same_article`). Its label names the line that carries the money and states this line's own count as not charged ("its own count here (26) is not charged — the quantity on BI-KNURLEDKNOB is"). A genuinely unpriced line (the key) still counts. | generic | `costed_facts._price_origin` | `this commit` | `test_a_same_article_line_is_nil_not_a_missing_price`; `test_a_genuinely_unpriced_line_is_still_missing` | implemented |
| D-220 | 24 Sep 2026 | 06:24 11650-06: MIRROR11650-03-02M was still described as "Bought-in / catalogue component" on the AI Provenance tab while nested, lasered and folded as steel. D-218 had cleared the role on the part record. | opposite hands with mirrored geometry | **THE GRAPH DECIDES THE KIND, AND IT DID NOT COUNT A MIRRORED FLAT AS EVIDENCE.** A line's kind comes from the canonical node first. `_bought_in_record` lets fabrication evidence overrule a transcribed bought-in role, but `has_fabrication_evidence` accepted only a DXF of the part's own. | `has_fabrication_evidence` also accepts `normalized_geometry.geometry_source == "mirror_of_measured"` with `mirrored_from` set. `apply_mirror_geometry` copies only from a base measured at DXF or model rank, so this is a measured flat one step removed. The key and other genuinely bought lines are unaffected. | generic | `bought_in_policy.has_fabrication_evidence` | `this commit` | `test_a_mirrored_measured_flat_is_fabrication_evidence` | implemented |
| D-222 | 24 Sep 2026 | Pre-run check of 12312-01-GA: two unnumbered FIXING rows (M6 washer ×4, M5 serrated washer ×2) sit beside FIXING320 (M6 PEM) and FIXING65 (M5 clinch nut). | uncoded hardware rows | **ONE MINTED CODE FOR TWO ITEMS.** Both washers mint BI-WASHER, so file_scan filed the second as "already present" (M5 washers became M6 ones), and the D-217 alias would have joined both FIXING rows to one record. The washers never merged with the PEM or nut: the thread-aware test (D-214) keeps those apart. | file_scan: when a row's minted code is already held by a DIFFERENT item (thread-aware `_bought_in_same_item`), the row gets its own code with its thread (`BI-WASHER-M5`). The D-217 alias applies only when every row printing the vague code is the same item. A global change adding the thread to every minted code was tried and reverted: it broke agreement between readers (a PEM quantity 4 → 1 in the 11350 fixture). | generic | `file_scan._reconcile_dualpath_into_part_estimates`, `route_compiler` alias pass | `this commit` | `test_two_washers_that_mint_one_code_stay_two_lines`; `test_a_vague_code_over_two_washer_sizes_is_not_aliased` | implemented |
| D-228 | 24 Sep 2026 | 12312-01-GA 13:39 book: the driver, LED tape, power cord and Y-splitter each stated 1 and were costed at 2 (£43.39 a unit). Their trails show two routes: GA → LIGHTING ASM → 08X → part, and GA → part. | every table that lists a sub-assembly and its contents | **ONE PART, TWO ROUTES.** The GA's table lists the lighting assembly and, beside it, the items the assembly holds. The table-wide printed-list rule (D-203) does not fire on a table that also lists assemblies, so the roll-up summed both. | `route_compiler.build_part_graph`: where one table lists sub-assembly S and part c, c is reached through S, and the table's count equals what S already holds, the direct edge is dropped and the line says "counted once, through S". A different count is a genuine extra and stays. | generic | `route_compiler` | `this commit` | `test_a_table_listing_an_assembly_and_its_contents` (2) | implemented |
| D-229 | 24 Sep 2026 | Review of the same book: comparing a BOM row's stated quantity with its costed quantity is mechanical, so the four 1-versus-2 differences should have been on the review list, not only in a column. | every job | **THE DIFFERENCE WAS IN A COLUMN, NOT ON THE LIST.** BOMs & Routes printed "states 1; costed at 2", but the list an estimator works from did not carry it. | `costed_facts`: new decision kind `quantity_check`. It is raised for a costed line whose node's `qty_own` differs from `qty_per_unit` and which is reached by more than one route, or whose difference nothing explains. A count multiplied down one route is the cascade working and is not asked. The entry names both counts, every route, and the money at stake. It is counted in the headline tally ("N quantity checks") and the release reasons, and labelled in the report. | generic | `costed_facts`, `job_report_html` | `this commit` | `test_a_quantity_difference_is_asked` (3) | implemented |
| D-230 | 24 Sep 2026 | 12312-01-GA 13:39 book and review: the 03A back panel (3 mm Foamex, its own DXF, nested and charged as material) and the 04G card graphic had no cutting row, and nothing said so. "Material priced, no way it becomes the drawn shape" should be detected. | every part with a measured flat | **A PRICED FLAT WITH NO WAY TO BE CUT WAS SILENT.** Cutting reached the route only from the drawing's notes, which name the laser on the steel pages and nothing for these two. | New `drawing_job_merge.propose_missing_cuts`, run after the mirror pass (the `file_scan` site and `augment_summary_with_dxf`). It covers a made part with a measured flat and no cutting operation. The shop's own rule (`CUT_METHOD_BY_MATERIAL`) is applied where one exists. Board, foam or plastic sheet with no rule gets `cnc_routing` as an inferred operation, so a timed row reaches the book, and the machine is put to the estimator as a decision. Card, paper, vinyl, or a part priced as bought-in with its own flat, gets no machine: the review list asks how it becomes the drawn shape. `costed_facts` adds any `route_gap` to the decisions list. | generic | `drawing_job_merge`, `file_scan`, `costed_facts` | `this commit` | `test_a_priced_flat_has_a_way_to_be_cut` (4) | implemented |
| D-227 | 24 Sep 2026 | 12312-01-GA 11:52 book: the BOMs & Routes sheet listed nine purchased rows printed "P/P" (LED driver, LED tape, grommets ×3, Velcro loop, Velcro hook, EPDM 1230 ×2, EPDM 300 ×2, power cord, Y-splitter) and two washers printed "FIXING" (M6 ×4, M5 serrated ×2). The Estimate sheet carried one "P/P LED POWER DRIVER" ×2 at £84.34 and one unpriced M5 washer. Every P/P row read "states N; costed at 2". James: these should be part numbers, which estimating can change. | every pack that prints a category code (P/P, FIXING, …) over different articles | **KEYED ON THE CODE, NINE PARTS WERE ONE.** `llm_full_extract.normalize_job` kept the first row per code. `route_compiler._extract_part_records` merged rows by code. The graph made one node per code, and the workbook made one line per node. So one driver stood for the lot, and the other articles had no line, no quantity and no price. | New `part_identity.category_code_identities` / `split_category_code_rows`: a category code (`is_category_not_a_code`) printed over rows naming different articles gives each article "&lt;printed code&gt;-&lt;its words&gt;" (e.g. `P/P-SEMI-BLIND-RUBBER-GROMMET`, `FIXING-M6-WASHER`). The printed code is kept as `printed_code`, and estimating can rename the line. A code whose rows all name one article is unchanged, so the BI- alias (11650 PEM) still applies. Applied where the rows enter: the merged document BOM rows and the dual-path rows (`file_scan`), the extract (`normalize_job`), and the graph (`_split_category_codes`: extract rows, its parts pool by description, an assembly child printed as the bare code expanded to every article at its own row quantity, and dual-path rows matched to the extract's identity). The graph's vague-code alias mints from the printed code, and joins a BI- record only when that record is the same item (thread-aware), so the M5 washer is never the M6 one. The BOMs & Routes quantity lookup uses the article identity through the graph's aliases. `file_scan._is_valid_part_number` accepts a category code carrying its article's words. `push-to-server.ps1` now sends `part_identity.py` (the service's `llm_full_extract` imports it). | generic | `part_identity`, `llm_full_extract`, `route_compiler`, `file_scan`, `bom_and_route_extract`, `tools/start/push-to-server.ps1` | `this commit` | `test_one_category_code_over_many_articles` (6), including route payload → workbook lines: 9 P/P lines at their own quantities, M6 ×4 and M5 ×2 | implemented |
| D-226 | 24 Sep 2026 | Restart at 11:08: pythonw pids 61184 and 53776, both started 11:08:00, were read as two runners, and the pass check (online 1, process_count 1, conflict false, build = HEAD) was a command typed by hand. James: the runner must be rock solid, and it will move to its own server reporting to the portal on another. | every runner restart | **A LAUNCHER AND ITS CHILD LOOK LIKE TWO RUNNERS.** The venv python(w).exe starts the base interpreter as a child with the same command line, and the script listed both as "new runner". Nothing in the script asked the service what it saw. | `restart-runner.ps1`: a runner is now a process whose parent is not itself a runner process. A launcher and its child are reported as one runner. Two separate runners stop the script (exit 8). The script ends by asking `/api/estimate/runners` for online 1, process_count 1, no conflict, and a build that starts with HEAD with no +local edits. It prints PASS, or FAIL (exit 9), or exit 10 if the service is unreachable. New `-Service` / `SDI_SERVICE_URL` for a portal on another machine, and `-ApiKey`. Also: the stale-runner test is now anchored to the newest source mtime rather than the clock. | generic | `tools/start/restart-runner.ps1` | `this commit` | `test_the_runner_can_be_restarted_by_one_command` (+3), `test_a_stale_runner_says_so_before_it_costs_anything` | implemented |
| D-225 | 24 Sep 2026 | James Gray: "when the s/sheet opens can it also open up on the estimating sheet with the page open top left so the condensed estimate is in front of estimating." | every workbook written | **THE OPENING VIEW WAS INHERITED.** The tab, scroll position and selection were whatever the blank template was last saved with, in the same way Show Formulas was (D-208). | New `wb_populate.open_on_sheet` runs before save. It makes the Estimate sheet the active and only selected tab, scrolls it to A1 and puts the cursor on A1. A frozen heading keeps its split, and the scrolling pane starts just below it. The sheet name comes from `CELL_MAP["estimate_sheet"]`. The template is not touched. | generic | `wb_populate.open_on_sheet` | `this commit` | `test_the_estimate_sheet_reads_short` (+2) | implemented |
| D-224 | 24 Sep 2026 | 12312-01-GA run (build 5716833) log: "vision overrode 30". The recon printed CODE-MATCH 'M6 WASHER' (code 12312-01-06M), 'M6 STAR WASHER' (code FIXING320) and 'M6 NYLOC NUT' (code 12312-01-06M-H). 14M was described as VELCRO HOOK and the washer rows were lost. | every pack whose two BOM readers number the table differently (unnumbered FIXING rows, a skipped item) | **ONE LINE'S CODE ON ANOTHER LINE'S WORDS.** `reconcile_page` pairs the two readings by item number. When their numbering drifted, item N was a different article on each. The contest kept vision's code, and precedence then ranked the text layer's description over vision's, so the two lines were spliced into one. Code-less FIXING rows "agreed" on an empty code and merged their descriptions. `06M` "agreed" with `06M-H` because one code contains the other. | When the codes disagree and the words name different articles, or when neither row has a real code and the words differ (thread-aware), the rows are two lines: vision's is kept whole, and the text layer's is kept too unless vision read that article at another item number. On any code override, the description goes with the winning code. A hand never agrees with its base (`is_mirror_code`). FIXING/STD PART/TBC count as no identity (the minter's rule). `merge_pages_into_parents` no longer folds two code-less lines that share an item number. | generic | `merge_boms` (`reconcile_page`, `merge_pages_into_parents`) | `this commit` | `test_two_readings_of_one_table_keep_their_lines` (4) | implemented |
| D-223 | 24 Sep 2026 | Pre-run check of 12312-01-GA Rev B (Tesco IMS digital header): the GA says "display, router and antenna supplied and fitted by Pixel Inspiration UK", and the BOM still lists the Bluefin 49.1" LCD (20-0129-0365), the RUT200 and the puck antenna. | every pack with a third-party supply note | **NOTHING READ A SCOPE NOTE ON A BOM LINE.** The note-scan's exclusion markers ("by others", "customer supplied") only stop it minting parts from note text. A coded BOM line went down the price chain, where a 49" commercial display is a four-figure researched figure. `customer_supplied_zero_cost` was read in two places and set nowhere. | New `third_party_supply`: reads clauses "&lt;items&gt; SUPPLIED [AND FITTED] / PROVIDED / FREE ISSUED BY &lt;party&gt;" (party not SDI). A bought-in line naming one of the items (small synonym table: display/screen/LCD/monitor, router/RUT, antenna/aerial; `config.THIRD_PARTY_ITEM_SYNONYMS`) is marked `supplied_by_third_party`. `estimate_part` lists it at £0 without asking a price. `costed_facts` classifies it NIL with the party named ("if SDI is buying it, price the line"). SDI's parts are protected three ways: fabrication evidence, an SDI detail code (-01M, -06M-H, -08X), or a structural noun (the 02M DISPLAY BRACKET and 05M ROUTER MOUNT PLATE). | generic | `third_party_supply`, `estimator` (mark + short-circuit), `costed_facts._price_origin` | `this commit` | `test_what_another_party_supplies_is_not_priced` (5) | implemented |
| D-221 | 24 Sep 2026 | 06:24 11650-06: the handed extender (11650-04-03A-HANDED) carried laser cutting, while the plain one (11650-04-03A) had only a drawn hole_machining (Drill row) and no cutting operation, although both are nested from one DXF at 420 × 133. | handed pairs | **THE MIRROR SHARED THE FLAT BUT NOT HOW IT IS CUT.** | In `apply_mirror_geometry`, a hand with NO cutting operation (laser, CNC route, waterjet, plasma, punch, guillotine) takes its pair's, as an inferred operation with a review flag. Nothing else is copied: the plain panel keeps its own drilled hole, and the handed panel is not given one. Whether the pair really differs there is for the estimator. | generic | `drawing_job_merge.apply_mirror_geometry` | `this commit` | `test_a_hand_with_no_cutting_operation_takes_its_pairs` | implemented |
| D-218 | 24 Sep 2026 | 05:40 11650-06: MIRROR11650-03-02M (HANDED ARM BRACKET) was nested and charged as fabricated steel from the plain arm's flat, while its AI Provenance material read "Bought-in / catalogue component — no fabrication material". Review: "The charge may be sensible, but those source claims need reconciling." | every opposite hand listed on a page tagged bought-in | **A STALE ROLE SURVIVED THE MIRROR.** The kit page listing the bracket (p.2, the exploded list) carries a bought-in page role, which the part inherited. `apply_mirror_geometry` then gave it a measured flat and never revisited that role. | When a hand takes a MEASURED base's flat (the rule already requires rank ≥ DXF), it is a made part: `is_bought_in` is cleared, the bought_in page role removed, a bought-in canonical kind replaced by the base's, and a review flag says why. The fold decision still records "engine inference"; that is not changed here. | generic | `drawing_job_merge.apply_mirror_geometry` | `this commit` | `test_a_hand_given_a_made_parts_flat_is_no_longer_bought_in` | implemented |
| D-217 | 24 Sep 2026 | 05:40 11650-06: the M4 PEM line arrived as `BI-PEMSTUD` ×30 at £0, while the BOM tab kept "FIXING ×18" as a separate leaf. Hardware-insertion labour was charged against BI-PEMSTUD. | every uncoded BOM row with a vague code ("FIXING", "STD PART") | **TWO NAMES FOR ONE FACT.** file_scan mints the record from the row's words through `synthesise_bought_in_code` ("FIXING" + "M4x12mm THREADED PEM STUD" → BI-PEMSTUD). The graph only did that for placeholder codes ("TBA") in one pack mode, so it kept "FIXING". The kit's printed exploded-list count (18) landed on a FIXING leaf nothing costs. BI-PEMSTUD was built from its own parent list: sub-assemblies 2 + 2 under ×3 each, plus the printed 18 as a direct edge = 30. | The graph aliases a BOM row code to the record the shared rule mints from it, only where that record exists and only where every row printing that code mints the same one. "FIXING" over a stud and "FIXING" over a knob stay apart. The printed-list rule (D-203) then sees one identity: BI-PEMSTUD 18, with the roll-up's 12 recorded beside it. | generic | `route_compiler.build_part_graph` (alias pass) | `this commit` | `test_the_kits_printed_count_lands_on_the_costed_stud`; `test_one_vague_code_over_two_different_items_stays_two` | implemented — verify on run |
| D-215 | 24 Sep 2026 | 04:22 11650-06: the Yiree key (DWG888000) ×2 was £65 each, £135.20 a unit, with "local sheet metal fabricator quotes" as its evidence. The same figure came back on every run since 16:49 on 23 Sep. | every researched bought-in line | **ASKED AS A PURCHASE, ANSWERED AS A MADE PART, AND CACHED.** The key did reach the market asker with `supply=bought_in` (its code, rejected as boilerplate, sent it down the class-word path in `wb_populate`, which calls the same chain). The model priced having a key made; nothing compared the answer's own evidence with the question, and the per-spec cache replayed it. | `pricing_service.answers_a_purchase`: a researched price for a bought-in whose own evidence is a fabrication quote (`FABRICAT`, `SHEET METAL`, `LASER CUT`, `MACHINING`… overridable as `config.RESEARCH_FABRICATION_EVIDENCE_TERMS`) is refused. It is applied by both askers: the pricing-service fallback, on cache hits too, and estimator rung 4. The line falls to the next rung or to the estimator. The purchase brief carries a new cache key, so answers stored before the check are asked again. | generic | `pricing_service`, `estimator._rung4_researcher` | `this commit` | `test_a_purchase_answered_as_a_made_part_is_refused`; `test_both_askers_apply_the_check` | implemented |
| D-212 | 24 Sep 2026 | Review of D-207: "appearing on an assembly BOM means fitting is already covered … The BOM establishes where the item belongs, not which operation's time includes fitting. … unsafe as a rule for every bought-in component. It could underprice future jobs." | every bought-in charged at its bought-in price | **D-207 WAIVED TOO MUCH.** The assembly rows are a fixed time per assembly (one event per BOM parent), not a time per item fitted, so a BOM-listed hinge or knob lost its fitting with nothing else paying for it. | The two-minute fitting applies by default again, BOM-listed or not. It is waived only where the record shows the fitting is charged elsewhere or never happens: (1) a pressed insert (`MANM_INSERT_PART_TOKENS`, PEM/clinch), which its own MANM insert row already times at `MANM_INSERT_SECONDS_EACH`; (2) an item whose owning assembly's description names a spare or loose set (`BOUGHT_IN_LOOSE_SET_TOKENS`: SPARE, LOOSE, SET OF). It is bagged and packed, not fitted. 11650-06's Yiree screw sits in "BINDING SCREW SPARE SET OF 4" and stays at its buy price. `research_context` now stamps `owning_assembly_description`; `BOUGHT_IN_FITTING_WHEN_ON_AN_ASSEMBLY_BOM` is removed. | generic | `estimator.estimate_part`, `research_context`, `config` | `this commit` | `test_a_bought_in_charges_its_buy_price` (7) | implemented |
| D-209 | 24 Sep 2026 | The 11650-06 JSON is 47 MB on one line: it cannot be sent, and Select-String returns the whole file or nothing, so the PEM and handed-arm questions could not be answered from the run itself. | diagnostics only | No tool cut a run's JSON down to the records a question is about. | `tools/diag/extract_job_facts.py <json> <terms…>` writes every record mentioning a term, with its path and long values shortened, to `<json>_json_extract.txt`. Read-only; nothing in the engine changes. | tooling | `tools/diag/extract_job_facts.py` | `this commit` | smoke-tested on a sample | implemented |
| D-208 | 24 Sep 2026 | The 04:22 11650-06 book opened with Formulas > Show Formulas selected on the Estimate sheet: every cell read =IF(H96=0,… instead of the money. James Gray: "switch off the formulas to show the values … in the next release." | every workbook written | **THE VIEW WAS INHERITED, NOT SET.** `SHOW_FORMULAS_ON_ESTIMATE` is False and the engine never turns the view on — but the blank template on the share (`Blank Estimate Sheet  WB 2026.xlsx`) was saved with Show Formulas selected, and openpyxl carries a sheet's saved view into the book written from it. | `wb_populate` sets `showFormulas = False` on every sheet of the book it writes, just before the save. The template is not touched; the opt-in switch (`SHOW_FORMULAS_ON_ESTIMATE`, applied after the save) still turns it on for an estimator who asks, and Ctrl+` toggles it in Excel. | generic | `wb_populate` (pre-save) | `this commit` | `test_the_book_opens_on_values_even_when_the_template_shows_formulas` | implemented |
| D-207 | 24 Sep 2026 | Review of the 20:57 11650-06 run: the Yiree binding screw was researched at £1.25 each ("priced as Yiree Binding Screw, one screw") and the workbook charged £2.29; the AI Provenance tab named its source "config_default_material_rates". Review: "Reconcile bought-in fitting. Correct price provenance." | every bought-in line priced by the catalogue, UDEF or AI | **(1) A FITTING UPLIFT ON TOP OF THE BUY PRICE.** Every bought-in charged at its bought-in price had two minutes of handling added (`BOUGHT_IN_FITTING_MIN_PER_PART`, never defined in config, so a hard-coded 2.0 — and `or 2.0` meant 0 could not turn it off). The screw sits in a spare set packed with the kit, and that packing time is already on the labour rows: the £1.04 was counted twice. Standard-commodity provisionals already took no fitting for exactly this reason. **(2) THE WRONG BLOCK WAS ASKED FOR THE SOURCE.** The AI Provenance tab reads the material block's `price_source`. On a bought-in that block holds the config-rate fallback the geometry path writes when it finds nothing to cut, so it was never replaced with the price that set the line. | (1) A bought-in that an assembly's BOM lists (`owning_assembly`, stamped from the line's own `bom_parent(s)` and from the BOM rows carrying its number or supplier code; a source file name is not an assembly) takes no fitting. It is placed or packed during that assembly's labour. A loose bought-in keeps the uplift. The minutes are now in config; 0 turns the uplift off; `BOUGHT_IN_FITTING_WHEN_ON_AN_ASSEMBLY_BOM = True` restores it on listed lines. The fitting charged is recorded on the line (`system_cost.fitting_gbp_each`). (2) Where the line is charged at its bought-in price, that price's source becomes the material block's `price_source`, so every tab names it. | generic | `estimator.estimate_part`, `research_context.owning_assembly` / `stamp_research_context`, `config.BOUGHT_IN_FITTING_*` | `this commit` | `test_a_bought_in_charges_its_buy_price` (5) | implemented |
| D-206 | 24 Sep 2026 | The 20:57 11650-06 run: unit £547.52 at 2-off (Yiree now £2.29 "priced as Yiree Binding Screw, one screw") — but the handed arm set was missing (3 arms, Rev B 6) with the workbook saying "Merged under one name \| 11650-03-SA01 \| also appears as MIRROR11650-03-SA01", and the Yiree key still £65 each from "local sheet metal fabricator quotes (AI-indicative)". James Gray: "Is the handed arm in the drawing? If so how did we miss it?" | generic | **(1) A LISTED MIRROR WAS FOLDED INTO ITS BASE.** The naming rule joins "Mirror<code>" to the drawing's "<code> MIR" line, or to "<code>" itself "for packs where the drawing does not list the mirror separately" — and it tested "listed separately" only by the "<code> MIR" spelling. The kit's BOM lists the handed arm under the model's spelling, "Mirror11650-03-GA" HANDED ARM ×3 and "Mirror11650-03-02M" HANDED ARM BRACKET ×3, so the fallback fired and the handed set became the plain one. **(2) THE KEY WAS NEVER ASKED AS A PURCHASE.** D-205's request is sent for lines the check calls bought-in; the key carried none of the markers it read, so its unchanged spec returned the stored £65 answer. | (1) `_drawing_code_aliases` takes the codes the drawing's own BOM prints (BOM rows and the extract's hierarchy): a mirror listed there under its own code never falls back onto a non-mirror base. 11350's case (the drawing's "MIR" line) joins exactly as before. (2) `research_context._is_bought_in` reads every marker the engine writes (`material_family`, `normalized_material` BOUGHT_IN/PURCHASED as well as the four before) and one fact: a line with no material and no measured geometry that is not an assembly is bought — nothing says what we would make it from. | generic | `route_compiler._drawing_code_aliases` (+ caller), `research_context._is_bought_in` | `this commit` | `test_a_mirror_the_bom_lists_on_its_own_line_is_not_folded_into_its_base`; `test_a_line_with_nothing_to_make_it_from_is_asked_as_a_purchase` | implemented |
| D-205 | 23 Sep 2026 | The 19:30 11650-06 run, on build `01b6c74` — which contains D-204 (`git merge-base --is-ancestor c9b333a 01b6c74` → yes) — still priced the Yiree screw at **£126.04** each, supplier "xAI Grok LLM - INDICATIVE", with no "priced as" note. | every bought-in line the catalogue cannot price | **D-204 FIXED THE SECOND ASKER; THE FIRST ONE ANSWERS.** The bought-in chain has two AI askers: `PricingService._get_web_ai_fallback` (inside the anchor price source, reached first) and the researched rung 4 in `estimator` (reached only if the first returns nothing). D-204 changed rung 4. The first builds its own spec — description and code, no context, not told it is bought, no request — and its stored answer to that unchanged spec came straight back: the cache is keyed on the spec, and the spec had not moved. | The first asker now sends what rung 4 sends: the description with the line's `research_context` (its other names in the pack, its parent assembly), `supply: bought_in`, and the bought-in request and unit from `indicative_price.research_brief` — one wording for both paths. The changed spec is a new cache key, so the line is asked afresh; nothing else's stored price moves. `item_priced` comes back on the anchor, into the stamp's metadata and onto the line as "AI researched price £x: priced as …". | generic | `pricing_service._get_web_ai_fallback`, `estimator._resolve_part_system_cost` | `this commit` | `test_the_earlier_fallback_asks_the_same_question` — fails on the previous code with the bare-code spec the run sent | implemented |
| D-204 | 23 Sep 2026 | The 16:49 11650-06 run: Yiree binding screw DWG491667 20 × **£126.04** = £2,621.63 — 72% of a £3,627.93 unit — from `xAI Grok LLM - INDICATIVE`. James Gray: "how did we get such a high amount for screws? was this LLM? How do we even fix this?" | every researched bought-in line | **THE MODEL WAS ASKED ABOUT A CODE, AS IF IT WERE A PART TO MAKE, WITHOUT THE REQUEST WRITTEN FOR IT.** Three faults: (1) the costed line's description is only "YIREE CODE - DWG491667" — no noun, size or pack — while the pack names it "Yiree Binding Screw" (sheet 2) and puts it in "BINDING SCREW SPARE SET OF 4"; (2) the prompt is written for fabricated parts ("a part from an engineering drawing", "trade/subcontract price") and nothing said this line is BOUGHT; (3) `research_brief` writes a bought-in request — per unit, the pack it is sold in, a real current listing — and `_build_spec_block` read only its fixed fields, so **that request never reached the model** (two names again). And nothing on the line said what £126.04 was for. | New `research_context`: before pricing, each bought-in line collects its other names in the pack (rows/records sharing its supplier code, e.g. DWG491667) and the description of the assembly its BOM row sits in — added to the research brief only, never to the sheet. The spec block now leads with "Supply: a PURCHASED catalogue component… not a part to be fabricated" for bought-in lines and carries the brief's request as "What is needed: …". The model must return `item_priced` — what it priced and what one unit is — carried through `indicative_price` and the researcher onto the line as "AI researched price £x: priced as …". The cache key already covers the spec, so a changed brief is asked afresh and unchanged lines keep their stored figures. **Not a price fix:** the catalogue still outranks this rung — put DWG491667/DWG888000 in SDI Live at the buyer's price and unit. | generic — rung 4 | `src/research_context.py`, `estimator` (stamp before the part loop; brief; line flag; researcher carries `supply`/`item_priced`), `indicative_price`, `web_ai_price_lookup` (prompt, spec block) | `this commit` | `test_a_researched_price_knows_what_it_is_pricing` (5): the pack's names and parent reach the context; only bought-ins are stamped and the sheet description is untouched; the model is told it is bought and given the request; it must say what it priced and that reaches the line | implemented |
| D-203 | 23 Sep 2026 | The 16:49 11650-06 run, reviewed: "Scope and tree: yes. Quantities and money: no." — arm 03-02M 0 + 3 (Rev B 3 + 3), slider 24 (12), M4 PEM 30 (18), M4 knob 20 (32); header "END PANEL GF CONVERSION PANEL SET"; the scope line titled the kit with an engine note; the shared-part warning told the estimator to switch to 11650-02-GA. "Prefer the printed kit sheet over re-multiplying 03-GA × sliders." | generic | **THE KIT'S PRINTED EXPLODED LIST WAS READ AS A SECOND PATH.** Sheet 2 of the kit GA prints every part once for the whole kit. Its rows became direct edges beside the tree (PEM: the tree's 12 plus the sheet's 18), the roll-up re-multiplied what it already counted (slider: 03-GA ×3 ×6 plus the loose 6), a part only it named stayed unlinked, and the knobs on the arms were lost. The title came from whichever title block the model read; the warning read a shared sub-assembly as evidence of a wrong number. | A table on one parent (one `bom_sheet`) listing only parts, at least three and at least half of which already reach that parent through a sub-assembly, is that parent's **printed exploded list**: its rows add no second path to a part the tree reaches; a part it prints that the parent reaches by no path is linked at the printed count; and for a leaf under a root the **printed total replaces the roll-up** after the cascade, with "printed total on <sheet>: 12 — used instead of the roll-up (24)" on the trail and a note. The kit's own BOM sheet (it lists assemblies) and an ordinary GA table are untouched. Title: the product's own file name ("11650-06-GA COFFRET HOSPITAL KIT_REVB.PDF") — `product_identity.title_from_files`, used by the header resolver and the scope line. The shared-part line now states the fact ("…shares 11650-02-SA02 with 11650-06-GA; it is priced only where 11650-06-GA's BOM reaches it. Check the Drawing Number names the one that ships") and never says "should be". **Not reproduced:** why the plain arm was unlinked in the real run — both likely shapes are covered; the run JSON confirms. The M4/M6 PEM links (item 3) are not in this change. | generic | `route_compiler` (exploded-list block, printed-total override, scope title/wording), `product_identity.title_from_files`, `client_quote_html._drawing_identity` | `this commit` | `test_a_printed_exploded_list_is_the_count` (6; the totals and the trail fail on the previous code): slider 12, PEM 18, knob 32, arm 3; the disagreement stays on the line; the kit's own BOM sheet and an ordinary GA table are unchanged; an arm claimed by an owner the kit never reaches is linked. Title and wording in `test_the_declared_product_is_the_only_root` (23) | implemented |
| D-202 | 23 Sep 2026 | Follow-up to D-200/D-201: D-201 was pushed with one suite failure (`test_every_environment_switch_the_engine_reads_is_documented` — `SDI_ESTIMATE_COMPACT` not in the README; the commit command did not stop on a failed suite). And the third 15:45 fault, the kit GA linked to FIXING632 (the M6 PEM) where its table lists the M4 PEM under the stem code FIXING, still unexplained. | docs + a regression pin | The README switch table now documents `SDI_ESTIMATE_COMPACT` and `SDI_PRODUCT`. For the PEM: the graph, `_raw_identity_aliases`, the prefix-alias rule, `_drawing_code_aliases` and both stem merges (`file_scan`, `drawing_job_merge`) each keep FIXING and FIXING632 apart on the rows as the tables read them — so the link in that run came from somewhere these rows do not reproduce, and **no fix is claimed**. It is pinned instead: a regression test with both GAs' real rows, both products, asserting two identities and two counts. The run's summary JSON (`output\json\11650-02.json`) is needed to find the actual source. | — | `README.md`, tests | `this commit` | full suite green (6,717); `test_the_m4_pem_is_never_the_m6_pem` | implemented (PEM: open) |
| D-201 | 23 Sep 2026 | James Gray: "**Can we also find a way of removing rows not written to in the estimating s/sheet to compress it for it to be easier to read from estimating? Not the blank one we use as the source but the one we create.**" Review: collapse, don't delete — "the labour total sums through row 167" — and test totals, quantity breaks and a manual line in an expanded row. | every generated workbook | **A SHEET OF EMPTY SLOTS.** The 15:45 11650-02 book had 16 real lines and 131 empty slots between them. They look blank and are not — each holds the template's formulas — and the totals, the quantity sweep, the report and the quote all read the sheet by row address, so deleting any would move them. | New `workbook_compact.compact_estimate`, called by `wb_populate` on the OUTPUT book just before it is saved (the template is never opened for writing): every run of unused slots inside each block is **grouped (outline level 1) and hidden**, never deleted. A slot is a row carrying a formula on its own row (titles, headers and totals carry none); it is unused when its description (C) and its Qty Per Unit cell are both empty — anything written keeps it visible. Refuses, with a flag, on a workbook where any formula skips hidden rows (SUBTOTAL 10x, AGGREGATE 1/3/5/7); the template totals with SUM and AGGREGATE(9,6), which include them. Switch: `config.ESTIMATE_COMPACT_UNUSED_SLOTS` (env `SDI_ESTIMATE_COMPACT=0` for the full-length sheet). On the 15:45 book: Standard Materials 7 shown/33 hidden, Wire 0/8, Sheet Steel 1/18, Other Sheet 0/8, Labour 8/64. | generated workbook only | `src/workbook_compact.py`, `wb_populate` (before save), `config` | `this commit` | `test_the_estimate_sheet_reads_short` (6): unused slots hidden and titles/headers/lines/totals kept; a quantity alone keeps a slot; refuses where a total skips hidden rows; idempotent; the writer compacts before saving; **recalculated in LibreOffice**: totals identical, a line typed into a hidden slot adds its £20, 18-off identical. Also checked on the real 15:45 book: £31.54 / £178.42 / £232.04 unchanged, 18-off £50.30 unchanged, a manual 2×£10 line → £254.14. Quote regeneration from an edited book not separately exercised. | implemented |
| D-200 | 23 Sep 2026 | The 15:45 11650-02 run (first on D-198/D-199): the leaks were gone and the unit fell from £1,956.02 to £232.04 — but the not-linked rule had caught **real parts of the top**: "Not linked to 11650-02-GA, so not priced: 11650-03-02M, ROSS HANDLING-TS-15M5X10, YIREE CODE-DWG491667", and 11650-02-SA02 (the top's own RSB) had no trail. The wrong-number hint named FIXING632. | generic | **TWO LINKS THAT WERE NEVER MADE, NOW CHARGED FOR.** (1) The BOM-table edge was refused for any part the extract or model had already placed — so 02-SA02, which the extract put under the kit, never got the top's own ×1 row, and with only linked lines charged the top lost its RSB plate, tabs and PEM. (2) `clean_part_number` collapsed whitespace but not spaced hyphens, so "ROSS HANDLING - TS-15M5X10" (the linked table spelling) and "ROSS HANDLING-TS-15M5X10" (the costed record) were two identities and the handles fell off the sheet while their pack labour stayed on it. (3) The hint named a shared fastener, weak evidence, instead of the shared sub-assembly. | (1) A table row naming a **second owner in a different tree** is linked beside the extract's parent — but only where the tables ALSO list the part under an existing parent (agreement, not the 12392 disagreement the extract still wins), and never where the new owner contains or is contained by an existing parent (an exploded table restating a sub-assembly's part would count it twice). (2) A spaced hyphen is the same hyphen in the canonical spelling. (3) The hint names the product's assemblies a set-aside GA takes, fasteners only if there are none. **Not fixed, not reproduced:** the kit GA linking to FIXING632 (M6 PEM) — neither the graph nor either code-merge rule does it on these rows; the run's summary JSON is needed. | generic | `route_compiler` (BOM-edge loop, `clean_part_number`, the hint) | `this commit` | four in `test_the_declared_product_is_the_only_root` (21), three failing on the previous code: 02-SA02 costs 1 on the top and 3 in the kit; an exploded-table restatement is still refused; the Ross handles are linked and not listed as unlinked; the hint names 02-SA02 | implemented |
| D-199 | 23 Sep 2026 | James Gray: "**we need to centre the job estimate around the drawing number entered into the estimating portal.**" Review: "Use the engine's same product resolver to show the matched GA, title and revision before Run. If the number matches none or more than one, ask the estimator to correct it; do not silently choose a root. If it matches 02-GA while the pack also contains 06-GA, show that distinction prominently without guessing which product they intended." | the estimating portal; generic | **THE ANSWER CAME FORTY MINUTES LATE.** The engine took the Drawing Number as the product (D-192), but the page only checked the number was a substring of some file name — so `11650-02` passed silently beside the kit's GA, and the first sign that the cabinet top had been priced instead of Tim's kit was the finished workbook. | One resolver, `src/product_identity.py` (pure — no config, loadable by the service's interpreter): `names_the_product` (the engine's rule, moved here; `route_compiler._names_the_product` now calls it), `drawing_of_file` (number, title, revision, assembly-or-not from the drawing office's file-name convention) and `resolve_product`. New `POST /api/estimate/product-check` reads the files and folders added (a folder to two levels, inside the shares) and answers with it. Under the Drawing Number field the page says, as you type or add files: **one assembly** — "This run prices 11650-06-GA COFFRET HOSPITAL KIT (Rev B). Also in the pack: 11650-02-GA TOP (Rev D); 11650-03-GA ARM (Rev C). They are detail to 11650-06-GA — priced only where its BOM reaches them… If one of them is what ships, enter its number instead" (amber when others exist); **none** (e.g. the Boots code AC0706-03) or **several** — red, the assemblies listed, and **Run is held** until the number is corrected; nothing is chosen for the estimator; **a part drawing** — said, not blocked; **no numbers in the file names** — said, not blocked. | portal + service + engine matcher | `src/product_identity.py`, `route_compiler._names_the_product`, `sdi-intelligence-backend/estimate_routes.py` (`/product-check`), `sdi-estimating-intelligence.html` | `this commit` | `test_the_portal_names_the_product_before_run` (8): the kit named with title and revision and the other GAs listed; 11650-02 names the top and shows the kit beside it without a guess; the Boots code is a correction; two spellings are one drawing; a part drawing is said, not blocked; the engine and the page share one resolver; the service reads a folder; the page holds Run on none/many and re-checks on every change | implemented |
| D-198 | 23 Sep 2026 | The 11650-02 run (units 1, 2, 18, Drawing Number 11650-02, a large drawing set): £1,956.02 per unit — "**Every charged material, bought-in item and labour operation must have an identifiable path back to**" the product. "A detail drawing can improve the specification of a line; it cannot create an extra charge merely by being in the folder." | generic — every run with a declared product | **THREE FAULTS, ONE RESULT.** (1) D-192 set aside only what ANOTHER GA reached; lines joined to nothing were still charged — a Yiree row (x4 at £126.04, £524.33) and (2) a bought-in "End Panel" the notes recogniser minted from 06-SA01's own BOM description "END PANEL GF CONVERSION PANEL SET", priced at £943.42 (£981.16): £1,505 of the £1,956 unit, on a cabinet top whose own lines come to ~£292. The recogniser's "already on the BOM" list held part records only, and a sub-assembly row is not one. (3) Nothing on any page said which product was priced or what was left out, so a Drawing Number naming the cabinet top instead of the kit read as a normal estimate. The header also printed the kit's Rev B on the top (Rev D). | (1) With a declared product, **reachability decides**: every line with no assembly path from the product is set aside — a line another GA reaches as `outside_the_product` (with that GA), a line nothing reaches as `not_linked_to_the_product`, named as a mapping problem ("its link to the BOM is what is missing"). Order-level generated lines (packaging, delivery, powder, plating placeholders) are never scoped. The scope is applied again at write-out to lines minted **after** the graph (`set_aside_late_lines`). (2) Every BOM row and extract assembly joins the recogniser's "already on the BOM" list. (3) The report opens with **What this estimate prices** and the covering note's "What needs your eye" leads with it: the product and its title, what was set aside, what was not linked — and, where a set-aside GA takes parts OF the product (06-GA takes 3 of 02-SA02), "if 11650-06-GA is what ships, the Drawing Number should be 11650-06-GA". The revision is the product's own sheet's: its record, else its PDF's file name. | generic | `route_compiler` (`build_part_graph`, `_is_generated_line`, `set_aside_late_lines`, `product_scope`, `product_scope_sentences`), `wb_populate`, `estimator` (recogniser dedupe), `job_report_html._render_product_scope`, `estimate_explained`, `client_quote_html._product_revision` | `this commit` | seven more in `test_the_declared_product_is_the_only_root` (17): unlinked lines not charged and named; generated lines kept; the wrong-Drawing-Number hint; late lines scoped at write-out and untouched without a product; an assembly's own row counts as on the BOM; the report leads with what was priced; the revision is the product's own (D, not B) | implemented |
| D-197 | 23 Sep 2026 | The portal, straight after a restart: "**Two runners are running on this machine. They share one SOLIDWORKS seat and one Excel, so an estimate may be wrong. Stop one before running anything.**" Two minutes later 8072 showed one process, no conflict. | the service | **A STOPPED RUNNER COUNTED AS A SECOND ONE.** Conflict was judged over the 90 s online window, so the process just ended on purpose stayed "live" for a minute and a half beside its replacement, and every restart raised the warning. | Conflict and `process_count` are judged over **20 s** (`SDI_RUNNER_CONFLICT_SECONDS`): a live runner checks in every 5 s, so two real ones both appear; a stopped one drops out. A runner deep in a job (which does not claim) falls back to the online window rather than reading as nobody. Online is unchanged at 90 s. | runner status only | `sdi-intelligence-backend/estimate_routes.py` | `this commit` | one in `test_one_runner_serves_every_service`: a process 40 s silent beside a new one is one runner; two both talking is still a conflict | implemented |
| D-196 | 23 Sep 2026 | James Gray: "**this runner going down all the time is completely unacceptable.**" | every machine with a runner | **IT WAS NOT GOING DOWN; IT WAS ON THE OTHER SERVICE.** On 22 and 23 Sep the runner was up, healthy and on the right build, but registered with the hand-started service on 8072 while the page was open on the installed service on 8071 (or the reverse). Each service keeps its own queue and runner list, and a runner polled exactly one; five fixes to *which* port to pick (start-runner, install-runner-task, restart checks) each moved the failure rather than removing it, because the fault was having to choose. | **No choice.** `--server` takes a comma-separated list, and the runner asks EVERY service on it for work each cycle — the claim is also its heartbeat, so every page shows it connected. One job at a time is unchanged (lock, one SOLIDWORKS seat). While a job runs for one service, the others receive `/runner/heartbeat` every 15 s, a new endpoint that records presence and `busy_elsewhere` and **can never hand out a job** (a claim mid-job would start a second COM automation); an older service without it answers 404 and simply shows offline for the length of the job, as before. After a job the runner goes back to the top of the list, so no service waits behind another. The task installer defaults to `http://localhost:8071,http://localhost:8072` (SDI_PORT or -Server still pin); start-runner serves every service that answers instead of the first. | runner + service | `tools/runner/sdi_estimate_runner.py` (`_ask_for_work`, `_heartbeat_while_busy`), `sdi-intelligence-backend/estimate_routes.py` (`/runner/heartbeat`, `busy_elsewhere`), `tools/start/install-runner-task.ps1`, `tools/start/start-runner.ps1` | `this commit` | `test_one_runner_serves_every_service` (5): a job on either service is found; while busy the others get heartbeats and never a claim; a dead service does not stop the live one; both scripts default to both ports; a heartbeat shows the runner online, hands out nothing, and a later claim clears the flag | implemented |
| D-195 | 23 Sep 2026 | James Gray, re-pointing the runner task at 8071: `Register-ScheduledTask : Access is denied` — followed by "**Installed scheduled task … server http://localhost:8071 … task state: Running**", and 8071 still `online: 0`. Earlier the same morning `restart-runner.ps1` waited 45 s and said "NO RUNNER IS RUNNING" while a healthy one heartbeat on 8072. | the runner machine | **TWO MORE FALSE REPORTS FROM MY SCRIPTS.** (1) The installer's `Register-ScheduledTask` failure is a non-terminating CimException, so the `try/catch` never saw it; the script printed "Installed … 8071" and started the unchanged task, still pointed at 8072. (2) D-193's "only a runner born after the restart counts" converted `CreationDate` with the WMI-string converter, but `Get-CimInstance` already returns a DateTime: it threw, the catch returned nothing, and the check could never pass. | (1) Both registrations run with `-ErrorAction Stop`; the task is then **read back** and its argument must name the requested server, or the script prints NOT INSTALLED with the argument the task still has, says to use an Administrator PowerShell, starts nothing and exits 8. (2) A DateTime is used as it is. | runner scripts only | `tools/start/install-runner-task.ps1`, `tools/start/restart-runner.ps1` | `this commit` | two in `test_the_runner_can_be_restarted_by_one_command` (35), **both failing on the previous scripts** | implemented |
| D-194 | 23 Sep 2026 | The D-193 restart: 8071 read "online 0" while the new runner (701eec7, pid 11880) was healthy — on **8072**. Its command line: `sdi_estimate_runner.py --server "http://localhost:8072"`. | the runner machine | **THE TASK WAS INSTALLED POINTING AT THE OTHER SERVER.** `install-runner-task.ps1` defaulted to 8072 (what `start-service.ps1` serves by hand), while `start-runner.ps1` had already been changed to prefer 8071, the installed service. Every task restart therefore registered with the hand-started service, and 8071 said nobody was there. | The task installer defaults to 8071, the same as `start-runner.ps1`; `SDI_PORT` or `-Server` still override. An installed task keeps its old argument until it is re-installed. | runner task only | `tools/start/install-runner-task.ps1` | `this commit` | existing runner/start/service tests (255) pass | implemented |
| D-193 | 23 Sep 2026 | James Gray: "**The process still advertising on 8071 is b4b4afa, pid 41104, started 22 Sep 19:21. The restart script claimed it killed that pid; the heartbeat says it did not.**" | the runner machine | **THE RESTART REPORTED A KILL IT NEVER CHECKED.** Three faults in `restart-runner.ps1`, all mine. The kill step searched `python.exe` only, while the scheduled task runs the runner windowless as `pythonw.exe` — the name step 6 already searched for. `Stop-Process` ran with `SilentlyContinue` and was never checked, so a refusal (another user's or an elevated process) read as success. And the "did it come back" wait counted ANY runner, so the survivor satisfied it on the first look and the restart was reported done. | Both names are ended; each refusal is printed; the script looks again, and if any runner survives it **stops with exit 7 and starts nothing** — a second runner beside a stale one is worse than none — telling the person to re-run from an Administrator PowerShell. The wait counts only a process **born after the restart**, and names its pid and start time. The closing lines say the checkout is only what is on disk and point at `/api/estimate/runners` (online 1, process_count 1, conflict false, build = the commit). | runner restart only | `tools/start/restart-runner.ps1` | `this commit` | three in `test_the_runner_can_be_restarted_by_one_command` (33 total), **all three failing on the old script**: pythonw is ended; a survivor stops the restart before anything starts; only a new-born runner counts | implemented |
| D-192 | 23 Sep 2026 | James Gray, on the 11650-06 re-run: "**Drawing Number on the portal is the product. Every other GA in the folder is detail, counted only on a path from that product.** Do not treat 'two GAs in one enquiry' as two things that ship... If Drawing Number is blank, or does not match a GA in the pack, do not guess. Flag it and stop rolling up... Never key off Boots code." | generic — every job run from the portal | **EVERY UNOWNED GA WAS A PRODUCT.** The forest rule (12392) made every assembly nothing owns a root that cascades at one per unit. 11650-06's folder held the kit GA and the cabinet-top GA; the kit takes three of the top's RSB sub-assembly, and the top's own BOM was costed beside it as a second product: SA02, tabs, RSB plates and PEM studs doubled, the top's panels and Ross hardware added to a kit containing none of them. **And the engine was never told which drawing was the product**: the portal's Drawing Number stopped at the runner, which passed only the folder. The quote then took its title from whichever title block the model read — the extender set's sheet, AC0706-05. | The Drawing Number travels: the runner passes it as `--product`, main.py publishes it as `SDI_PRODUCT` (the `SDI_ORDER_QTY` idiom) and the scan stamps it on the summary as `declared_product`. Every compile reads it through `declared_product_of`. Where it names exactly one assembly (a revision and one sheet-role token set aside — "11650-06", "11650-06-GA Rev B"; never a part OF the product such as -SA01), that is the **only root**. Whatever only another root reaches leaves the graph with an `outside_the_product` issue naming the root it hung from, and its records leave the costed population at all three gates (pre-cost, post-reconcile, workbook) and are filed on the summary as `set_aside_outside_product`. A part the product also reaches keeps only the product's edges. **No guess:** a number that names nothing, or two things, is flagged `declared_product_not_resolved`, and with more than one root **nothing is rolled up**. With a single root that root is still costed and the mismatch flagged — there is nothing to choose between, and stopping would un-multiply every single-GA job whose Drawing Number is typed differently from its sheet. Undeclared (hand-run CLI) is unchanged. The quote header uses the product's node, not the read title block, when the two differ. | generic | `route_compiler` (`build_part_graph`, `_names_the_product`, `declared_product_of`, `set_aside_outside_product`, every compile) + `estimator` + `wb_populate` + `main --product` + `file_scan` + `tools/runner/sdi_estimate_runner` + `client_quote_html._drawing_identity` | `this commit` | `test_the_declared_product_is_the_only_root` (10): **the control reproduces the fault** (SA02 4, Ross costed) and the declared product gives SA02 3 / RSB 3 / tabs 6 with 02-GA, 02-01M and Ross set aside and named; records leave the population; the Boots code stops the roll-up; one root plus a mismatch cascades and flags; the matcher never names -SA01 or GA2; the runner passes the number; **the quote is titled 11650-06-GA, not the sheet the model read** (fails on the old code: 11650-06-SA01) | implemented |
| D-191 | 23 Sep 2026 | The 11650-06 re-run after D-189/D-190: the extender that should cost 3 came out at **18**, and the trail said why — *11650-06-SA01 x3 -> 11650-04-03A x6 (SolidWorks)*. The cross-check that should have flagged "18 against 3" was not on the page. | 11650-06; generic — every SolidWorks-backed assembly | **MY D-189 REGRESSION, TWO FAULTS.** (1) The per-parent count was taken by **adding up** the model's edges from parent to child — but the SLDASM edge list can report the same edge more than once (per instance, per configuration, per file), so one extender per set summed to six. Worse than the 9 it was written to fix. (2) The CHECK note was written with `setdefault`, so any part `_per_parent` had already remarked on — the extender had such a note — **never got the line that would have caught (1)**. A check that yields to an earlier remark is not a check. | (1) The per-parent count is the **ratio of the model's own product totals**: the child's full-depth count ÷ its single parent's (3 extenders ÷ 3 sets = 1), with the arithmetic on the record. Edges are no longer summed. Where the totals do not divide, no per-parent count is derived, the BOM edge decides, and the record says so. With no parent total the edge is used only if every repeat agrees. (2) The CHECK is **appended** to any existing note, never instead of it. Not in this change: two roots on one enquiry (02-GA and 06-GA both costed) — awaiting James's ruling on the portal Drawing Number as the explicit root. | generic | `source_connectors.solidworks.apply_native_to_pre_estimate` + `route_compiler` (the cross-check) | `this commit` | two more in `test_a_model_count_is_not_rolled_up_twice` (8 total), **both failing on the D-189 code**: repeated edges [1,1,1,3] under a drawing count of 2 give 1 per set and 3 costed (was 6); a two-parent slider keeps its multi-parent note **and** gets the CHECK (was swallowed) | implemented |
| D-190 | 22 Sep 2026 | Review of 11650-06: "**The workbook proves that many own quantities and effective quantities differ, but it does not prove all effective quantities are wrong.** For example, the extender is recorded as three per end-panel set and three sets in the job, which can legitimately produce nine... **The system needs to show the exact parent-to-child multiplication trail, then an estimator can approve or correct it.**" | generic — every assembly | **A PRODUCT WITH NO WORKING.** The roll-up kept only its answer. So when a count looked wrong, nobody — reviewer, estimator or engine — could say WHICH edge made it, and the argument about 11650-06 turned on inference from outside the book rather than on the book's own record. The review was right to refuse "every differing quantity is wrong" as proven. | Every path from a root is recorded **as it is walked**: each step with the count ONE parent takes and the reader that said so, ending in the product — *11650-06-GA x1 -> 11650-06-SA01 x3 (BOM) -> 11650-04-03A x1 (BOM, SolidWorks agrees) = 3*. A part reached by two paths gets two lines that sum to what is costed. It is carried on the graph node as `qty_trail` and printed on the BOMs page as **how_the_quantity_multiplies**, with a derivation entry saying what it is not: not a ruling that the count is right — the working, laid out so an estimator can approve an edge or correct the one that is wrong. Nothing about the count itself changes. | generic | `route_compiler.add_descendants` + `PartNode.qty_trail` + `bom_and_route_extract` | `this commit` | two in `test_a_model_count_is_not_rolled_up_twice`: the kit extender's trail names every edge, its count and its reader and ends in the costed figure, **and it reaches the BOM page**; a part under two parents gets two lines that add up | implemented |
| D-189 | 22 Sep 2026 | James Gray, reviewing the 11650-06 Coffret hospital kit against his own exploded list — and having warned in advance: "**Treat 12 as the full-explode including sub-assy; don't buy 6 + 12.**" | 11650-06; generic — every SolidWorks-backed assembly | **THE MODEL'S COUNT WAS ROLLED UP TWICE.** Sliders 24 against 12, locking tabs 12 against 6, RSB plates 18 against 3, extenders 9 against 3. The report named the cause in its own words: *"qty 1.0 -> 3 from the SolidWorks assembly BOM (component count, all levels)"*. The native BOM is **full-depth** — its figure is how many the whole PRODUCT contains, already multiplied through every assembly above. The connector wrote that into `quantity`. But `quantity` on a record means how many ONE PARENT takes — `route_compiler._per_parent` says so in terms (*"both are per-parent statements about the same thing"*) and multiplies it down the tree. The model outranks the drawing's BOM edge, so the product total was kept as a per-parent count and rolled up a second time: 3 extenders in the product, under a set the kit takes 3 of, became 9. **Two facts under one name.** | Each fact under its own name. The connector writes the model's **own per-parent edge count** — from the SLDASM edge list it already reads — to `quantity`, and only where it is unambiguous (one parent). The full-depth figure is kept as `quantity_total_per_unit`, and the compiler **checks its roll-up against it**: two independent roads to one number, and a disagreement is printed and noted on the line rather than silently resolved, because the model's top assembly is not always the job's. A part under several parents is decided by the drawing's per-parent BOM edges, with the reason on the record. **A model with no tree behaves exactly as before** — nothing then says which parent a count belongs to. The compiler docstring that stated the false premise now says when it stopped being false. Not in this change: the binding screws (4 against 20) are the opposite fault, a set quantity never applied, and the missing mirror arms are the handed-assembly rule. | generic | `source_connectors.solidworks.apply_native_to_pre_estimate` + `route_compiler` (the cross-check, `_per_parent` docstring) | `this commit` | `test_a_model_count_is_not_rolled_up_twice` — four, and the first three **fail on the old code exactly as the book did** (extender written as 3, costed as 9): three sets of one extender cost 3; the model total is kept and a disagreement is said; a two-parent part is decided by the BOM; **and the control — no tree, unchanged** | implemented |
| D-188 | 22 Sep 2026 | James Gray, on the second concept book: "**The portal quote is also too customer-like for a render-only pack: it says 'manufactured to drawing' and 'As drawing', despite having one concept PDF, no DXF and 34 assumptions.** Keep the concise 'Concept budget' basis, but remove those two false claims. No giant warning is needed." | render packs | **TWO SENTENCES THAT ARE NOT TRUE.** They are not caveats and no warning replaces them: there IS no drawing, so nothing was manufactured to one and no material or finish is "as" one. The quote said both on a pack of one render. | The lead reads *"&lt;product&gt; — concept budget from the customer's visual"*, and the material and finish placeholders read *"Sighted from the visual"*. The Basis row keeps the concise **Concept budget** line from D-179 and nothing is added. Both placeholders are named in one constant, because the lead SKIPS the finish when nothing was charged and a second spelling of "nothing" reaching that test is how the page came out reading *"sighted from the visual, sighted from the visual"*. Every drawing job is untouched. | render packs only | `client_quote_html` (`_materials_line`, `_finish_line`, the lead, `_NO_FINISH_STATED`) | `this commit` | `test_a_render_pack_is_not_manufactured_to_a_drawing` — executed: neither claim survives, the Basis row does, the lead says it once, **and the control — a drawing job still says "manufactured to drawing"** | implemented |
| D-187 | 22 Sep 2026 | James Gray, on the second concept book: "**The four board panels are charged correctly on the workbook's nested-sheet rows (£31.91 total), but the report and Provenance tab show each as £0 and call the £31.91 an unexplained residual. That is a generic line-to-workbook mapping failure, not an estimating gap.**" | render packs; the join is generic | **A SPACE IN A PART NUMBER BROKE THE JOIN.** `costed_facts._material_row_key` keys a nested block row on **the first word of its description**, because that is where `wb_populate` writes the part number — a rule its own docstring already worries about. The concept mint was `<CODE> <NAME-SLUG>`, two words, so the Other Sheet Material row keyed on `…-CPT01` while the part looked itself up as `…-CPT01 HEADER-PANEL`. **Two keys for one part**: the money landed under one, the line read the other as £0, and the difference fell out as an unexplained residual on every surface that adds the lines back up. Every concept job would have hit it. | A part number is a code and **a code has no space in it**. The name was never needed there — it is the DESCRIPTION, in the next column, already carrying it. The mint is now `<JOB>-CPT01`, and the **CPT token earns its keep**: `1234-C01` is a code a drawing could genuinely print, and telling an estimator their own part number is an invention is the one error `is_sighted_code` must never make. | render packs only; nothing about the join itself changes | `concept_scan.parts_from_concept` / `unit_assembly_part` + `part_identity._CONCEPT_CODE` | `this commit` | two in `test_a_render_is_estimated_by_sighting_its_parts` (56 total): every minted number is one word and **the nested-block key resolves back to the part it belongs to**; and six real codes, `1234-C01` among them, are not mistaken for ours | implemented |
| D-186 | 22 Sep 2026 | James Gray, on the second concept book: "**The hinge and four castors are still £0. That is against your standing rule: visible bought-ins must enter the pricing pipeline.**" | render packs; the rung is generic | **THE FIGURE WAS FOUND AND THEN DROPPED BY A MISSING KEY** — the same shape as the date that made rung 4 inert, one layer further down. `lookup_web_ai_price` asks once per specification, stores the answer and returns `price_is_reproducible` to say so. Three adapters in a row — the researcher, the producer, the estimator's stamp — did not carry it. `indicative_price_to_withhold` then saw an AI figure with nothing saying it holds still and did the one thing it exists for: **kept it off the price column**. So the castors showed £48.16 in one table and *"no price"* in another, and the money column read £0. | The flag is carried through all three. **This is not a relaxation of the withholding policy** — the policy turns on exactly one question, *"is this the same number next run?"*, and a cached figure answers it yes. An UNreproducible figure is still withheld, and the test pins both halves. | generic — every researched price on every job | `estimator._rung4_researcher`, `indicative_price.resolve_indicative`, the rung-4 stamp in `estimator` | `this commit` | `test_a_researched_price_that_holds_still_is_written_to_the_column` — executed: the producer carries the flag, the withholding rule lets a reproducible figure through **and still refuses an unreproducible one**, and both adapters are pinned by source | implemented |
| D-185 | 22 Sep 2026 | James Gray, briefing the 11650 Fragrance Coffret run: "**PETG 04-03A pair — laser acrylic path, do not nest as steel**" and "**Piano hinge + Yiree lock + Häfele + Essentra knobs need real rates, not £0**" | 11650 (Boots Fragrance Coffret); generic | **A BOARD REACHED THE BOTTOM RUNG AND A PLASTIC FELL OFF THE LADDER.** The nesting was already right — PETG has its own 3050×2050 sheet and never nests as steel. The PRICE was not. `config.MATERIAL_PRICE_GBP_PER_KG` deliberately holds no rate for PETG, HIPS, ABS, PVC, FOAMEX, PP or PS — *"a price is a commercial fact and SDI owns it; inventing one would put a number on a quote nobody has agreed to"* — and that refusal is right. What was wrong is what happened next: **nothing**. `_researched_board_rate_m2` states in its own docstring that *"Rung 4 is not a property of being a bought-in; it is the last rung, and every line is entitled to it"*, and it was wired for FACED BOARD alone. So a part with a **measured blank, a known density and a known sheet size** was reported UNDER-CHARGED at £0 — on the packs where the plastic IS the job. 11650's PETG side panels are the standing example, named in that config comment and again in `estimator_inputs`' NO_VOCABULARY branch. | The last rung is offered to **any recognised sheet material with no rate**. The rung is the same rung; only the noun changes — it asks about a *sheet*, not a *board*, because asking the market for "2mm PETG board" gets an answer about the wrong product. The figure comes back **per square metre** and is converted where this path costs by mass, with the arithmetic on the line: **£/kg = £/m² ÷ (gauge × density)**. The line names its source, its date, its quantity basis, what it converted, and **the one config entry that would end the question for every future job**. The source is `llm_indicative_researched`, which `is_non_reproducible_source` already recognises, so the job reports as not firm by itself. **Nothing is invented**: where the producer cannot supply a source, a date and a basis it returns nothing and the line stays unpriced and visible, exactly as before. | generic — every recognised sheet material; a material that HAS a rate never reaches it | `estimator.estimate_material` + `_researched_board_rate_m2` (noun) | `this commit` | `test_a_recognised_material_reaches_the_last_rung` — five, executed: the premise is pinned (PETG recognised, no rate, has a density) so the test fails loudly if somebody adds one; PETG nests as plastic; a researched rate prices a 1570×525×2.2 panel at **£10.72 instead of £0.00** with the conversion checkable and the source named; **no evidence means no figure**; and the control — a material with a rate never asks the market | implemented |
| D-184 | 22 Sep 2026 | James Gray, on the castor line of the first full concept book: "**we need to be able to price castors and hinges**" — and the refusal that read "'5E09BE03B9741E5F-BDAB4AD-C11 CASTOR' **is a real code**, so it was put to the purchasing catalogue… that is a gap on our side" | render packs | **THE WRONG DIAGNOSIS, ACTED ON.** Castors ×4 and hinges ×2 stood at £0 and the sheet sent the estimator to look for a catalogue row that could never have existed: the code is one THIS ENGINE minted from a render. `is_engine_minted_code` knew its own `BI-` mint and not concept_scan's, so a placeholder was reported as somebody's part number. And the reason the researched rung produced nothing was in the brief: it must name a real current listing, and it was handed the single word **CASTOR** — no diameter, no fixing, no load. | Two halves. **The sentence**: a sighted code is recognised as minted (`is_sighted_code`, a full-match on the exact shape `concept_scan` writes, so nothing printed on a drawing can match it) and the line gets its own refusal — *"this part was SIGHTED on a render, and nothing names which item it is… Name the item — a code, a manufacturer reference, or the specification you would order by"*. **The price**: a bought-in sighted line carries `research_description` — the name, the material impression and the sighted size, each marked approximate and sighted in its own text — and the researched rung asks with THAT, and with **no code**, since a minted number invites an answer about a part no supplier lists. The description on the sheet is unchanged; only the question asked of the market is. | render packs only; the recogniser is generic | `part_identity` (`is_sighted_code`) + `concept_scan.parts_from_concept` + the rung-4 call in `estimator` + `estimate_explained` | `this commit` | two in `test_a_render_is_estimated_by_sighting_its_parts` (50 total): a castor carries a briefable specification that says it was sighted while a made panel carries none, and the rung asks with it and without our code; the sighted-code recogniser matches the mint and **refuses five real codes** | implemented |
| D-183 | 22 Sep 2026 | James Gray, on edging: "**I would not merely add it to the confirm list while still charging £65.04. Make it an explicit editable concept assumption with a stated visible-edge basis; otherwise it should not mint a deterministic edge-banding route.**" | render packs | **£65.04 OF BANDING OFF ONE WORD.** All eight panels were banded because the vision model listed `edge_banding` on each — two department set-ups and **over half the labour on the job**. A render cannot show which edges are finished unless the finish is actually visible, and this engine's own rule for a drawing (D-104) is that edging is measured where the drawing MARKS it, never round a perimeter because a part has one. The render path was held to a weaker standard than the drawing path on the same question. | The same standard: **an edge that can be named is work, an edge that cannot is an assumption**. The prompt (c3) asks for `banded_edges` — *"front and top edges show a banded lip"* — and the assembler gates the operation on it. Named, it mints and the basis is recorded on the part. Unnamed, it **does not reach `inferred_operations` at all**: it goes on the assumptions list as *banded edges*, with the action that turns it into a charge — `estimator_decisions.banded_metres`, the field the engine already reads. The rest of the part's route is untouched. | render packs only | `concept_scan` prompt c3 + `parts_from_concept` (`EDGE_BASIS_FIELD`) | `this commit` | `test_edging_is_charged_only_where_the_edges_are_named` — executed: unnamed edging is not charged, the saw survives, one flag names the lever and the assumption is on the list; **named edging mints and records its basis** | implemented |
| D-182 | 22 Sep 2026 | James Gray, on the biggest number in the first full concept book: "**Prevent a render-invented material/description from matching a catalogue item without matching specification and unit basis.**" | render packs | **A CATALOGUE HIT ON A SIGHTED PRINT.** The line was `HEADER GRAPHIC SET`, material `PRINTED_VINYL` — a word this engine uses NOWHERE, invented by the vision model — with no size at all, because a graphic is never nested (D-172). It matched a UDEF row on its description tokens and took **£115.56 each, £120.18 with scrap: a quarter of a £459.56 unit and ~41% of the material**. The pack then stated the source two ways, "catalogue — Udef parts table for estimating" on one page and "the line records no source" on another. A wrong citation is worse than no figure. | **A sighted line may match on a REFERENCE and nothing else** — its own exact part code, or a manufacturer reference it actually carries; both are things somebody wrote down. The description arms are safe for a drawing and unsafe for a render, and the reason is not the arms but what they are matching: a draughtsman's description names a real part with a real size beside it, a sighted description is the model's impression of a picture, carrying no specification to check a row against and no unit basis to compare. **Guarded in front of every word-matched arm** — UDEF's own description query, its token-overlap rescue and its description recall, plus PMA, the bought-in catalogue, the historical-quote RAG and the supplier catalogue — because guarding one would have moved the match one arm down the chain. Nothing real is lost: a sighted line's code is one we minted, so the code arms could never have matched anyway. The line falls to the researched rung, which must name a listing, a date and a unit basis before it can produce a figure. | render packs only; every other line's chain is unchanged | `pricing_service` (`_is_sighted_line`, `_get_udef_anchor`, `_select_anchor_price_source`) | `this commit` | `test_a_sighted_description_cannot_match_a_catalogue_row` — executed: a concept record is recognised and a drawn one is not, the guard sits **before** the RAG and the supplier catalogue in the chain, and UDEF's own description arms return None for a sighted line | implemented |
| D-181 | 22 Sep 2026 | James Gray, on the same book: "**Assembly still ruled out on every panel, so glue-up of the box is missing.**" | render packs | **NOBODY ASSEMBLED THE BIN.** Twenty route lines — saw, cnc_routing, edge_banding, laminating — and not one operation that put the carcass together. **The cause was a field nobody reads, and it was mine**: D-171 wrote the unit's work to `summary["assembly_events"]`, and the route compiler builds its assembly events from its own payload while the workbook costs from the part records. Neither has ever read that key. The engine's own rule was already right and had nothing to fire on — `estimate_process_times` mints bench fitting on a BOARD ASSEMBLY, gated on the part being an assembly parent, and a render pack presented no parent. A rule that is true and idle. | The concept read mints **the product itself**: an assembly parent whose children are the sighted made parts, carrying the unit operations, declared under all three names the assembly rules ask by (`is_assembly_parent`, `canonical_kind`, `assembly_children`) because a parent known to one and not the others is the same fault one level down. **The minutes are not invented here** — the existing rule takes the shop's measured joinery bench rate inside its scope and the house allowance outside it, and says which on the line. The build is timed as the board **most of the sighted panels are made of**, so a scoped pilot measured on faced board cannot silently govern a plain MDF carcass, and the line names the mix it was judged on plus the lever that overturns it. One panel or a pack of bought-ins is **not** an assembly and mints nothing. The dead key is gone. | render packs only | `concept_scan.unit_assembly_part` + the concept hook in `file_scan` | `this commit` | three in `test_a_render_is_estimated_by_sighting_its_parts`: the parent carries all three names and no size of its own and the dead key is gone from the source; **the labour subtotal rises and the line says which rate timed it**; and the control — a two-line render is not an assembly | implemented |
| D-180 | 22 Sep 2026 | The first live concept run: `[packing] declined — 5E09BE03B9741E5F-BDAB4AD-C01 LEFT-SIDE-PANEL is 'MDF'` | render packs | **THE SIGHTED PARTS WERE NUMBERED AFTER A HASH.** A render is scanned as a *content-keyed* PDF in the output tree, so the anchor's stem is `5e09be03b9741e5f-…` — and every sighted part inherited it. Nobody can match that code to a job by eye, and nobody would type it into a confirmations file, which is the door D-179 depends on. | The sighted parts are numbered after the **job folder** — `BDAB4ADF-3340-40M-S-C01 HEADER-BOARD` — falling back to the pack's own stem only where there is no job folder. | render packs only | the concept hook in `file_scan._finalize_scan_summary` | `this commit` | `test_a_sighted_part_is_numbered_after_the_job_not_the_wrapper` — executed: the hook names the job folder, and a sighted part carries the job's code | implemented |
| D-179 | 22 Sep 2026 | James Gray, reviewing whether the render path is safe to re-run: "**The concept path still turns a render's guessed MDF, 5 mm thickness, dimensions and operations into normal pricing inputs... It is acceptable only as a clearly editable concept budget, with each assumption available to confirm—not as a technical estimate reconstructed from a PNG.**" | render packs | **PROVENANCE ANSWERS THE WRONG QUESTION.** Every concept figure was already stamped `vision_concept` and carried the cue it was scaled from — which answers *"where did this come from"* about a datum already in your hand. An estimator needs the opposite and could not get it: **what did this assume, all of it, in one place**, and how do I confirm one? Confirming meant hand-authoring JSON for a part number nobody wants to retype, and nothing in the deliverables said the document was a concept budget at all. | **The list, the door, and the two places a document says what it is.** (1) Every figure the mapper puts into pricing is recorded on the part as an assumption with its cue, and `assumption_register` gathers them — **a figure an estimator has since answered drops off the list**, so it is not re-asked every run. (2) `write_assumptions_file` writes the answers file `estimator_confirmed` **already reads and already finds**, pre-filled — no new convention, nothing copied anywhere. **It cannot apply itself**: every entry is `"basis": "inferred"` with its reasoning left EMPTY, and that door refuses an inferred figure that states none — *"a claim without its working is a guess wearing a person's authority"*. Omitting `basis` would have defaulted it to **"read" — printed on the sheet** — which is precisely the laundering this refuses. A person who types their reasoning has confirmed that assumption on purpose and it enters at THEIR rank, above the render. **An estimator's own file is never overwritten.** (3) The job report gains section 8.5 — the whole list, with the file named — and the quote's **Basis row** reads *"Concept budget — every size, material and count sighted from the render"*. No banner, no warning block: the Basis row is where a document says what it is. Two supporting fixes: the confirmations pass runs **again** over the sighted parts, because it runs before they are minted and would otherwise report every entry as naming no part of this job; and a leading underscore is now a comment on a **part** entry as it has always been on a decision, so the cue written beside each figure is not reported as a line that did nothing. | render packs only; the answers-file door and the comment rule are generic | `concept_scan` (`assumption_register`, `assumptions_payload`, `write_assumptions_file`) + `file_scan` concept hook + `job_report_html._concept_assumptions_section` + `client_quote_html` Basis row + `estimator_confirmed.load_corrections` | `this commit` | seven in `test_a_render_is_estimated_by_sighting_its_parts` (43 total): the list holds every assumed field with its cue and is empty on a drawing pack; the written file is the one the engine's own finder finds; **an untouched template applies nothing and reports each part as awaiting reasoning**; an answered one displaces the render at `estimator_inferred` and leaves the list; an estimator's own file is never overwritten; a note beside a figure is not an error; the report section and the Basis row say concept budget and are silent on every other run | implemented |
| D-178 | 22 Sep 2026 | James Gray, reviewing whether the render path is safe to re-run: "**The console message also says 'client quote written' even when the result is `_quote_PORTAL.html`; it should say 'portal estimate written' unless it is actually releasable.**" | every LLM-only run | **THE FILENAME FAULT IN THE OTHER PLACE A DOCUMENT IS NAMED WITHOUT BEING OPENED.** D-174 made the filename say whether a file may go out; the console line beside it still announced a *client quote* for a file written as `_quote_PORTAL.html`. An operator watching the run was told the release gate had passed when it had not — the same fact stated two ways, one of them wrong. | The console noun is decided by the SAME `_releasable` fact as the filename: **client quote** when the estimator has completed the commercial inputs and authorised release, **portal estimate** otherwise. Nothing else about the line changes — it still names the vision-model read and where to see which readers ran. | generic — every quotation | `client_quote_html.generate_quote_files` | `this commit` | `test_the_console_calls_the_file_what_it_is` — executed: an untraceable LLM-only run prints *portal estimate written*, never *client quote*, and lands on `_quote_PORTAL.html`; plus a source check that the noun and the filename branch on one fact so they cannot drift apart again | implemented |
| D-177 | 22 Sep 2026 | James Gray, same review: "**There is also a smaller scope issue: the CAD refusal scans every file in the job folder, not only the selected/staged pack.**" | render packs | **A FOLDER IS A PLACE, NOT A SELECTION.** The measured-CAD guard (D-173) listed `job_folder.iterdir()` — so one stale STEP in a customer's drop, an old revision, a neighbouring job or a file somebody parked there refused the concept read on a pack of renders that had nothing to do with it. The refusal is right; its reach was a directory listing standing in for the estimator's choice. | The guard reads **what this run staged or actually attached**: the scanned documents, the anchor PDF, the DXFs the run attached — *including ones the job discovered*, because that geometry is IN the estimate whatever found it — and `staged_inputs`, the selection exactly as handed to `scan_folder_job` **before it dropped what no reader opens**, so a STEP the estimator chose still refuses the read while a STEP merely lying beside it does not. | render packs only; the refusal itself is unchanged | `file_scan.scan_folder_job` (records `staged_inputs`) + the concept guard in `_finalize_scan_summary` | `this commit` | `test_the_cad_guard_reads_the_staged_pack_not_the_folder` — executed: no directory listing survives inside the guard, it reads the staged pack and the attached DXFs, and the staged selection is recorded where it can read it | implemented |
| D-176 | 22 Sep 2026 | James Gray, same review, blocking a normal re-run: "**Any unexpected LLM `kind` falls through as fabricated and can still receive a blank, material, dimensions and route. The prompt constrains the model, but the mapper does not validate its output.**" | render packs | **THE PROMPT WRITES THE WORD AND THE MAPPER TRUSTS IT — TWO NAMES AGAIN, ONE LEVEL UP.** `record["concept_kind"] = kind or "fabricated"` made every word the prompt did not write — a translation, a typo, *subassembly*, *electrical*, tomorrow's prompt edit — into a made panel. A made panel gets a blank, **and a blank is an instruction to nest**: the identical route by which a castor was nearly nested 338 to a sheet, reached this time through the classification rather than the size. | `_concept_kind` validates against the three kinds the mapper knows and normalises **the prompt's own words only** — case and punctuation, so *Bought-In* is `bought_in`; a synonym it would have to guess at (*purchased*) is not mapped, because a guess wearing a schema's clothes is the fault itself. **An unrecognised or absent kind is `unknown`: a refusal with a name on it, not a default.** The line stays on the sheet with its count and description — *"we dont drop something if it's obviously something that is part of the unit"* — and carries **no material, no size, no operations** until somebody classifies it, with ONE estimator action naming the word the model returned. Everything the model said is kept as words in `concept_unclassified`, off every field that prices. The BOM page says `unknown` and *"not classified"* rather than defaulting to fabricated with empty columns. | render packs only; a measured pack never reaches this mapper (D-173) | `concept_scan.parts_from_concept` / `_concept_kind` + `bom_and_route_extract.bom_sheet` | `this commit` | four in `test_a_render_is_estimated_by_sighting_its_parts` (35 total): an unknown kind gets no blank, no material and no route but keeps its quantity and one flag naming the word; a missing kind is not a made panel and is not asked for dimensions on top of its classification; *Bought-In* normalises while *purchased* does not; the BOM row says unclassified — **plus the controls that a fabricated panel still nests and a castor still does not** | implemented |
| D-175 | 22 Sep 2026 | The review of the render deliverables: "**BOM HTML > 0 rows (or an explicit concept-BOM section — not 'Nothing in this record')**" | render packs | **THE ONE DELIVERABLE WHOSE JOB IS "WHAT PARTS" ANSWERED WITH SILENCE.** `bom_sheet` read `document_analysis.bom_rows` and nothing else — the DRAWING'S own parts list, which a render has none of. So a concept run sighted its parts, priced them, put them on the Estimate sheet, and the BOMs & Routes page still printed *"Nothing in this record"* on the one pack where the question matters most. The review's pass criterion 1 would have failed on a build where everything else worked. | Sighted parts appear as BOM rows — same table, same detail — with `read_by: vision_concept`, the assumed blank marked *(assumed)*, the work sighted on the line, and `kind` saying fabricated / bought_in / graphic so nobody infers "guessed" from a blank page column. A bought-in line shows no blank at all, because it is bought by the each. **They are NOT written into `document_analysis.bom_rows`**: that field means "read off the drawing's parts list", and a sighted line is not one — two facts, two names. | render packs only | `bom_and_route_extract.bom_sheet` | `this commit` | three more in `test_a_render_is_estimated_by_sighting_its_parts` (30 total): the page shows all eleven with reader, quantity, assumed blank, work and kind, and a bought-in carries no blank; the drawing's own `bom_rows` is **not** mutated; **and the control — a real drawing pack's table comes out unchanged**, or this would have quietly altered every job in the building | implemented |
| D-174 | 22 Sep 2026 | James Gray, on the deliverables of an LLM-only run: "**WHY IS THE filename also LLM ONLY.. we need to stop making decisions like this. we know quotes won't go out without being checked.**" | every quotation | **THE SEVENTH REMOVAL OF THE SAME INSTINCT, THIS TIME FROM THE FILENAME.** The red LLM-only band, the invariant banner, *DRAFT — not for issue*, the undrawn-parts gap list, PRICE PENDING and the PORTAL VIEW panel all came off the page. `_quote_LLM-ONLY.html` was the identical reflex relocated: labelling a document with what is imperfect about it, on the assumption somebody will attach it unread. **They will not — a quote is checked before it is sent**, and which readers ran is already on the page's own Basis row (*"Indicative — for internal comparison"*) and in section 4.1 of the job report. A third place is friction, not safety. | The suffix is gone. **The name now carries exactly one fact — whether this file may reach a customer** — because that is an EXPORT GATE and not a caveat: a released quote is `_quote.html`, an unreleased one is `_quote_PORTAL.html`, whichever readers ran. The console still tells the operator the run read the pack with the vision model alone and where to read which readers ran. | generic — every quotation | `client_quote_html.generate_quote_files` | `this commit` | `test_a_measurement_run_writes_a_marked_quote` updated **on instruction, with the instruction quoted in the docstring**: the filename must NOT carry LLM-ONLY, and must still distinguish released from unreleased | implemented |
| D-173 | 22 Sep 2026 | James Gray, setting the split between the two paths: "**Main model: leave the assembler... Do not run concept-kind mapping over a measured DXF. If you point the render assembler at a real pack, you will flatten a weldment into one 5 mm panel again. That is the defect we just saw.**" | every --llm-only run | **THE GATE HAD A HOLE THE SIZE OF A DRAWING PACK.** The concept read fired on `--llm-only AND (a render pack **OR nothing came out**)`, and the second half is the danger: a REAL pack — flats, models, title blocks — whose BOM read happened to return nothing would have been handed to the render assembler and **guessed at from a picture**, with measured geometry sitting unread in the same folder. Nothing would have failed; the book would have carried sighted panels wearing a measured pack's name. | `why_not_sightable()` is an **absolute refusal on measured CAD**, whatever else is true: a `.dxf/.dwg/.sldprt/.sldasm/.slddrw/.step/.stp` anywhere in the pack, a DXF-sourced scan, or any part already carrying a measured flat or SolidWorks geometry. **A drawing pack that produced no parts is a READER FAILURE**, and the honest output is that failure — not a picture-guess. Decided **before** the try, because a deliberate refusal reported as a crash is a different fact; and **failing closed**, because the one thing worse than refusing a pack we could have sighted is sighting over a pack we could have measured. | generic — the boundary between the two paths | `concept_scan.why_not_sightable`, `file_scan` (refusal wired ahead of the read) | `this commit` | two more in `test_a_render_is_estimated_by_sighting_its_parts` (27 total): every measured signal refuses — by file, by scan format, by a flat on a part, by SolidWorks geometry — **and the control, that a render pack is still sightable**; plus that the refusal is decided before the read and fails closed | implemented |
| D-172 | 22 Sep 2026 | James Gray: "**Is it the Grok prompt or the assembling of the data returned by grok or both? We need to get both correct**" — and "**Never nest a caster**" | Plan A bin, generic | **BOTH, AND HE WAS RIGHT THAT EITHER ALONE CHANGES NOTHING.** D-171 fixed the prompt; measured immediately afterwards, the parts still costed **£0 labour** — *"Prompt without assembler = nicer JSON, same £102.70."* The assembler was forcing a render record through the drawing pipeline: **a castor came back as a 75×75 envelope, was written as a BLANK, and nested 338-up on a 2500×1250 sheet.** A blank is an instruction to nest, and nesting a bought item is how a sheet of wheels gets priced. | **The mapper refuses illegal kinds rather than relying on the prompt asking nicely.** A `bought_in` or `graphic` line NEVER receives a blank, whatever size the model returns — the size is kept as `concept_sighted_size_mm` (it is a real observation) and refused as geometry, with one line saying why. A fabricated panel still nests, or there would be no material cost at all. **Measured end to end on the fixture: material £27.80 + labour £81.45 = £109.25 for the product**, against £5.78 board and £0 labour before — seven panels each carrying `saw` / `cnc_routing` / `edge_banding` / `glue` / `wet_spray` / `hole_machining`, and the three bought-in lines carrying none. | generic | `concept_scan.parts_from_concept` (kind guard, `_positive`) | `this commit` | three more in `test_a_render_is_estimated_by_sighting_its_parts` (25 total): a castor is not nested **even when the model sizes it** and the size survives as a note; an applied graphic is not nested either; **and the control — a fabricated panel still gets its blank**, because a guard that refuses everything is not a guard | implemented — **the remaining £0 lines are PRICE gaps, not shape gaps**: MFMDF sheet price is unset by design (`BOARD_SHEET_PRICE_GBP['MFMDF'][18.0]['gbp'] is None` — prices are not hard-coded), and the bought-in lines need a catalogue or evidenced figure. Both are owned estimator actions, which is the D-153 rule working |
| D-171 | 22 Sep 2026 | James Gray, on the first render deliverables: "**You have a noun list, not a credible BOM or route... A route that charges nothing is not a route... The image already showed enough to write that list by hand; the engine did not.**" | Plan A bin, generic | **THE SIGHTING CONTRACT ASKED THE WRONG QUESTION.** It said *"name the parts you can SEE"* and got exactly that: four nouns — header, cabinet, lid, castors — with the **whole carcass as one 900×450 blank** and **no operations at all**. The compiler had nothing to mint but a generic `assembly` on each leaf part, correctly ruled every one out, and the labour column came to **£0.00**. The £102.70 was two market-guessed freight lines (£87) on parts that cost nothing to make. A list of visible objects is not a bill of materials, and a route with no charged row is not a route. | **A MAKE LIST, NOT A NOUN LIST.** The prompt now demands every line be something CUT or BOUGHT with a quantity per unit, and says the part that was missing outright: *an enclosure is NOT one part — a cabinet is its PANELS, front, two sides, back, base, top, each its own line with its own blank*. Fittings whose effect is visible are lines too (a lid that lifts has a hinge). **And every made line names its work**, from a fixed vocabulary (`SIGHTABLE_OPERATIONS`) where each word resolves to a real department on the rate card — a word outside it mints nothing and charges nothing *silently*, so it is dropped and **said**. Operations are emitted as `inferred_operations`, the honest field: they ARE inferred, from a picture, and the route report says `inference` against each. Unit-level work (`bench_work`, `assembly` — assemble the carcass, fit the lid and castors, pack) rides on the top assembly, charged once rather than once per panel. A **fabricated** line with no work says so; a bought-in castor and an applied graphic correctly carry none. | generic | `concept_scan` (prompt, `SIGHTABLE_OPERATIONS`, `unit_operations`, `concept_kind`, operation emission), `file_scan` (unit assembly event) | `this commit` | eight more in `test_a_render_is_estimated_by_sighting_its_parts` (22 total): the carcass is panels and no line is named CABINET; **every fabricated part carries at least one operation and every operation resolves to a department**; an off-vocabulary word is dropped AND flagged; the unit has work of its own; a made part with no work says so; a bought-in needs none. Plus two that protect the mechanism: **every word offered to the model resolves to a department**, and **the prompt cannot change without its cache version** — a pinned hash, because a prompt edit without a version bump replays the old answer for ever and the whole rewrite would reach nothing | implemented — **re-run the Plan A pack: expect a panel-level BOM and a charged route** |
| D-170 | 22 Sep 2026 | James Gray, on the first Plan A render deliverables: "**Quote needs to have a price — even if a bad one since we know it's only indicative... it keeps being over ridden.**" | every estimate | **ONE REPORT GAVE BOTH ANSWERS, TWENTY LINES APART.** Its headline read *"Unit cost PENDING — NOT TRACEABLE TO A WORKBOOK CELL"*; its own Q&A read *"What does a unit cost? **£102.70** — material £92.78 + labour £0.00"*. The quote took the refusal and printed a dash. `_price_fact` asked `publishable_total` whether the number could be traced to a signed-off cell, got "no", and returned `amount: None` — **discarding the figure instead of labelling it**, so every surface downstream, including the estimator's own `_quote_LLM-ONLY.html`, had nothing left to show. A dash where the workbook says £102.70 protects nobody; it sends the estimator to a second document to read the number this one is about. | **`workbook_amount` is carried through the refusal**, and the internal pages (`_quote_PORTAL`, `_quote_LLM-ONLY`) print it captioned *"indicative, from the workbook"* with one action line — *"has not been traced to a signed-off cell; settle the open lines and regenerate before it is quoted."* **The customer document is untouched and still fails closed.** And the refusal is now three states, not two: **untraced** (the check ran, found no cell, nothing disagrees) shows internally; **contradicted** (the cited cell holds a different figure — the 401912-02 case at 149.87 against 321.88) shows **nowhere**, because there is no "the workbook's figure" when there are two; **check threw** shows **nowhere**, because a check that failed said nothing at all and a figure on that basis invents the evidence we just failed to obtain. | generic — every estimate | `quote_state._price_fact`, `client_quote_html` (internal-audience branch) | `this commit` | new `test_an_internal_quote_shows_the_figure_the_workbook_holds` (8 cases: the figure survives the refusal; the internal page prints it marked up once and says what it is; the customer path still raises `NotReleasable`; a workbook with no figure still asks for one; no warning block). **Three existing guard-rails caught the change** and two were correct as written — the contradicted and check-threw cases were tightened rather than relaxed; the third was narrowed **on instruction**, with the instruction quoted in its docstring | implemented — **re-run the Plan A pack: the quote will carry £102.70 × markup, labelled** |
| D-169 | 22 Sep 2026 | The FIRST LIVE RENDER RUN on the Plan A bin: **"Found 1 drawing file(s)."** then **"Folder-as-job: 0 job folder(s) from 1 file(s)."** — a summary filed, **no workbook**, exit 0 | Plan A bin enquiry, generic | **THE PACK BECAME EMPTY BETWEEN TWO FUNCTIONS.** D-167 taught `config.SUPPORTED_EXTENSIONS` about renders, so `list_input_files` discovered the PNG — and `group_input_files_by_folder` still filtered to `.pdf` alone and dropped it. Every stage after that point already handled renders (`scan_folder_job` wraps them); **the one gate that had not been told was the one between discovery and the job**. Nothing failed, nothing raised: the run reported success over an estimate of nothing, and the only thing that noticed was the collector at the very end — *"NO WORKBOOK... the engine wrote a summary and no estimate"*. | Grouping accepts `.pdf` **and** image renders, so a discovered render is still there when the job is assembled. **And the disagreement itself is now a refusal**: files found but none grouped prints every file it could not place and exits — `raise SystemExit(2)`, not `return 2`, because `main()` is annotated `-> None` and called for its side effects, so a returned code is discarded and the process exits 0, which is precisely the failure being fixed. Discovery and grouping disagreeing is a defect in this engine, never a fact about the pack. | generic | `file_scan.group_input_files_by_folder`, `main.py` (the guard) | `this commit` | two in `test_a_render_is_estimated_by_sighting_its_parts`: a staged render survives **discovery AND grouping** (the join is where the defect lived, so both halves are asserted); and the guard exists, names what it could not place, and exits non-zero rather than returning | implemented — **re-run the Plan A pack** |
| D-168 | 22 Sep 2026 | James Gray: "**we need to build in the pipeline to populate the pricing s/sheet from the LLM only model**" | Plan A bin enquiry, generic | **"LLM SCAN ONLY" ON THE ENQUIRY PANEL PRODUCED NO WORKBOOK, AND NOTHING SAID SO.** The multi-drawing batch set `wants_engine=False` for `method="llm"`: the fast text read ran on the service, the run was marked **done**, and no estimate workbook was ever filed — built when "llm" meant a comparison figure, silently under-delivering once it meant an estimate. The Job panel's identical-looking mode queued a real `--llm-only` run all along: **two buttons with one label and two meanings.** | The batch path now queues the runner job with `llm_only=True`, exactly as the single-pack path does: `main.py --llm-only` reads the pack with the vision model alone (a render pack gets the concept read, D-167) and the ordinary waterfall fills the workbook, wearing its LLM-ONLY label on every deliverable. The fast figure still lands on the row in seconds. A runner-less llm enquiry is still **accepted** — its scans answer today, its workbook runs queue until a runner connects, and each run's log says so. A scan no longer finishes a drawing; **the workbook does.** | generic | `estimate_routes` (batch route, `_scan_one` gate, `wants_engine` comment, run lines), portal hint text | `this commit` | two reversed tests in `test_a_hundred_drawings_are_a_hundred_enquiries` that **name the instruction that reversed them**: the runner claims an llm-only drawing and the claim carries `llm_only`; the scan alone finishes nothing and the runner's completion finishes it. `test_the_llm_read_is_one_word…` now guards the new rule | implemented |
| D-167 | 22 Sep 2026 | James Gray, with two renders of an M&S Plan A collection bin: "**Can we run an LLM AI scan only on this...**" and "**WE HAVE THE add PDFs button. we should make this recognise this file format type also**" | Plan A bin enquiry, generic | **A CUSTOMER RENDER COULD NOT ENTER THE PIPELINE AT ALL.** Staging refused a PNG as "not a drawing file"; wrapped by hand, the drawing readers correctly found nothing — no text layer, no title block, no BOM — and an --llm-only run produced an **empty book with no line saying why**. The strict BOM vision prompt ("only what is VISIBLY in a BOM table; do NOT infer") is right for drawings and returns nothing on a visual, correctly. | **(1) Images are inputs**: `file_scan` wraps one image as a one-page PDF, losslessly, content-keyed, into the output tree — never beside the source, where it would double the job on a rescan. Folder jobs read them pooled. **A render is a drawing only when somebody says it is**: staged when selected by name, skipped with a note when swept in with a folder, and excluded from enquiry folder discovery — a job folder's site photos are not parts. **(2) The concept read** (`concept_scan.py`): on an --llm-only run whose pack yielded no parts, ALL pages go to the vision model in ONE call (two renders are one product, not two carcasses) and it names what it can SEE — parts, sighted materials, envelopes scaled from named cues — **never a price, and the schema has nowhere for one to land**. Parts are written through `apply_field` at new rank `vision_concept` (20, with the inferences; length/width share one source so the D-152 pair rule holds), each carrying its confirm-this action, then **priced by the ordinary waterfall**. Offline or key-less, it **refuses by name**: "this book is empty because nothing was read, not because the pack has no parts" — D-157's lesson, kept. Same isolated-call + content-keyed cache shape as the BOM vision reader, so an unchanged pack never asks twice. | generic | `config.IMAGE_RENDER_EXTENSIONS`, `file_scan` (`image_as_pdf`, dispatch, folder wrap, concept hook), `concept_scan.py` (new), `source_precedence` (`vision_concept` rank + display name), `staging` (explicit-only images), `enquiry` (images out of folder discovery), `llm_scan_price` wording, portal button + hint, `README` switches | `this commit` | `test_a_render_is_estimated_by_sighting_its_parts` — 11 tests against a **fixture answer for the real Plan A pack**: the wrap is lossless, content-keyed and never beside the source; a selected render stages while a folder's stray photo does not; stamps, rank, pair rule; a sighted castor is BOUGHT_IN not £0 (D-153); **a sighted part enters `estimate_part` and comes back owned, never `customer_supplied`**; a poisoned answer volunteering prices changes nothing; a missing key refuses loudly; a cached answer never re-asks. Plus three guard-rails that caught real gaps: `vision_concept` had no display name, three switches were undocumented, and a photos-only folder must still read as empty | implemented — **run the Plan A pack from the portal on LLM scan only; expect a workbook of sighted parts with confirm actions, and the price gates holding it portal-only** |
| D-166 | 21 Sep 2026 | The installer's `-WhatIf` on the logistics machine: every check green, *"runs at 06:30 daily, as SDI\\james.gray"* | logistics | **THE TASK WOULD RUN ONLY WHILE HE IS LOGGED ON, AND NOTHING SAID SO.** `Register-ScheduledTask -User` with no password registers an **interactive-only** task: it runs while that user has a session and otherwise waits for the next logon. On a desktop used every morning that is workable — `StartWhenAvailable` fires it at logon; on one that is not, 06:30 passes silently every day. The unattended alternative that needs no password, S4U, **cannot reach a network share**, so it is no use for a job whose input and output are both on one. The choice is real and was being made by default, invisibly. | `-RunWhenLoggedOff` stores the account's password with the task — **prompted with `Read-Host -AsSecureString`, never a parameter**, because a command line is in the shell history — and the installer's summary states which mode it registered and what that means: *"REQUIRED — runs only while SDI\\james.gray is logged on; if 06:30 is missed it fires at the next logon."* The trade-off is written where it will be read: a stored password must be updated when it changes, or the task stops with one line in the task history. | generic | `Install-SplitScanTask.ps1` (`-RunWhenLoggedOff`, splatted `Register-ScheduledTask`, summary) | `this commit` | one in `test_a_user_can_split_the_days_scans_from_the_page`: the switch exists, no password parameter does, the prompt is a secure one, and the default's consequence is stated | implemented — **his choice to make before registering** |
| D-165 | 21 Sep 2026 | James Gray, after the first real write and an immediate re-run that skipped all four: "**it runs for the current day only and won't duplicate runs?**" | logistics | **THE TASK HAD NO WINDOW AT ALL, AND THE OBVIOUS WINDOW IS WRONG.** The installer passed no `--since`, so the first night would have OCR'd all 947 scans. And `--since 1` — what a person naturally runs by hand — is a **rolling 24 hours from the moment the task fires**: at 06:30 a scan made at 06:00 the previous morning is already outside it, and a machine off over a weekend loses three days silently. **Duplication is not the window's job and never was**: a scan already split is skipped by its ledger without being read (proven on the share: `4 scan(s) already done, skipped`), and a note re-scanned under a new filename is caught by its number in the output folder. The window decides what gets **looked at**, not what gets done twice. | The installer takes `-SinceDays`, **default 7**, and passes it to the task; the "try it now" line prints the task's own arguments rather than a different command. Seven catches a missed weekend and costs nothing, because everything already done inside it is skipped. Zero (everything) is available and deliberate: filing the back catalogue is a decision, taken by hand, once. | generic | `Install-SplitScanTask.ps1` (`-SinceDays`, arguments, summary) | `this commit` | one in `test_a_user_can_split_the_days_scans_from_the_page`: the default is 7 and the window reaches the task's arguments. **Proven on the share: eleven notes written, then an immediate re-run wrote nothing** | implemented |
| D-164 | 21 Sep 2026 | The D-163 re-run: the count line appeared at once — **and then nothing, again** | logistics | **SILENT WORK LOOKS LIKE A HANG, AND WAS READ AS ONE TWICE.** After the count line the job renders each page at 200 dpi and reads it four times over, which is a minute or two for a day's post and prints nothing while it does it. The first stall was real (D-163); this one was the OCR working — and from the console the two are the same picture, so the person watching drew the same conclusion and was wrong the second time for the right reason. | `run()` takes a `progress` callback and announces each scan before reading it — *"reading scan21092026.pdf (2 of 4) ..."*, flushed — and the front door wires it to the console. Cheap, and the difference between a job that is working and one that is not. | generic | `split_delivery_notes.run(progress=…)`, `main` | `this commit` | one in `test_a_user_can_split_the_days_scans_from_the_page`: the announcement **precedes** the read, in order, per scan, and reaches the console through `main()` | implemented |
| D-163 | 21 Sep 2026 | The re-run after D-162: **printed its two paths and sat there** | logistics | **THREE THOUSAND ROUND TRIPS BEFORE A PAGE WAS READ.** `scans_to_read` called `Path.resolve()` on every file — one call to the server each, 947 per listing — and the front door listed the folder **three times over**: `every`, `chosen`, then `run()` again to read the same answer. On an idle share that is a second or two, which is why it was invisible on the earlier runs and on the Linux box; on a busy server it is a run that looks hung. Nothing in a listing needs the file resolved: one `iterdir` cannot return the same file twice, and the output-folder check is on the **parent**, of which a non-recursive run has exactly one. | Dedupe on the path as given, case-folded; resolve each **distinct parent** once (a dictionary), and the output folder once. `recent()` is a **filter on the listing**, not a second listing, and `run()` takes the listing the front door already has. 947 files: two resolves, one listing. | generic | `split_delivery_notes` (`scans_to_read`, `recent`, `run(pdfs=…)`, `main`) | `this commit` | two in `test_a_user_can_split_the_days_scans_from_the_page`, **counted not trusted**: 40 files in one folder cost at most two `resolve` calls; the front door lists the folder exactly once through a full `--since` run | implemented — **the run that stalled may simply have been slow; re-run and it should reach the count line at once** |
| D-162 | 21 Sep 2026 | The first **working** run on the real share: 9 notes named, 3 pages to `_Unsorted`, and three `UnicodeDecodeError: 'charmap' codec can't decode byte 0x9d` tracebacks from `subprocess._readerthread` | logistics | **THREE PAGES WERE FILED AS "NOT A DELIVERY NOTE" BECAUSE THEIR TEXT NEVER ARRIVED.** Tesseract writes UTF-8. `subprocess.run(text=True)` decodes in the **locale codepage**, which on the logistics machine is cp1252 — and cp1252 has no character at 0x9d, the last byte of a curly quote (`”` is `e2 80 9d`). The reader thread died with a traceback, `stdout` came back empty, and a page with no text is, correctly, not a note. **Same-count coincidence made it look like the reader's judgment**: three tracebacks, three `_Unsorted` pages. The Linux box this was measured on is UTF-8 throughout, so 11/11 there said nothing about this. | `encoding="utf-8", errors="replace"` on the OCR subprocess: the encoding Tesseract actually writes, named, and a byte it still cannot place becomes U+FFFD rather than the loss of the page. | generic | `split_delivery_notes._tesseract` | `this commit` | one in `test_a_user_can_split_the_days_scans_from_the_page`, which **fakes what Windows does** — strict cp1252 unless an encoding is named — and feeds a `”`: the number and the quote must both come back. Fails on the old call | implemented — **re-run on the same four scans and compare: the three `_Unsorted` pages should mostly become notes** |
| D-161 | 21 Sep 2026 | The first run on the **real** share: **947 PDFs found**, then four identical `tesseract could not be run: [WinError 2]` lines — **and an exit code of 0** | logistics | **TWO FAULTS, AND THE SECOND IS MINE.** (1) `winget install UB-Mannheim.TesseractOCR` answers *"Found an existing package already installed"* while `tesseract` is *"not recognized"*: **that installer does not add itself to PATH**, and a PATH edit does not reach a shell that is already open — so the same command works or fails depending on which window it is typed in, and the remedy offered was to install software that was already installed. (2) `run()` catches per file so one corrupt PDF does not cost the day, which turned **a fact about the machine** into one error line per scan. On `--since 1` that was four lines; on the full folder it is **947 identical lines ending in "0 delivery note(s)" and a success code** — the same shape as D-157, arrived at from the opposite direction. | **Found rather than trusted to PATH**: `find_tesseract()` checks an explicit path, then `SDI_TESSERACT_PATH`, then PATH, then `C:\\Program Files\\Tesseract-OCR` and the rest — the same order and the same shape as `cad_inputs.find_converter` for the ODA converter, because two ways of finding an external tool is two places to fix. **And a missing tool is its own type.** `NoOCR` is raised, re-raised past the per-file catch, and checked **once before a single page is rendered**, exiting 2 with the install line, the new-shell warning and the setting. The task installer does the same and, where it finds tesseract off PATH, says so and prints the `.env` line rather than refusing — plus the warning that matters on a schedule: **a user-only PATH entry works by hand and does nothing as a task.** | generic | `split_delivery_notes` (`NoOCR`, `find_tesseract`, pre-flight, narrowed catch), `config.TESSERACT_PATH`, `Install-SplitScanTask.ps1`, `README` | `this commit` | four in `test_a_user_can_split_the_days_scans_from_the_page`: no OCR is said **once** and exits 2 naming winget, the new shell and the setting; OCR vanishing mid-run raises rather than reporting a good day; **one bad scan still does not cost the day** (the other half of the rule the narrowed catch could have lost); an explicit and a configured path both win over PATH | implemented |
| D-160 | 21 Sep 2026 | `(Get-PSDrive K).DisplayRoot` → **"A drive with the name 'K' does not exist."**, and `Win32_LogicalDisk` for `K:` → nothing | logistics | **THE GUESS SHOULD NEVER HAVE BEEN IN THE SOURCE FILE.** `DEFAULT_SOURCE = r"\\\\sdi-dc01\\shareddata$\\Logistics\\Scans"` was reasoned from two true facts — the estimating share **is** `\\\\sdi-dc01\\shareddata$` (`MANUAL_OVERRIDE_QUOTE_DIR` has used it all along) and the drive in Explorer reads `K:\\Logistics\\Scans` — and the conclusion was still wrong. D-159 stopped it *building* that tree; it left the guess sitting in two source files as the thing that runs when nobody passes a flag. **James Gray: "config needs to be in config files. not json files lying around and being copied manually around."** These are the folders the whole job depends on and they were the one setting not in the settings. | Both folders are `SDI_SCAN_SOURCE_DIR` / `SDI_SCAN_SPLIT_DIR` in the project's one `.env`, read through the project's one config module — the same loader, the same search order and the same "which file did I actually read" answer as every other setting, via a new `config.dot_env_path()`. **They have NO DEFAULT.** Unset, the splitter **exits 2 and names the setting, the `.env` that was actually loaded, and the line to put in it**, which is the honest answer to "where are the scans" when nobody has said. The installer's `-Source`/`-Out` default to empty and it passes neither flag, so there is exactly one place the folders live; it also stops `New-Item -Force`-ing the parents of a path it was asked to check, and refuses to create the **scanner's** folder at all. | generic | `config.SCAN_SOURCE_DIR`/`SCAN_SPLIT_DIR`/`dot_env_path`, `split_delivery_notes` (`_configured`, `_where_settings_live`, the unset guard), `Install-SplitScanTask.ps1`, `README` switch table | `this commit` | three in `test_a_user_can_split_the_days_scans_from_the_page`, **behavioural not source-grep** (the first attempt was a grep and tripped on its own explanatory prose, twice): unset yields **no** folder and the setting yields exactly that folder, through a real reload; an unset run exits 2 naming the setting and the `.env`; the installer's param block declares no path. Plus `test_every_switch_is_written_down`, which **caught both new switches being undocumented** | implemented — **the real UNC behind `K:` is still unknown.** `Get-PSDrive` and `Win32_LogicalDisk` both say `K:` is absent from that shell |
| D-159 | 21 Sep 2026 | The D-158 census, first run: **"it holds 0 file(s) and 1 folder(s) / first few: SplitScan\\"** | logistics | **THE JOB BUILT THE SHARE IT WAS TOLD TO CHECK.** `run()` opened with `out_dir.mkdir(parents=True, exist_ok=True)`, so the guessed UNC `\\\\sdi-dc01\\shareddata$\\Logistics\\Scans\\SplitScan` **created `Logistics`, then `Scans`, then `SplitScan`** on a share where none of them existed. Every run after that found its source folder present — it had just made it — empty, and correctly reported a quiet day. `Test-Path` returned **True**. `source.exists()` passed. **The front-door guard added in D-157 for precisely this could not fire, because the job had already answered its own question.** Three rounds of diagnosis went to `Path.glob`, to enumeration, to permissions; the folder was real and this code had made it. And the wrong guess was reached for because **`net use` printed "There are no entries in the list."** — which it does for any drive mapped by Group Policy or a logon script, i.e. most of them. | **It creates the output folder and never the tree above it.** `prepare_out` refuses when the folder above the output folder is absent, so a path with a typo in it is **refused rather than built** — which is the entire point of checking a path. The same refusal is at the front door, exiting 2 before anything is created. A scan folder holding **no files and only our own output folder** is reported as what it is: *"a path that was CREATED rather than found. The scanner has never written here."* And every place that gives up on a path now says how to get the real name of a mapped drive — `(Get-PSDrive K).DisplayRoot`, `Win32_LogicalDisk`'s `ProviderName` — **and says outright that an empty `net use` does not mean the drive is not mapped**, because that is the reading that produced the guess. | generic | `split_delivery_notes` (`prepare_out`, `_how_to_find_the_share`, `main`'s out-parent guard and census branch) | `this commit` | four in `test_a_user_can_split_the_days_scans_from_the_page`: `prepare_out` refuses a missing tree **and the tree is verifiably not there afterwards**, while the ordinary case still makes the folder; a scan folder holding only our output says so and points at the real share; the front door exits 2 without manufacturing the path; the drive-letter advice does not rest on `net use` | implemented — **the real UNC behind `K:` is still unknown; `DEFAULT_SOURCE`/`DEFAULT_OUT` remain a guess and are now refused rather than built.** Delete the `Logistics` tree this created on `shareddata$` |
| D-158 | 21 Sep 2026 | James Gray, on the real share: `Test-Path "\\\\sdi-dc01\\shareddata$\\Logistics\\Scans"` → **True**, and the run on the very next line → **"0 PDF(s) in the folder"** | logistics | **D-157 FIXED THE FRONT DOOR AND THE SAME SENTENCE CAME BACK ANYWAY.** The path exists, `source.exists()` passed, the guard printed nothing — and `glob("*.pdf")` returned nothing on a folder Explorer shows **948 items** in. `Path.glob` gives an empty list for a folder it cannot enumerate, for a folder whose files are one level down in date folders, and for a folder that is genuinely empty: **three different situations, one line of output, and the line says the folder is empty.** D-157 made the job name the folder it looked in; it still could not say what it found there. Reporting a day's post as an empty folder is the same class of failure as reporting a missing share as a clean run. | **The listing is asked for, not globbed.** `iterdir` **raises** where it cannot enumerate, and that refusal now reaches the console verbatim and **exits 2** — a permissions problem says "Access is denied" instead of "nothing to do". Where the folder lists and holds no PDFs, the run prints **what is actually in it**: file and folder counts, the suffixes it saw, the first few names, and — where there are subfolders — the `--recurse` flag that reads them. One listing and one suffix comparison also settles the case question properly: `*.pdf` and `*.PDF` are the same files on Windows, and there is now no second pattern to double them. `--recurse` excludes `SplitScan` and `_Unsorted` **by name**, so walking subfolders cannot walk into the output, and the skip ledger is keyed on the path within the scan folder so two date folders may each hold a `scan001.pdf`. | generic | `split_delivery_notes` (`folder_listing`, `folder_census`, `scans_to_read`, `_ledger_key`, `main`, `--recurse`), `estimate_routes.SplitScansRequest` | `this commit` | seven in `test_a_user_can_split_the_days_scans_from_the_page`: a folder that refuses to list raises rather than reading as empty, and the CLI exits 2 saying why; a folder with no PDFs prints its file count, its suffixes and the `--recurse` hint; `.PDF` is still a scan; a doubled listing yields one file; subfolders are reached only when asked for; `--recurse` still does not read back the output; two date folders do not share a ledger key | implemented — **this is the diagnostic; the next real run says which of the three it was** |
| D-157 | 21 Sep 2026 | The first live run on the logistics machine | logistics | **IT PRINTED "0 delivery note(s)" AND EXITED 0.** Nothing was wrong with the reading — **nothing had been read**. `Path.glob` on a folder that does not exist returns no files and no error, so a missing share, a typo, a VPN down, a task with no drive mapping and a permissions refusal all look **identical from inside the loop**: a clean run that found nothing. This file carries comments about that exact failure mode in three places — the UNC default, the task installer's pre-flight checks, the endpoint's error handling — and **did not guard its own front door**. | It says where it looked before it looks: source and out paths, the count of PDFs in the folder, and how many the `--since` window kept. A source that does not exist or is not a folder **exits 2 and names the path**, rather than succeeding at nothing. Where `--since` filtered everything out it says so and says what to do about it. | generic | `split_delivery_notes.main` | `this commit` | two in `test_a_user_can_split_the_days_scans_from_the_page`: a missing source exits 2 and names the folder; a normal run states the path, the file count and the window | implemented |
| D-156 | 21 Sep 2026 | James Gray, with the share open: "**input dir is : K:\\Logistics\\Scans... K:\\Logistics\\Scans\\SplitScan IS THE OUTPUT DIR**" | logistics | **TWO DEFECTS THE SCREENSHOTS SHOWED AND NO TEST HERE COULD HAVE.** (1) **THE OUTPUT FOLDER IS A CHILD OF THE INPUT FOLDER.** Everything the job writes lands in the folder it reads, and only non-recursive globbing stood between it and **eating its own output for ever** — every note re-split into a one-page note named after itself, on every run, until somebody looked. One line of code, unremarked. (2) **`*.pdf` AND `*.PDF` ARE THE SAME FILES ON WINDOWS.** Globbing both and adding the lists is correct on the Linux box this was written and measured on, and **doubles every file** on the machine it runs on: each scan read, OCR'd and split twice. And the folder holds **948 items going back months**, so the first run is hours, not minutes. | `scans_to_read` — the output folder is excluded **by name** so the guard survives somebody reaching for `rglob`; paths are resolved into a **set** so a case-insensitive filesystem cannot double them; and `--since DAYS` makes the first run tractable without weakening the rest. Both defaults are now the real UNC paths, in the CLI and the task installer. | generic | `split_delivery_notes` (`scans_to_read`, `DEFAULT_SOURCE`, `DEFAULT_OUT`), `Install-SplitScanTask.ps1` | `this commit` | four in `test_a_user_can_split_the_days_scans_from_the_page`: the nested output is not read back; a faked case-insensitive glob does not double a scan (this filesystem cannot show it); `--since` filters and no `--since` still takes everything; both defaults are UNC. **Re-run end to end with the output nested inside the input: 11 notes, then nothing** | implemented — needs Tesseract on the machine's PATH |
| D-155 | 21 Sep 2026 | James Gray: "**how do we make sure it doesn't re generate the same individual invoices again over and over?**" and "**we can add to the SDI Intelligence Apps with way of an end point... to start with I can run manually**" | logistics | **A LEDGER CAN DRIFT FROM THE SHARE AND EVERY WAY IT DOES IS SILENT.** The first cut recorded which scans it had split, keyed on name, size and mtime. That stops a re-split and nothing else: delete a note and it never comes back; restore the folder from backup and everything is filed twice; the record lives on a share that was offline at 06:30. **AND THE OUTPUT PATH WAS A MAPPED DRIVE** — `K:` belongs to a logged-on session, which a scheduled task and this service both lack, so `K:\\IT\\...` resolves to nothing, the job **creates a folder of that name on the local disk, writes the day's notes into it and exits 0**. | **The output folder IS the ledger**: a note is done when a file for it is there, matched on the NUMBER so a re-scan whose client line reads differently is still the same note. Skipping an unchanged scan stays for speed but **verifies its own claim** — skipped only while every note it produced is still present, so a deleted one returns on the next run and the others do not move. Output defaults to the **UNC** `\\\\sdi-dc01\\shareddata$\\IT\\DeliveryNotesOutput`, in both the CLI and the task installer. `POST /api/estimate/logistics/split-scans` runs **the same script** the task runs — a button and a nightly job that read a document differently is two answers to one question, and the one nobody watches is the one that goes wrong. | generic | `split_delivery_notes` (`already_filed`, verified skip, `DEFAULT_OUT`), `estimate_routes.logistics_split_scans`, `Install-SplitScanTask.ps1` | `this commit` | `test_a_user_can_split_the_days_scans_from_the_page` and three more in the splitter's own file: the default is UNC and the installer agrees with it; a folder outside the shares is refused; the route runs the task's script; a missing Tesseract is explained rather than echoed. **Proven on the real scans: 11 notes, then nothing, then one deleted and exactly that one returned** | implemented — needs the scan folder named and Tesseract on the machine's PATH |
| D-154 | 21 Sep 2026 | James Gray: "**We get a number of these each day and we want to have a process that runs once a day and splits each delivery note out to a new folder K:\\Logistics\\Scans\\SplitScan with a delivery note number as the file name and client name if we can find it**" | logistics, daily | **THE SCANS HAVE NO TEXT LAYER AT ALL** — every page of the four files sent is a photograph of a sheet of paper, so the whole job rests on OCR. **Two approaches were tried and MEASURED before the third**: whole-page text got **4 delivery note numbers out of 11** and client names like *"Daley Noah Mepene"*; word boxes with value-to-the-right got **3 out of 11**, because the anchor it hangs on (*"Delivery Note N"*) OCRs as *"Delivery Nots"*, *"Delivery Note |"* and *"Delivery Note l"* — the least reliable text on the page. Both failed for one reason: the header is **three columns**, so read as prose the label a value belongs to is gone. | **Fixed regions on SDI's own template, with a digits-only charset for the number**: 11/11 numbers, 11/11 clients, 9/11 page markers. Regions are **fractions of the page, not pixels**, so a re-scan at 300dpi still works. **The first question is "is this one of ours"**, never "what is the number": one of the four files is a **GB Lift Trucks LOLER examination report, scanned upside down**, and it carries a reference number, a date and a company address that would all parse — a delivery note filed under somebody's forklift inspection is never opened again. It goes to `_Unsorted` under its source and page. A page that reads as nothing is **retried at 180°**. The source scans are **never touched**; a small ledger keyed on name, size and mtime stops a re-split, and identical content is not a clash so **running twice is safe**. | generic — any day's batch | `tools/logistics/delivery_note_reader.py`, `split_delivery_notes.py`, `Install-SplitScanTask.ps1` | `this commit` | `test_a_days_scans_split_into_one_file_per_delivery_note` — **23 tests against REAL OCR of the real scans**, frozen as a fixture including everything Tesseract got wrong: *"Page 1 of 1"* as **"Page Lot 1"**, *"Pageloftl"*, *"Page lofi"* and *"rage ord"*; **TES01 as "Tes01"**; **MAR002 as "MAROO2"**. Each of those cost a version of the reader, and a hand-written fixture would have contained none of them | implemented — **end to end on the four real scans: 11 notes named and split, 1 page to _Unsorted, second run adds nothing**. Needs the source folder naming and Tesseract installed on the logistics machine |
| D-153 | 18 Sep 2026 | James Gray, on D-152 having only half-fixed it: "**we price everything. we dont drop something if it's obviously something that is part of the unit**" | every job | **A PART IN THE BUILD WAS BEING COSTED AT NOTHING BEFORE THE WATERFALL BEGAN.** `estimate_part` returned a complete costed part — nil material, nil labour, no operations, `costing_basis: customer_supplied` — for any part whose normalised material was `BOUGHT_IN`, `PAPER` or `PRINTED_PAPER`. **Not researched, not looked up, not offered to SDI Live.** D-152 took PAPER out of that set and **left BOUGHT_IN in**, on the reasoning that BOUGHT_IN had "been normalised to the customer supplies this". That is the same inference one step further back: **BOUGHT_IN is a SOURCING fact** — SDI buys this rather than makes it — which is the start of a price chain, not the end of one. A part can be bought in and still be ours to buy, and that is the ordinary case. | The gate is **removed entirely**. Every line goes down the bought-in price chain and its rungs like any other purchased item, and where nothing answers it is an **owned gap with one estimator action**, not a zero that sums as free. **FREE ISSUE STILL EXISTS AND IS STILL FREE** — it is a person's ruling with its own `nil_by_design` classification and its own sentence, which even before this ended *"if SDI is buying it, enter the rate"*: the wording of somebody who knew the assumption was sometimes wrong. It is recorded as a decision, not read off a material string. **Nothing else in 6,531 tests depended on that zero**, which is how load-bearing it was. | generic — every job | `estimator.estimate_part` | `this commit` | three cases in `test_a_blank_is_a_pair_not_two_numbers`, **behavioural**: no material name yields `customer_supplied`; a line nothing priced is `unpriced` and owned by the estimator and named in the release reasons; and free-issue still resolves to a ruled nil owned by nobody | implemented — **live money change**: lines that came out at £0 now carry a price or an action. Prove on the next rerun |
| D-152 | 18 Sep 2026 | James Gray, supplying the 0355255 pack: "**identify the source of the phantom blank; confirm each BOM/commercial line enters the precedence pipeline**" | 0355255, generic | **THE PHANTOM WAS NEVER THE DXF'S, WHICH IS WHY THREE FIXES TO THE DXF READER DID NOT CLOSE IT** (D-030, reopened twice). *"DXF flat 792 x 760.3mm is 377% of the model flat"* is **two parts, from two pages**: page 1 is the ACRYLIC L-STAND (381g) whose flat is 760.3 x 210 — **the DXF agrees exactly, 760.252 x 210** — and page 2 is the **GRAPHIC, MATERIAL: PAPER, WEIGHT: 3g, 792 x 210**. `source_precedence` writes one FIELD at a time and each wins its own arbitration, so `blank_length_mm` was taken from the reading that won for length and `blank_width_mm` from the one that won for width. **The 377% is arithmetic on a pair that was never measured off anything**, and `_dxf_blank_mm` — which answers the general "what is this part's blank" — handed it to a caller that prints *DXF flat* in every sentence. **A wrong citation sent three people to the right module for the wrong reason.** AND THE SECOND PART OF THE SAME PACK NEVER REACHED A PRICING RUNG: `PAPER` and `PRINTED_PAPER` sat in a set beside `BOUGHT_IN` and took its rule, so **any printed line short-circuited to £0.00 `customer_supplied` before the waterfall began**. | **A blank is a pair.** `document_builder.flat_blank_mm` refuses a length and width whose **recorded sources differ** — narrow on purpose: where neither is stamped the pair is taken as it always was, because refusing those would throw away every blank the engine holds. And `_dxf_blank_mm` returns **only a DXF-sourced pair**, so the arbitration says *"no DXF blank could be measured"* — true, and the sentence that would have ended the hunt on the first reading. **A material name is not a decision**: `BOUGHT_IN` keeps its meaning (it HAS been normalised to "the customer supplies this"), and a printed line goes down the ordinary waterfall, with one estimator action where nothing prices it. The three earlier DXF fixes are correct and stay. | generic | `document_builder.flat_blank_mm` (+`_one_reading`), `source_connectors.solidworks._dxf_blank_mm`, `estimator` (customer-supplied gate — **removed entirely in D-153**) | `this commit` | `test_a_blank_is_a_pair_not_two_numbers` — the phantom pair refused, a one-reader pair kept, an unstamped record untouched, the DXF's own path unchanged, the citation gated, and **the three earlier fixes asserted against the real GA and flat files** so nobody goes back to them a fourth time | implemented — **D-030 closed**; the money consequence of pricing printed lines is a live change to flag on the next rerun |
| D-151 | 18 Sep 2026 | James Gray: "**we need to still price everything and we have our pipeline precedence model... I ALSO DO not want massive do not give price to client and that sort of a thing**" and the four-rule statement that followed | every estimate | **TWO FAULTS, AND THE SECOND IS THE SIXTH TIME.** (1) **THE PIPELINE DID NOT REACH THE COMMERCIAL LINES.** Packaging and delivery went to `_ask_market` behind `COMMERCIAL_LINE_ASK_MARKET`, **which is `False`** — so in practice they were never researched at all, and were held at £0 with a four-line note naming a config key and a *5.7x* anecdote, while every other line in the engine went to `indicative_price`. The objective is *"a fully priced estimate, using the precedence pipeline — not a polished list of missing prices"*. (2) **THE QUOTE PAGE HAD GROWN A WARNING BLOCK AGAIN** — a PORTAL VIEW panel with a bulleted catalogue of everything outstanding. That is the red LLM-only band, the invariant banner, *DRAFT — not for issue*, the undrawn-parts gap list and PRICE PENDING, **six removals of the same thing**. Every one accurate; every one telling a professional what is wrong with a document instead of what to do next. | (1) Commercial lines go through **the same rung 4 as every other line** — house rate first, then `indicative_price` with `_commercial_researcher` behind it. **The reason the old ask was turned off is answered by what replaces it**: `resolve_indicative` refuses a figure that does not carry its source, the date it was true, what it is per and the quantity it was found at, so the 5.7x spread either fails the contract or arrives with the evidence to see why — and an estimator can check the first and could only re-do the second. (2) The page carries **one line** — *"Enter the unit cost on the Estimate sheet and regenerate"* — and **nothing else**: no PRICE PENDING, no PORTAL VIEW, no catalogue, no caveat. A **priced** estimate awaiting sign-off shows its price and says nothing at all. The safety is unchanged and is where it always was — the file is not named, served or attached as the customer's document, none of which needs a sentence on the page. | generic — every estimate | `commercial_lines._line` + `_commercial_researcher`, `client_quote_html` (action line, CSS, print block) | `this commit` | `test_a_quote_carries_the_cost_of_getting_it_there` reworked: the pipeline **reaches** a commercial line; the house rate is still asked first; a researched figure **without its evidence is refused**; the unpriced line is one action with the measured consignment. Four banner tests now assert the warning block is **absent** and name the six occasions | implemented — verify on the next rerun |
| D-150 | 18 Sep 2026 | James Gray: "**commercial inputs should ultimately record a source/reference alongside the value—not only `name=value`—so a completed input remains traceable to SDI Live, a supplier quote, or evidenced research**" | every quotation | **THE PRICING WATERFALL REACHING THE LAST FIELDS THAT ESCAPED IT.** Every other number on the estimate names where it came from — material lines carry the system that answered, totals carry their workbook cell, labour carries the department rate card. The commercial inputs were the one place a figure could be **typed and released with nothing behind it**, and they are precisely the figures with **no drawing to check them against**, which makes the reference the only evidence there will ever be. | An input completes only with a value **and** a source; `margin = 0.25 @ SDI standard for this customer` on the form. A figure with no source blocks release and **says so in its own sentence** — *"these commercial inputs carry a figure with no source: delivery — name SDI Live, the supplier quote or the evidenced research behind it"* — because *"still open"* would send somebody looking for a figure that is already there. The older bare `{"margin": 0.25}` record still reads; it simply reports as unsourced, which is the thing being asked for. | generic — every quotation | `quote_state.commercial_inputs`, `sdi-estimating-intelligence.html` (parser, placeholder) | `this commit` | three cases in `test_the_portal_can_release_a_quote_the_engine_then_issues` (unsourced blocks and names the input; no-figure says something different; sourced completes) and the page parser's own | implemented — **release/output work parked here**; next: BOMs, routing, pricing, quantity |
| D-149 | 18 Sep 2026 | James Gray: "**stop bought-in items such as tape inheriting the assembly's steel material on any output**" | every job | **THE TITLE BLOCK DESCRIBES THE DRAWING, NOT EVERY ROW ON IT.** A sheet states one material and the engine stamps it onto the parts it found there — right for the fabricated leaves, which ARE that material, and wrong for every bought-in on the same parts table. 401912-02's BOM lists *"25.4mm ADHESIVE MAGNETIC TAPE, L: 450mm"* and the record called it **MILD STEEL**, read off the GA title block, which states what the DIVIDER is made of; on 0355255 the same line read **ACRYLIC**. **Nothing was mis-costed by it** — the tape is priced as a bought-in and never touched the steel route — **and that is why it survived**: no total moved, so no check fired. And it reached the quotation's Material line, the report's part streams (counted as a *Sheet steel* part), the workbook's material column and the SQL export, each reading `normalized_material` raw. **One surface already knew** — `estimate_explained._material_stated` had the rule and applied it to one column. | `display_material` — the same shape as `fold_count` and `displayed_charge`, which this session has now paid for three times. A bought-in's material is printed **only when it was stated about that part**, judged on two signals **already on the record and never used for this**: `material_source` (a sheet-level reader — title block, overall dims, DXF filename, BOM tree, engine inference — is describing the drawing, not the row) and `page_roles` (**a bought-in with its own detail page has its own title block**, which is the rule the one surface that knew was using). Otherwise it reads **— (bought-in)**, not a guess: a bought-in is BOUGHT, what it is made of does not price it, and the drawing office's description is still on the line. The inherited reading is **kept on the record** with where it came from. Commercial lines, subcontract services and roll goods keep the labels `costed_facts` already gave them, now in the same place. | generic — every job | `display_material` (new), `costed_facts._material_label` (delegates), `client_quote_html._materials_line`, `job_report_html` (cost streams), `estimate_explained._material_stated` | `this commit` | `test_a_bought_in_is_not_made_of_the_assemblys_material` — **renders the documents**, not just the fact: the quotation not naming the tape's inherited material, the costed line's `material_label` (which the workbook, the report table and the explanation all read), the report's streams no longer counting it as steel. Plus the three ways a bought-in legitimately speaks for itself, and **the fabricated part left untouched** — if that fails, the fix has started deleting materials from parts that have them. **And a guard that `wb_populate` does NOT import it**: it reads the raw field to pick a £/kg rate, a sheet size and whether a part can be powder coated, and a reporting change and a costing change must not travel together | implemented |
| D-148 | 18 Sep 2026 | James Gray on `5e945cb`: "**The remaining work is usability and presentation: the portal release form**" | every quotation | **A GATE WITH A KEY NOBODY CAN REACH.** D-147 built `/release` and nothing called it, so releasing a job meant posting to an endpoint by hand. And a form on its own would have *appeared not to work*: the release record is read when a quote is **built**, so an estimator would authorise a job and go on looking at a page headed PORTAL VIEW with a send button that refuses — everything behaving correctly and the screen saying the opposite, which is how somebody concludes a control is broken and asks for it to be removed. **A separate hole found while wiring it:** `client_quote_html.main()` called `build_quote_html` directly and wrote `<stem>_quote.html` **unconditionally**, so a portal copy regenerated from the command line landed on the share under the released document's name — the naming rule held on every path but the one the rebuild would use. | The form records **a name and the unit cost being approved**, then rebuilds the quotation through the **engine's own generator** over `POST /quote/regenerate`, and **relabels the file on screen**, which the run's own deliverable list still named. **THE FIGURE IS TYPED, NOT PRE-FILLED** — what is authorised is a number, so the person authorising it states it; the record binds to what they typed, so a typo, a stale page or a workbook amended in between yields a quotation that stays in the portal. A double entry for the cost of one field. Commercial inputs are `name=value` lines and **a heading with no value stays outstanding** rather than being dropped. The CLI goes through `generate_quote_files` like everything else; `--out` became `--out-dir`, because the **filename is the engine's to choose** — it says which document this is. The quote's tick-box stays unticked by default but **no longer decides anything**: the route reads the document's own declaration. | generic — every quotation | `sdi-estimating-intelligence.html` (release form, rebuild, relabel, `.btn-quiet`), `estimate_routes` (`/quote/regenerate`), `client_quote_html.main` | `this commit` | `test_the_page_can_release_a_quotation_and_see_it_change` — the **whole loop through the real endpoints and the real generator**: portal copy → release → customer document with `content="customer"`; a release at a transposed figure leaving it in the portal; withdrawal putting it back; a summary outside the shares refused; and the CLI writing the engine's name and **not** `_quote.html` | implemented — `display_material` next |
| D-147 | 18 Sep 2026 | James Gray on `073cc40`: "**The portal must write the commercial-input completion and named release authorisation. Until then every quote correctly stays portal-only**" | every quotation | **D-145 BUILT THE GATE AND NOTHING COULD OPEN IT.** The engine read `commercial_inputs.complete` and `quote_release.authorised_by` from the summary and **nothing anywhere wrote either**, so every estimate produced a portal copy — correct, and permanently correct, which is a gate with no key and eventually a gate somebody removes. And the obvious place to put the record is the wrong one: the summary JSON is a **run artefact the engine rewrites whole**, so an authorisation written there is destroyed by the next estimate of the same job. | `POST /estimate/release` writes `<stem>_release.json` **beside the deliverables**; `release_record.apply_to_summary` merges it in at the one point a quote is built, and never over a decision already on the summary. **THE SIGNATURE IS BOUND TO WHAT IT SIGNED**: Dave authorises £149.87, a drawing is revised, the re-estimate reads £212.40 — and a record that says only *"Dave authorised this job"* releases the new figure on the old signature, with nobody having been careless. So the figure and cell are recorded and `quote_state` refuses the authorisation once the estimate says something else, naming both figures and the estimator; a move inside half a penny is two reads of one number and does not cost anybody their signature. The endpoint refuses an unnamed or unpriced release, and `/release/withdraw` returns a job to the portal. The writer is in the SERVICE and the reader in the ENGINE because **the two are not on the same machine** — that separation is why the runner exists — so the record's shape is the joint, not an import. | generic — every quotation | `src/release_record.py` (new), `quote_state.authorisation` (binding check), `client_quote_html.generate_quote_files`, `sdi-intelligence-backend/quote_release.py` (writer), `estimate_routes` (`/release`, `/release/withdraw`) | `this commit` | `test_the_portal_can_release_a_quote_the_engine_then_issues` — **written by the service, read by the engine**; the same job portal-only then releasable with nothing else changed; end to end through the file the engine writes (`_quote_PORTAL.html` → `_quote.html` with `content="customer"`); the re-estimate at a different price going **back** to the portal and at the same price staying released; withdrawal; and a corrupt record costing the run nothing | implemented — `display_material` next |
| D-146 | 18 Sep 2026 | James Gray on `073cc40`: "**Audit every customer delivery route—not only `main.py` email attachments. Print CSS and `_PORTAL` filenames deter misuse but are not the actual security boundary; download/share/export routes must enforce `customer_releasable`**" | every quotation that leaves the building | **THE AUDIT FOUND TWO OPEN ROUTES.** (1) `POST /estimate/{run_id}/email` **with an explicit file list from the page was not gated at all** — its code read *"An explicit choice is a decision, quote included"* and sent the list untouched, so a tick-box put a quotation in front of a customer past the only gate there was. That gate is `_looks_provisional`, a keyword scan of the run's console that ends in an unconditional `return True`: **nothing has leaked only because it holds everything**, and the moment it became honest there was no check behind it. (2) `GET /api/file` takes a path and returns a file, and a quotation is just another path to it. The filename and the print stylesheet are labels — worth keeping, and one Ctrl+P from irrelevant. | **The verdict travels inside the document.** The quotation declares its audience in its head (`<meta name="sdi-quote-release">`) at the moment the engine decides it, and every delivery route reads the file it is about to hand over — so the check is on the ARTEFACT, not on a record that refers to it, and a record can be stale, describe another run, or be missing, each of which ends in release. The service still reads no estimate: giving it one would put a second opinion about the same question on the far side of a network boundary. `choose_attachments` asks **before** `include_quote`, because that is a request and this is a permission; the explicit list is filtered and a selection with nothing sendable **409s rather than mailing an empty message**; `/api/file` serves an unreleased quotation **inline only, never as a download**, with the reason in a header — the estimator keeps their page because `portal_editable` is a promise, and it is stated plainly that a viewer can still Save As: **the boundary is that the system never hands one over.** Undeclared fails closed. | generic — every quotation | `quote_state.release_meta_tag`, `client_quote_html` (emits it), `sdi-intelligence-backend/quote_release.py` (new), `estimate_email.choose_attachments`, `estimate_routes.email_run`, `app.get_file` | `this commit` | `test_a_quote_declares_its_audience_to_the_routes_that_deliver_it` — **the joint between the two checkouts**: a real quotation rendered by the engine and parsed by the service, so the restated constant cannot drift; plus undeclared-fails-closed, non-quotes unaffected, the name deciding what to INSPECT and never what to send, and a route census that fails when a new `FileResponse` appears. Three tests in `test_a_finished_estimate_reaches_the_people_who_asked` now carry real declared files, having failed **closed** on bare paths | implemented — write-path next; **no quote is customer-sendable until it lands** |
| D-145 | 18 Sep 2026 | James Gray on `1d77273`: "**`PRICE PENDING` on a 'quotation' is still a customer-facing disclaimer... the portal needs an editable quote view, not an incomplete quote for a customer to see**" | every quotation | **A DISCLAIMER IS EVIDENCE THAT THE WRONG DOCUMENT IS BEING PRODUCED.** Every notice this page has carried and lost said the same thing in a different voice — the red LLM-only block, the invariant banner, *DRAFT — not for issue*, the undrawn-parts gap list, and last of all D-144's PRICE PENDING. Each was removed for the same reason and each came back somewhere else, because the page kept being asked to apologise for a state it should not have been in. **The state is the fixable part.** Separately, whether a quote may go out was FOUR conditions in three files — `release.draft`, `money_provenance`, the requested-quantity mismatch and the quote's own price check — none of which knew about the commercial inputs or about anybody authorising anything, so a quote could be attached to a note going out while both were still open. | `quote_state` — **one fact, six gates**: a traceable price, nothing open on the costed record, money that can be evidenced, the batch actually asked for, the commercial inputs **recorded** complete, and a **named** estimator's authorisation. `portal_editable` is a constant `True` because it is a promise; `customer_releasable` needs all six and **absent means no**. The incomplete page is the **PORTAL VIEW**, says what is outstanding in short estimator-facing lines, **does not print** (print-to-PDF is the one export no server-side gate sees, and it is replaced rather than watermarked), is named `_quote_PORTAL.html`, and is never attached to an email. `build_quote_html(audience=CUSTOMER)` raises `NotReleasable` rather than producing a page that has to explain itself. **The engine's reasoning still does not reach the page**: blocking items carry a full `what` for the log and report and a short `short` for the page, because the first cut listed `release.reasons` verbatim and the quotation acquired *the consistency checks have not run* and the part number of every unpriced line — the exact material the invariant banner was removed for, by a new door. | generic — every quotation | `quote_state` (new), `client_quote_html` (audience, portal render, print block, filename), `main.py` (attachment gate) | `this commit` | `test_a_quote_has_an_audience_before_it_has_a_price` — the control that a settled record IS releasable, then each of the six gates removed one at a time, the empty record failing closed on all of them, **the portal keeping its page in every case**, the customer document refused by name, and the print/filename/attachment halves. Three existing tests stopped being source greps or were pointed back at the customer document they were written to protect | implemented — D-144 closed; `display_material` next |
| D-144 | 18 Sep 2026 | James Gray on `2faf17d`: "**If that module/check raises, quote generation crashes entirely. That is fail-closed for price, but it violates your rule that a draft quote must still be generated in the portal for estimator editing.**" | every quotation | **FAILING CLOSED ON THE PRICE IS NOT FAILING CLOSED ON THE PAGE.** D-143 removed `except Exception: pass` — correctly, since it left the old price live when the guard broke — and removed the handling with it. A raising traceability check then took the whole quotation down, so an estimator had no draft to edit and no sight of what the pack priced, against the standing rule that **a draft quote may always be generated in the portal**. Three further faults were found by rendering it: the refused price showed as a bare em-dash, which reads as a page nobody finished and invites a figure typed in from memory; the reason printed on the quotation was `publishable_total`'s own — *"the proposed total (£149.87) does not match what Estimate!M105 holds (£321.88)"* — **two unpublishable figures and a cell reference on the one document that leaves the building**; and it called itself a *draft*, the internal word. Separately, the internal report computed `unit_withheld_why` and **rendered it nowhere**, so an estimator was told PENDING and not which of the three reasons it was. | The quote catches, **refuses the figure and keeps the page**: specification, quantity, scope and included operations all render, the box reads **PRICE PENDING** with the caption *awaiting a traceable price*, and a note says the price follows once the estimate is confirmed — **no figures, no cell, no engine wording, and not the word draft**. The reason itself goes where it is actionable: the job report's unit-cost note now prints *Not publishable — …*, naming both numbers and the cell. | generic — every quotation | `client_quote_html` (handler, pending render, CSS), `job_report_html._render_headline` | `this commit` | `test_a_broken_price_check_still_leaves_a_usable_quote` — makes `publishable_total` RAISE and renders the document: HTML still generated and the job still identified; **no currency figure anywhere on the page** (regex over every £ amount, not a wording check); visibly pending traceability; and no engine internal leaked. Plus the control that the same fixture prices normally. And `test_the_quote_fails_closed_when_the_check_cannot_run` **stopped being a source grep** — it asserted the string `except Exception` was absent near the call, which is a check on spelling, and it failed against the correct fix | implemented — D-143 closed; `display_material` next |
| D-143 | 18 Sep 2026 | James Gray on `8bf6293`: "**Quote generation fails open**" and "**Amount and cell are still paired from different records without proving they agree**" | every published total | **(1) THE GUARD FAILED OPEN.** The quote's traceability check sat in `except Exception: pass`, which leaves `unit_cost` SET when the check itself breaks — **the one failure mode a guard exists for**, on the page a customer keeps. **(2) THE AMOUNT AND THE CELL CAME FROM DIFFERENT RECORDS AND NOTHING PROVED THEY AGREED.** The figure arrived from `workbook_equivalent_pricing.m105_total_unit_cost_gbp`, the citation from `final_estimate.totals.unit_cell`, paired by a renderer and vouched for by nobody: the fact checked only that a cell EXISTED. So *"£149.87, Estimate!M105"* could be published without anything establishing that M105 holds £149.87. **That is worse than the untraceable figure it replaced** — an unsourced number invites checking, and a wrong citation stops it. | (1) a check that cannot run **refuses the price** — first by removing the handler altogether, which D-144 then corrected, because that fails closed on the DOCUMENT too. (2) **the cell and its value are one read**. Every reader that locates the total now records what it found there in the same pass — the COM read-back (`unit_cell_value` off the same cell it scanned), the openpyxl label scan, and the quote-regen reader. `publishable_total` **refuses unless a proposed amount agrees with the recorded cell value** (0.5p), and the refusal **names both figures and the cell**, because a disagreement is a real fault in the read-back or the pricing and an estimator can only chase it if the page says which two numbers differ. | generic — every published total | `displayed_charge.publishable_total`; `wep_readback_from_xlsx`; `estimate_explained._sheet_totals`; `client_quote_regen`; `client_quote_html`; `job_report_html` | `this commit` | 6 more tests, **rendered not source-checked**, as specified: a quote with no traceable total carries no customer price; a quote whose cell DISAGREES carries neither figure; the report refuses the same way; the refusal names both numbers; and the control that a verified total still prices — **a guard that refuses everything is not a guard**. Four fixtures carry the pair, modelling readers that now record it | implemented — D-142 closed; `display_material` next |
| D-142 | 18 Sep 2026 | James Gray: "**the report and quote still bypass it for their headline totals**" and "**`pending_traceability` has no explicit renderer branch — it falls through to a dash**" | every published document | **THE FIVE-SURFACES SHAPE, INSIDE ONE FILE.** `job_report_html` printed raw `hl["unit"]` in its **header, summary, verdict and footer** — four sites each deciding for itself — while the covering email had already been put behind `publishable_total`. `client_quote_html` had its own direct path from `m105_total_unit_cost_gbp` to a marked-up price, and **a quote is the one deliverable a customer keeps**: the last place an untraceable figure should be allowed. **And `pending_traceability` fell through to a plain dash**, so a line where the sheet DID calculate a figure — with only the record of WHICH CELL missing — looked like an empty row. That is the one presentation under which nobody would ever go and fix it. | The decision is made **once**, in `_extract_headline` where `hl` is built, and `_unit_text(hl)` is what all six report sites print — a figure, or *"PENDING — NOT TRACEABLE TO A WORKBOOK CELL"*. The quote asks `publishable_total` before it marks anything up; where the cost cannot be traced it computes no unit price and renders its own missing-price path rather than a confident figure resting on nothing. **`client_quote_regen` records the cell it read the price from** — it scans for the price by LABEL, so it has always known it — which is why the regenerated-quote tests needed no fixture changes: the path supplies its own traceability. The report's pending-traceability branch says **"PENDING — WORKBOOK CELL NOT RECORDED"**, and the three pending states (a price, its traceability, nothing at all) are distinguishable because they send a reader to different people. | generic — every published document | `job_report_html._unit_text` + 6 sites; `client_quote_html`; `client_quote_regen._find_value_in_row` / `read_estimate_figures` | `this commit` | 7 more tests in `tests/test_a_published_document_carries_no_untraceable_money.py`. The site-coverage test strips **docstrings as well as `#` lines** — `_unit_text`'s own docstring names the pattern it replaced, and the first cut matched it: the seventh time in this suite a text search has found prose about a thing rather than the thing | implemented — D-141 closed; `display_material` next |
| D-141 | 18 Sep 2026 | James Gray on `b64425d`: **three P1 contradictions and one policy mismatch** — "I would not accept it as complete yet" | every published document | **(1) THE REPORT STILL RENDERED A PENDING ENGINE FIGURE AS CURRENCY.** `_render_bom_tree` printed *"pending a price (engine: £4.40)"* — an engine-only amount in pounds, from the branch I wrote to honour the rule. **(2) THE EMAIL HEADLINE STILL PUBLISHED AN UNTRACEABLE TOTAL.** It called `publishable_total` and then printed `totals["unit"]` regardless, using the fact only to decide whether to append a cell — **the refusal reduced to a formatting preference**. **(3) THE "RENDERED FIXTURE" RENDERED NEITHER DOCUMENT**: it called `_unpriced_section()` alone and source-grepped the covering-email module, so it could not see either defect above. That is the "test the source, not the deliverable" weakness **in the very file written to avoid it**. **(4) `displayed_charge` published a charge with no `charged_cell`**, against the stated rule. | (1) the report says **"PENDING A CURRENT PRICE"** and no figure; the diagnostic stays on the record for debugging and reaches no page. (2) the headline prints `_job_total["amount"]` or **"UNIT COST PENDING"** with the reason — and the fixture immediately caught the SAME total published a second time four lines below, in section 1's table, which now asks the fact too. (3) the fixture calls **`build_report_html()` and `covering_email()`** and reads their rendered text for £3.88, £4.40 and £321.88. (4) a charge with no recorded cell is **`pending_traceability`**, not workbook money. `_sheet_totals` records the unit cell where its label scan finds it — the same principle as the read-back, in the reader that runs when Excel does not. | generic — every published document | `job_report_html._render_bom_tree`; `estimate_explained` (headline, section 1, `_sheet_totals`); `displayed_charge` (`PENDING_TRACEABILITY`) | `this commit` | `tests/test_a_published_document_carries_no_untraceable_money.py` rewritten to render both documents in full. Six fixtures updated to carry `charged_cell` / `unit_cell` — they model a read-back, and the read-back now records them; the rule biting them **is** the rule working | implemented. **Remaining asymmetry, flagged not hidden**: the report's and quote's own headline totals still publish without the traceability check the email now applies |
| D-140 | 18 Sep 2026 | James Gray: "**wire the covering email's line-level output to `displayed_charge`; that is the correct next task**" and the acceptance test he specified | every published document | **THE COVERING NOTE WAS THE LAST RENDERER DERIVING MONEY FOR ITSELF.** Its outstanding-items table multiplied `row.price x row.qty` and printed the product; its labour table printed `Estimate!{row}` with no column, sending a reader to fourteen columns to find the money. Both are the habit that put £3.88 on five pages — a renderer answering a question the record already answers. | The table asks `displayed_charge` and prints the sheet's figure WITH ITS CELL; a line the sheet never charged reads *"not yet priced on the sheet"* and carries no currency. The labour rows print the recorded cell where the read-back stamped one. **`displayed_charge` accepts either shape of row** — a costed line's `charged_ext_gbp` or a read-back row's `total_value_gbp` — because both are the sheet's own figure from the same cell, and refusing the second would discard a legitimate workbook amount over a caller's accident. **And one over-correction, caught by the suite**: where the sheet holds a unit price and a quantity but the extended cell is empty, the product is arithmetic over SHEET values, not an engine figure — the ruling is about engine-only amounts. It is published, labelled *"the sheet's own unit price x quantity"*. | generic — the last line-level renderer | `estimate_explained` (outstanding table, labour cells); `displayed_charge` (either row shape) | `this commit` | `tests/test_a_published_document_carries_no_untraceable_money.py` — the acceptance fixture James specified, rendering a real report from a 401912-02-shaped summary and asserting the four properties by reading the OUTPUT: no engine-only currency, every published amount carrying its recorded cell, £3.88 and £321.88 absent, and a pending line named but amountless. **Written as a rendered fixture and not more unit tests, deliberately**: `displayed_charge` was unit-tested from the day it was written and the £3.88 still reached the page, because the renderers were not asking it | implemented — verify against a real rerun once `081703b` is pulled |
| D-139 | 18 Sep 2026 | James Gray on D-138: "**This write-up overstates what `6808332` does... it does not wire `publishable_total()` anywhere. It has zero callers outside its test, so the £321.88 protection is not live**" — plus two design corrections | every renderer | **I DESCRIBED D-138 AS CLOSED AND IT WAS NOT.** `publishable_total` had zero callers: a fact nothing asks is not a protection, it is a docstring. The covering email never consumed `displayed_charge` either. **And two design faults in the fact itself.** (1) It built `Estimate!M{row}` from a `MONEY_COLUMN` constant, on the grounds that every money column in today's template is M — true today, and **the same bet that shipped wrong twice this afternoon** on the comparator gate (the engine's `cost_method`, then the block's display label). (2) It returned an engine-only figure as `amount`, so a renderer printed it in pounds with a caption — **which is exactly how £3.88 reached five pages and £321.88 reached a narrative**. | **The cell is RECORDED where it is known.** `_read_block` locates the value column by reading the block's header and stamps `charged_cell` there; `read_unit_price_composition` already scans for the unit-cost cell and now records it as `unit_cell`. `cell_reference` reads what was recorded and **returns None rather than guessing a column**. **An engine-only amount is `PENDING`, never currency**: `amount` is None, the figure is kept as `diagnostic`, and the line reads as pending a price. Section 11 keeps its CLASSIFICATION from the engine — it must survive a run where Excel never opened, or every zero row reads as unpriced — but publishes no figure it cannot trace, and its closing sentence no longer promises one. `publishable_total` is wired into the covering note's headline, which now names the cell beside the figure. | generic — the publication model | `displayed_charge`; `wep_readback_from_xlsx` (`charged_cell`, `unit_cell`); `costed_facts` (carries it); `job_report_html` (pending, membership lookup); `estimate_explained` (headline, `unit_cell`) | `this commit` | 19 tests in `tests/test_one_fact_says_what_a_line_prints.py`, including that no column is ever inferred and that the wiring exists. **Four tests deliberately reversed**: they asserted an engine-only amount was published, which is the behaviour the ruling forbids — each keeps what it was protecting (the line is NAMED, never dropped) and drops the figure. `if _found:` became a membership test: `None` now means "costed, no publishable figure", and truthiness sent every such line back to `blanks` | implemented — D-138 was **not** closed; this closes the fact and its wiring. The covering email's own line rendering is still separate |
| D-138 | 18 Sep 2026 | James Gray: "**Use one `displayed_charge` fact per line. Where a workbook sheet owns the charge, every renderer gets the workbook amount or a dash — never an engine comparison**" and "**Every published currency amount must have a source fact and workbook-cell reference. Narrative totals may not use independent engine aggregates.**" | every renderer, every job | **THE £3.88 WAS FOUND FIVE TIMES, ONE PER RERUN** — the HTML report's money cell, the AI Provenance column, the AI Explanation table, the AI Explanation narrative, and section 11's reassurance that a dashed row IS costed. Each was fixed alone, each fix was tested green, and the next book carried it again somewhere else. **That is not five bugs: it is one missing fact**, asked for in five places, each renderer re-deriving the answer from the raw engine estimate. Twice the gate was keyed on a value nobody checked — the engine's `cost_method` (which on that part is not the sheet formula at all), then the block's DISPLAY LABEL (a costed line carries the block KEY) — and both shipped. **£321.88 is the same fault in an aggregate**: a narrative printing the engine's sum over its part estimates, before the sheet's blocks, absorption divisor and customer terms, four sentences from the same paragraph calling the sheet's figure the real one. | **`displayed_charge(line)`** — the amount, the CELL it came from, the basis (`workbook` / `engine` / `none`), the engine's diagnostic kept whatever happens to it, and `publish_diagnostic` with the reason when false. A renderer that asks it **cannot publish an engine figure by accident, because it never sees one unless the fact says it may**. **`publishable_total(record)`** answers the aggregate question: the sheet's own total with `Estimate!G6`, or **None and why** — a sentence with no total is better than one with an untraceable total. Cell references are real (`Estimate!M63`, not `Estimate!63`): every money column on that sheet is M, verified against 401912-02's book. Both the block KEY and its label are understood, so neither of the two shipped mistakes is expressible. Every other block keeps its cross-check. Wired into all three deciding renderers — the report's money cell, the Provenance tab, and section 11 — which no longer decide anything. | generic — the architecture, not a job | `displayed_charge` (new); `job_report_html` (money cell, section 11); `estimation_report` (the gate) | `this commit` | `tests/test_one_fact_says_what_a_line_prints.py` — 16 tests: the cell reference, the ruled block, the block key AND its label, every other block keeping its cross-check, agreement not published as disagreement, an uncharged line, rubbish in, the £321.88 refusal, and that each renderer asks rather than decides. Three existing tests re-pointed from the ad-hoc gate to the fact | implemented — first of the five James listed; `display_material` next |
| D-137 | 18 Sep 2026 | James Gray: "**Weak generated copy: 'quantitys', 'set of explains'. Add a copy-quality test/linter for generated email text and prohibit raw engine instructions in email templates.**" | every covering note and report | **A READER WHO FINDS TWO ERRORS IN THE PROSE CHECKS THE NUMBERS DIFFERENTLY.** `_plural` appended `"s"` to anything, so the section about quantity breaks read **"3 quantitys"** — in a document whose whole claim is that its arithmetic can be trusted. The opening offered *"a set of explains you can work with"*, which is not English and survived because nobody reads their own boilerplate. And the note closed a section by instructing an estimator to *"Overwrite anything tagged **AI ESTIMATE — INDICATIVE, NOT A QUOTE**"* — quoting the workbook's internal marker at a person as though it were an instruction from the machine. **A sixth producer of "AI market indication" was still live in `costed_facts`**, which is the SOURCE label the note, the report and the Provenance tab all render, so fixing the four call sites in `estimate_explained` had left the origin untouched. | `_english_plural` inflects the endings English actually inflects — consonant + `-y` → `-ies`, and the sibilants that take `-es` — with the explicit `plural` argument still there for irregulars, because a general pluraliser would be a library and a source of new wrong answers. The opening is rewritten. The instruction says **what is wanted, in the words of the person being asked**: *"These lines need a current source — SDI Live, a supplier price, or a researched figure with its date."* The `costed_facts` label is corrected at source, so all four surfaces say one thing. **And a linter over the RENDERED note** — not the module source: an earlier cut of it grepped the file and matched a COMMENT recounting a 2026 fault, the seventh time in this suite a text search has found prose about a thing rather than the thing. | generic — every generated document | `estimate_explained._english_plural` / opening / instruction; `costed_facts` (the market label at source) | `this commit` | `tests/test_the_generated_copy_reads_as_written.py` — 15 tests: the pluraliser against ordinary and irregular nouns, and a linter over a REAL rendered note for mis-pluralised nouns and engine vocabulary. Its regex excludes `...ss` because the first run flagged "84 across" — a linter that cries wolf is one somebody switches off. Two fixtures carried `"xAI market indication"` in a SUPPLIER field, so the note read "researched market price (xAI market indication)" — the label twice; the fixtures now hold a supplier | implemented. `wb_populate._INDICATIVE_TAG` (the workbook's own row marker) deliberately NOT changed — a signed-off cell, asserted by four tests, and a separate decision |
| D-136 | 18 Sep 2026 | James Gray on the generated covering note for 401912-02: "the e-mail that's created and the html report have to be corrected" | every covering email and every report | **THE NOTE READS AS AN ARTEFACT, NOT AS A MESSAGE, AND IT OFFERS PEOPLE THINGS THEY SHOULD NEVER BE SENT.** Six faults, all generic. (1) It opens with a job/client/quantity strap and a large number — a document header, where a person opening their email needs a sentence saying what this is and what is wanted of them. (2) **"Attached" was a BLACKLIST**, so every output path the engine has ever learned to write joined it the moment somebody added it: logs, SQL, CSV exports, source-drawing-data dumps, BOMs-and-routes exports, parity bundles, variant workbooks. Naming a log file as an attachment on a note to the head of estimating costs the whole document its credibility. (3) **"No customer quote"** reads as "we did not produce one" when a draft exists and is deliberately held — the distinction decides what somebody sends. (4) **500-off discussed on a job costed at 1 and 3**: the prose carried 500 as a rhetorical example from whichever job it was written for, and a reader has no way to know that. (5) **Internal system language** — "AI market indication", "Overwrite it" — on a document for a person. (6) The same market-price phrase on the HTML report. | (1) opens with a sentence addressed to the reader; the detail below is unchanged. (2) **a WHITELIST** — `estimate_xlsx`, `report`, `quote` — so a new deliverable must be added deliberately before it can reach anybody's email. (3) *"No customer quotation is attached or issued — it is held until the outstanding lines below are settled."* (4) the sentence uses the breaks **this run actually costed**, read from the sweep. (5)/(6) *"researched market price … not a quotation, replace before quoting"* and *"Replace it, or accept it deliberately"*, in the note and on the report, so the two say the same thing. | generic — every covering email, every report | `main.py` (the sendable whitelist); `estimate_explained` (opening, quote sentence, quantities, market wording); `job_report_html` (market wording) | `this commit` | 4 tests updated to the new contract. `test_the_attached_line_names_what_the_service_will_attach` now asserts the whitelist AND that three known internal artefacts cannot reach it. Its sibling's brittle `SRC[i - 4500:i]` window — already widened once, broken again here — is **anchored** to the line that reads the release: a window needing re-tuning whenever the file changes is measuring the file's length, not its behaviour | implemented — prove on the next rerun |
| D-135 | 18 Sep 2026 | James Gray on the 11:35 book: "one remaining £3.88 in section 11, 'Why these lines carry no price', where it calls the steel part 'costed elsewhere'... never the retired engine comparator" | every job report | **THE FIFTH SURFACE, AND THE ONE THAT POINTED AT THE COLUMN WE HAD JUST WITHHELD.** Section 11's reassurance read *"1 line(s) show zero here and ARE costed... These are not waiting on anybody: 401912-02-01M (**£3.88**). The per-part figures are on the AI Provenance tab."* That section exists to stop a reader chasing a price that already exists, and it did it by publishing the ENGINE's figure — the one removed from four other surfaces in D-130, D-133 and D-134 — and then **sending them to a Provenance column that now correctly reads a dash**. A pointer to a number we had deliberately suppressed. It read `material_estimate.extended_material_cost_gbp` because when the section was written that was the only per-part figure to hand. **Everything else on that book was clean**: no £3.88 anywhere in the workbook, Provenance column reading `—`, no £321.88, no disagreement paragraph, tape purchased, fold at one bend closing *"the charge follows the measurement; no confirmation is needed"*. | The sentence quotes **what the sheet charged**, from the costed record's own `charged_ext_gbp`, and names the **block row** instead of the Provenance column. The engine's figure survives only as the fallback for a line the sheet never charged — the one case where it is the only number there is. The section's purpose is untouched: a row that vanishes reads as a suppressed finding, so the line is still named and still says the money exists. | generic — every job report | `job_report_html._unpriced_section` | `this commit` | 4 more tests in `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py`. `test_the_moved_rows_are_named_and_say_where_the_money_is` re-pointed from "AI Provenance" to the block row, intent unchanged: the sentence must say WHERE the money is | implemented — **fifth surface; unproven until the next rerun** |
| D-134 | 18 Sep 2026 | James Gray on the 10:41 book: "**It must be removed from every output for this workbook-sheet-steel route. The reported £3.07 is the only applicable steel basis**" — and "it still says 'confirm the fold count if this matters.' That should be softened or removed... the drawing/DXF establishes one fold already" | every job | **FOUR SURFACES CARRIED THE COMPARATOR AND I FOUND THEM ONE AT A TIME ACROSS THREE RERUNS.** The HTML report and the Provenance tab were gated in D-130/D-133; the **AI Explanation tab carried it twice more** — a table column headed *"The engine's own figure"* (`I45 = £3.88`) and a paragraph beginning *"The two columns disagree, and the sheet's is the one you pay"* (`A48`). Everything else on that book was right: one charged fold at `J97 = 0.505`, `M97 = £20.44`, £149.87 at 1-off and £55.87 at 3-off, both break columns, £321.88 gone, tape purchased, no formula errors. **And the fold's own sentence asked for a confirmation it already had**: the flat pattern MEASURED the bend and the line still closed with "Confirm the fold count if this matters to the price." | The column and the paragraph go. **Every row in that table is on the sheet-steel block by definition** — that is what the block is — so the column is removed rather than gated row by row. The paragraph's useful half survives: the nest explanation (*a part yielding one per sheet carries the whole sheet*) is stated as the METHOD in the note above, not as a discrepancy. **And a measurement is not asked to confirm itself**: where the winning rung measured the part the sentence explains why the other readings differ and closes *"the charge follows the measurement; no confirmation is needed"*; where the charge rests on something unmeasured the ask is real and stays. A page full of dutiful confirmations is a page nobody reads — the lesson three other flags here have paid for. | generic — every job | `estimate_explained` (the steel-block table, its note, the disagreement paragraph); `fold_count.press_brake_folds` (the closing clause) | `this commit` | 6 more tests in `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py`. `test_the_two_views_of_the_steel_are_shown_disagreeing` is **deliberately reversed** and renamed, with the reversal and its reason recorded in the docstring — it asserted the paragraph 12552 needed, and keeps asserting what that paragraph protected: the sheet's charge is the bold quotable figure and the nest explanation survives | implemented — **unproven until the next rerun** |
| D-133 | 18 Sep 2026 | The 401912-02 rerun of 10:41 — build `e3918ff`. The fold is right, and the £3.88 is still there | every job report and Provenance tab | **THIS GATE HAS NOW BEEN KEYED ON THE WRONG VALUE TWICE, AND SHIPPED BOTH TIMES.** D-128 asked whether the ENGINE had costed the part by `workbook_sheet_steel_formula` — on this part it had not, because £3.88 is the engine's own method and £3.07 is what the SHEET's nest row calculated. D-131 corrected that to the block, and used the block's **display label**, `"Sheet Steel"` — while a costed line carries the block **key**, `steel`, which `estimate_explained` reads in four places and which `costed_facts` sets from `_FABRICATED_BLOCKS`. Written, tested green and shipped, twice, without either version ever meeting a real record: the tests built their own fixtures using the value I had assumed, so they asserted my assumption back to me. **Everything else on this rerun landed** — J97 0.505 h, M97 £20.4362, unit £149.8704 against a prediction of £149.8707; the tape purchased on `BOMs & Routes`; £321.88 gone; sections 3 and 5 agreeing. | The accepted vocabulary is **taken from `costed_facts._FABRICATED_BLOCKS`**, the module that defines it, rather than from a third literal typed by hand — with both known spellings as a floor so a rename cannot silently stop the gate matching, which is exactly how this fault behaves. One helper, shared by the report and the Provenance tab, because the two disagreeing about which block is ruled is the whole reason the figure is withheld at all. The tests now **read the definition** instead of inventing a fixture: if the key moves, they fail here rather than on a customer's estimate. Board, tube and wire keep their cross-check — only the steel block has a ruling. | generic — every report and Provenance tab | `job_report_html._sheet_ruled_block_names`; `estimation_report` (shares it) | `this commit` | 5 more tests in `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py`, including the block key a costed line actually carries, the vocabulary pinned to its defining module, and the control that the other fabricated blocks are not swept in | implemented — **third attempt; unproven until the next rerun** |
| D-132 | 18 Sep 2026 | James Gray: "**we can't show three folds when there is only one**" — and his own diagnosis: *"SolidWorks is not necessarily 'wrong'... The defect is ours: we treated that model feature count as though it were the number of press-brake operations and gave it a higher generic source rank than the flat-pattern/DXF evidence."* | every folded part on every job | **THE DIVIDER HAS ONE FOLD AND THE SHEET CHARGED THREE.** The flat DXF measured one bend axis and was right — `_extract_bend_data` counts **distinct axes**, so a bend line the exporter broke around a cut-out is one fold, not three. The SolidWorks cut list reported **three bend features**, and the connector wrote that into `geometry_rollup.estimated_bend_line_count` at rank 90, over the top of the measurement. Thirty minutes of brake set-up and **three times the run time** on a part with one bend in it: £0.40 a unit, constant per unit, growing with the order. **Nothing was lying** — three CAD features can make one physical fold. They are not the same quantity, and only one of them is what the shop does. **AND THE PROVENANCE LABEL FOLLOWED THE WRONG NUMBER**: `infer_bend_count` stamped `dxf_bendlines_layer` off the flat pattern's one, the connector overwrote the count afterwards, and the review line read *"3 fold(s) charged, counted by dxf_bendlines_layer — measured, not inferred."* A label attached to a figure it did not produce is worse than no label: it tells a reader to stop checking. That one is mine, from D-120, two commits earlier. | **The two facts are kept apart** — `solidworks_bend_features` is CAD evidence, the flat pattern's bend lines are the operation count — and **one resolver**, `fold_count.press_brake_folds`, answers the question every consumer asks: the process time, the synthesis, the workbook row, the provenance, the report and the review flags. Rungs: measured flat pattern → the drawing's stated fold callouts → the SolidWorks feature count → our own dashed-line inference. **Every losing reading is published as a review sentence and none of them ever changes the charge.** **WHERE THE DASHED PROXY SITS, AND WHY IT IS NOT WHERE THE BRIEF PUT IT**: the brief ranks "a clear drawing fold note/dashed bend line" above the model. A fold NOTE is a statement by the drawing office and is ranked there; a DASHED-LINE COUNT is our own inference from vectors in a PDF view. Ranking it above the model would undo the fix the override was built for — on 12552 the scan read 26 and 28 against cut lists of 6 and 8, folding the bay's two largest parts about twenty times each for folds that are not there. So the proxy sits BELOW the model, and both jobs come out right. | generic — evidence type, not job or part | `fold_count` (new); `source_connectors.solidworks` (records, no longer charges); `feature_synthesis`; `estimator.estimate_process_times` | `this commit` | `tests/test_a_fold_is_charged_from_the_flat_pattern.py` — 16 tests: the brief's four controls, both 12552 directions, a measured zero not falling through, agreement not flagged, and that each consumer calls the resolver. `test_a_dashed_line_is_not_a_bend` re-pointed from the old field to the charge it protects | implemented — prove on the next rerun |
| D-131 | 18 Sep 2026 | James Gray, inspecting the 09:58 book and report line by line and tracing the contradictions himself | every job report and workbook | **FOUR REPORTING FAULTS, THREE OF THEM PLACES A FIX HAD ALREADY BEEN MADE SOMEWHERE ELSE.** (a) **The £3.88 comparator survived on BOTH surfaces.** D-128 gated it on the ENGINE's `cost_method` being `workbook_sheet_steel_formula` — and on this part it is not: the engine reached £3.88 by its own method and **£3.07 is what the SHEET's nest row calculated and handed back**. Two different questions, and the one that matters is WHO CHARGED THE LINE. So the gate never fired, on the Provenance tab or the HTML report. (b) **The tape still read MILD STEEL on `BOMs & Routes`** — a THIRD inheritance site, after the drawing-quality table and the provenance wording, each fixed alone because nobody looked for the others. Its own comment already named the case — *"on a page carrying several, they are not [the same thing]"* — and the fallback took the title block anyway. (c) **£321.88 in `AI Explanation!A111`** — `document_total_provisional_gbp`, the engine's sum before the sheet's blocks, absorption divisor and customer terms — printed beside a workbook reading £150.32, **four sentences from the same paragraph calling the sheet's figure the real one**. (d) **"No specific review points — the estimate read cleanly"** in section 5, on a page whose section 3 lists four flags: the same contradiction D-127 fixed against the workbook, moved one heading down, because section 5 is built from a different structure. | (a) keyed on **`block`**, which `costed_facts` already records per line for exactly this question ("Sheet Steel", "Other Sheet Material", "Tube", "Wire"), on both surfaces. (b) `bought_in_policy` — the one module that answers "do we buy this", and it answers from the row's own part number and description, so no new signal was needed. (c) the **ratio is kept and its denominator is described rather than printed**: a number appearing on no other deliverable is worse than no number, which is the steel comparator's lesson applied to a total. (d) section 5 renders the same `part_notes` section 3 does. | generic — every report, every workbook | `job_report_html` (`_sheet_ruled_basis`, `_render_checklist`); `estimation_report` (the gate); `bom_and_route_extract` (the title-block fallback); `estimate_explained` (the narrative total) | `this commit` | 2 rewritten + 1 added test in `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py` (including the one naming why the first gate did nothing); `test_the_explanation_adds_up_to_the_sheet` asserts the document total is absent while per-line money — which that section is FOR — is untouched | implemented — **unproven until the next rerun** |
| D-130 | 18 Sep 2026 | The 401912-02 rerun of 18 Sep 09:58 — build `49b82ee`, three fixes landed and one did not | every job report | **THE FIX WENT INTO THE WRONG RENDERER, AND THE TEST THAT SHOULD HAVE CAUGHT IT PASSED.** D-128 suppressed the competing engine-steel figure in `estimation_report`, which builds the Provenance TAB. The page James reads is built by **`job_report_html`**, and it printed `£3.07 engine £3.88 — not charged` on the very next run. There are two pages and two modules and only one of them had been found. **The test asserted the SHAPE OF THE SOURCE of the module I had edited** — it read `estimation_report.py` for a string and found it — so it went green while the report did not change. That is the exact weakness James named in September: *"the new tests are mainly source-shape checks."* A test that reads the file you just wrote proves you wrote it, and nothing else. | The suppression is applied in `job_report_html.row()`, where the money cell is actually built, through a shared `_sheet_ruled_basis` that reads the cost method off **either shape of part record** the renderer is handed (a costed part with a nested `material_estimate`, or a bare node). Both pages now agree, which was the point of withholding it in the first place — removing a figure from one page and leaving it on the other moves the argument rather than ending it. The test now **calls the renderer's own predicate** and checks the guard sits on the line that writes the cell, instead of grepping the file it was written in. | generic — every job report | `job_report_html._sheet_ruled_basis` and the money cell in `row()` | `this commit` | 3 rewritten/added tests in `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py`, including the one that failed to catch this, rewritten as the one that would have | implemented — prove on the next rerun |
| D-129 | 18 Sep 2026 | James Gray: "**this sheet is dave not tim. we have a team of four estimators.**" | the rate card, and every reader of it | **A SHARED OFFICE RATE TABLE WAS NAMED AFTER ONE ESTIMATOR.** `tim_rate_card_ingest.py` wrote `tim_rate_card.json`, config held `TIM_RATE_CARD_LOADED`, and the label map was `TIM_LABEL_TO_OP`. The thing all of them describe is the **estimating template's own Labour / Rate / Dept block** — the office's card — and the template is **Dave's**. SDI has four estimators. A rate table carrying one of their names is read as that person's personal figures: over-trusted by some readers, dismissed by others, and argued about by anybody who knows it is not their sheet. It also put a false attribution into D-122's own commit and register row, which quoted "Tim's 1310" as the source of the £331.42 that has been an open question against the £355.43 the engine books. **And it fed a worse habit**, which James named in the same breath: *"every price needs to be from sdilive or llm, not copied from another s/sheet unless it's a rate that the s/sheet has embedded... it cannot validate or replace the current template rate."* | Named for **what it reads**, not for whose sheet it first read: `rate_card_ingest.py`, `rate_card.json`, `INGESTED_RATE_CARD_SOURCE`, `LABEL_TO_OP`. **A rename must never silently change a price**, so the legacy filename is still read where it is the only one present — and using it is REPORTED on the run banner, so the old name cannot quietly outlive the rename; where both exist the current name wins. Per-figure attribution is untouched and stays where it belongs: `SHOP_STATED_PROVENANCE`, where a name is attached to a ruling somebody actually made, with the job and the date. The historic £331.42 is no longer attributed to anyone in the test's own prose — it is recorded as a figure from a completed estimate that **settles nothing**, because the template is what settles it. | generic — the mechanism and everyone who reads it | `config` (paths, source flag, comments); `src/rate_card_ingest.py` (renamed); `docs/KNOWLEDGE_SOURCES.md`; the staleness test's name list | `local only — not pushed` | 4 more tests in `tests/test_the_rate_card_comes_from_the_sheet.py`: no estimator name survives in the machinery (prose may recount the history), the card is looked for under its own name, a runner holding the old filename keeps its rates and is told, and the current name wins where both exist | implemented, unpushed |
| D-128 | 18 Sep 2026 | James Gray on the 401912-02 book: three report/workbook fixes | every job | **(2) TWO STEEL FIGURES ON A QUESTION THAT HAS BEEN ANSWERED.** *"Remove the competing £3.88 engine-steel figure. The estimate must present the editable workbook rate — £900/tonne in Estimate!L5 and £3.07 in the nest row — as the single charged steel basis."* **(3) THE TAB SHOWED ONE OF THE TWO QUANTITIES ASKED FOR.** The run was `--order-qty 1 --quantity-breaks 3` and Quantity Breaks came back with a single column headed 3, so the 1-off cost sat on the Estimate sheet and the 3-off cost on the tab with nothing saying they are one estimate at two quantities — £150.32 against £56.31, which IS the story of this job. **(4) A PURCHASED ITEM READING AS MILD STEEL.** The drawing-quality table reported `25.4mm ADHESIVE MAGNETIC TAPE` with *Material stated: MILD STEEL*, read off the GA title block, which states what the DIVIDER is made of. Nothing was mis-costed — the tape is priced as a bought-in and never touched the steel route — but a purchased line reading as mild steel on a page headed "Drawing quality, sheet by sheet" is a wrong fact an estimator carries to a supplier. | **(2) reverses part of D-118, and should.** Naming both bases was right while four steel rates were live and nobody knew which governed; the ruling settled it, and a second figure beside the controlling one stopped being evidence and became an invitation to re-open a closed question. **Scoped to the route that has a ruling** (`cost_method == workbook_sheet_steel_formula`) and no further — everywhere else the engine's figure beside the sheet's has caught real faults and is untouched. The Provenance column is withheld with the report sentence, because removing it from one page and leaving it on the other moves the argument rather than ending it. **(3)** the quantity the job was costed at is always a break: deduplicated and sorted, so `--order-qty 3 --quantity-breaks 3` is one column and not two identical ones. An order quantity alone never starts a sweep. **(4)** `_material_stated` applies the same test the "What it could not give" column two cells along already uses — a line whose only page role is `bought_in` has no detail drawing, so the sheet states the assembly's material and not its own. It says **why** rather than going blank, because an empty cell reads as a drawing that failed to state a material, which is a different complaint and a false one. A bought-in that HAS a detail drawing keeps its own material: the rule is about inheriting from an assembly, not about being purchased. Applied to **both copies** of that table, HTML and markdown. | generic — every job | `estimation_report` (the basis sentence and the Provenance column); `main.py` (break selection); `estimate_explained._material_stated` | `local only — not pushed at James's instruction` | `tests/test_the_sheets_rate_is_the_only_steel_basis_shown.py` — 14 tests, including the controls that every other route keeps its cross-check, that a bought-in with its own drawing keeps its material, and that an order quantity alone does not start a sweep | implemented, unpushed |
| D-127 | 18 Sep 2026 | The 401912-02 book of 18 Sep 07:31 — build `0262c3e`, the first complete run on that job | every job, every report | **TWO DELIVERABLES FROM ONE RUN, DISAGREEING ABOUT WHETHER ANYTHING NEEDED CHECKING.** The workbook's footer: *OUTSTANDING ESTIMATOR INPUTS (5) — this sheet is NOT a price until these are filled*. Section 3 of the report beside it, in full: *No provisional or low-confidence items flagged for this job.* A two-names fault of the kind this month has spent itself on — `_render_review_items` builds from `estimate_review_signals.parts_flagged`, a structured signal with a fixed vocabulary, and **never from `part["review_flags"]`**, which is where every sentence the costing rules write actually lands. So the mass check's *"the line is light by 0.91 kg"*, the fold's *"counted by dxf_bendlines_layer — measured, not inferred"* (D-120, shipped hours earlier), the steel rate's refusal note and every other rule written this month reached the record, reached the workbook, and stopped **one page short of the estimator**. The page did not merely omit them: **it stated the opposite**, which is worse than silence, because a reader who checks section 3 and finds it empty has been told there is nothing to check. | The parts' own flags are gathered into `part_notes` and rendered, **deduplicated across parts** — a per-part rule would otherwise print forty times on a forty-part job and be scrolled past, which is how a warning stops being read. The row names which parts it was about, because a sentence with no part on it cannot be checked. **NOT PRICED lines are deliberately excluded**: they are the Decisions-required section's subject, that section is the one that gates sending, and the same item twice on one page reads as two problems. The gatherer is a named function rather than twelve lines inside a ninety-line builder — buried, it could only be tested through the thing it fed, which is how it went unnoticed. The empty sentence stays reachable and is its own test: a section that always has rows is a section nobody believes. | generic — every job report | `job_report_html.part_review_notes` and `_render_review_items` | `this commit` | `tests/test_the_report_says_what_the_engine_flagged.py` — 8 tests: a real flag reaching the page, the part named, one rule on forty parts rendering once, the decisions items not printed twice, and the control that a clean job still says so | implemented — prove on the next 401912-02 run |
| D-126 | 18 Sep 2026 | James Gray: "are the stop and restart commands on that page now for the laptop and the server" — checked, and the answer was no | the laptop, and SDI-APP01 | **THERE WAS NO STOP COMMAND FOR A SERVICE AT ALL** — only start and restart — so "take the site down for five minutes" had no answer, and D-125's page could not carry one because none existed. **And a worse thing behind it: `restart-service.ps1` knew only about the laptop's scheduled task.** SDI-APP01 runs the portal as an **NSSM service** (`SDIIntelligence`) — `configure-nssm-service.ps1` exists precisely because of it. On that machine the script would find no scheduled task, **kill whatever was listening on 8071**, then print *"start it yourself: install-service-task.ps1"* and exit 1. That is the intranet stopped, on the production box, with the remedy naming a mechanism that machine does not use. It is the same wrong-machine fault the `-Port` guard in that same script was written for, one step further in. **And NSSM restarts what you kill**, which makes the other order actively deceptive: end the process while the supervisor is still running and the OLD code comes straight back, the health check passes, and the restart reports success having changed nothing. | `restart-service.ps1` **finds whichever supervisor this machine uses** — the scheduled task or the Windows service — resolves it ONCE before anything is stopped so the restart uses the same answer the stop did, and stops the supervisor BEFORE the port is killed. A machine with neither is told the truth (a hand-started window, restart it with `start-service.ps1`) instead of being sent to install a scheduled task nobody wanted. **`-StopOnly`** stops the supervisor and the orphan and leaves it stopped, on either machine. The page now carries a **Stop / Restart / Supervised-by row for every box** — laptop 8072, laptop 8071, SDI-APP01, and the runner — with the two things that are easy to get wrong stated outright: **`push-to-server.ps1` copies and stops, it does not restart anything**, so the server keeps serving the code it imported exactly as the runner does; and the server is NSSM while the laptop is a task. | generic — every service on every machine | `tools/start/restart-service.ps1`; the Operate section of `sdi-intelligence-portal.html` | `this commit` | 10 more tests in `tests/test_the_runner_can_be_restarted_by_one_command.py`, including that the supervisor is stopped before the port is killed and resolved once, that the unsupervised case no longer offers the laptop's remedy, and that every box's stop and restart appear on the page. `test_a_restart_refuses_to_act_on_the_wrong_machine` re-anchored on the STEP NUMBER rather than its title — five tests failed on a reworded comment about a guard that had not changed, which is a test testing the comment | implemented |
| D-125 | 18 Sep 2026 | James Gray: "what are the stop and restart commands for the runner. pls also update the #tools with exactly how this needs to be ran for the 8072, 8071 and the current main server" | operating the engine, on every machine | **THE HONEST ANSWER WAS: THERE AREN'T ANY.** `restart-service.ps1` has existed for the SERVICE since the afternoon a restart failed to restart anything. The **runner** — the process that actually costs jobs — had nothing. Stopping it was a five-line PowerShell incantation printed inside a `Write-Host` in `start-runner.ps1`, to be copied out of a terminal by hand. **That is what 401912-02 cost**: three runs in twenty minutes, every one stamped `2314342 +local edits` while the branch had moved four commits ahead — one of them the fix for the crash ending each run. The pull had been done each time; the runner was never restarted, so the pull could not reach the process, because a running Python holds the code it imported at start-up all day. **And the distinction underneath it was written down nowhere**: the SERVICE queues jobs, the RUNNER costs them, they are different processes on different machines, and a `git pull` on the service box changes nothing whatever about how a job is priced. Every box has a `C:\ClaudeVision` and an identical prompt. | **`restart-runner.ps1`** — one command. It stops the scheduled task AND ends every runner process by name (the task's action is powershell.exe which starts python as a CHILD, so stopping the task alone can restart nothing; and one runner is TWO processes because a venv python.exe is a launcher). It **refuses on a machine with no runner in the checkout** rather than reporting a success that happened elsewhere. `-Pull` pulls FIRST, before anything is stopped, and **refuses on a dirty tree** — local edits are somebody's work and are never merged over, stashed or discarded. `-Clean` deletes `__pycache__`, opt-in and announced, for the stale-bytecode case whose tell is a traceback printing nonsense source lines. Then it **says which commit the engine will now run** and shouts if the checkout is still behind its branch — the line that would have ended that afternoon on the first run instead of the fourth. **And the #tools page now carries the operating instructions**: a table of all three deployments (8072 laptop hand-started, 8071 laptop installed task, 10.0.0.5:8071 SDI-APP01 which has no git and is reached only by `push-to-server.ps1`), the runner's own row, a card per script, and what a good run's first three lines look like. | generic — every machine, every run | `tools/start/restart-runner.ps1` (new); the Operate section of `sdi-intelligence-backend/sdi-intelligence-portal.html` | `this commit` | `tests/test_the_runner_can_be_restarted_by_one_command.py` — 20 tests: ASCII-only for PowerShell 5.1, the root not resolved in a parameter default, processes ended after the task and named before they are killed, the destructive git verbs ABSENT by name, the pull ordered before the stop, the commit reported after the start, and the page asserted to name all three addresses and to state which box changes a price | implemented |
| D-124 | 18 Sep 2026 | 401912-02 run three times in twenty minutes, every one ending "The engine exited with code 1. Nothing was filed." | every run on every job | **TWO FAILURES OF THE ENGINE TO LOOK AFTER ITSELF, and between them they cost an afternoon.** (1) **It could not tell it was behind its own branch.** Every run stamped `2314342 +local edits` while `main-2026` had moved FOUR commits ahead — one of them the fix for the very crash ending each run. The two existing stale checks both compare the PROCESS against the CHECKOUT, so they are silent precisely when the two agree, which is the state of a runner that simply never pulled. "+local edits" was printed each time; it is true and it is not the problem, and nothing said a word about the four commits. (2) **One part took the whole job down.** A `KeyError` costing `25.4mm ADHESIVE MAGNETIC TAPE` ended the process, and the DXF augment, the SolidWorks extract, the SQL price lookups and every other part on the job went in the bin with it — forty-nine seconds of work and an estimator holding nothing, not even the parts that costed perfectly well. | **The banner asks the one question nobody was asking**: `behind_origin_warning` reads `HEAD..@{u}` from the git tree — no fetch, no network, no delay on a runner that may have neither — and names how many commits, which branch, and the tip's subject, because "behind" with nothing attached is not actionable. It reports what the last fetch knew, so it can UNDERSTATE the gap and never overstate it: a warning that appears is always real, and a checkout that has never fetched says nothing. It yields to `stale_process_warning` when that fires, because a process out of step with its own checkout is the worse fault and two banners competing for one reader's attention gets both ignored. **And a part that throws no longer ends the run.** The trade is not "hide the error" — it is WHICH failure the estimator is handed: a job with one line marked NOT COSTED can be read, priced by hand and sent; no job at all cannot be anything. The line is dropped WHOLE rather than half-built, because a partially costed part is how a £0 reaches a total, and its absence is made impossible to miss — on the log, in `summary.parts_that_failed_to_cost` with the full traceback, and as a review flag saying the line is MISSING FROM THE TOTAL and must be priced by hand before the estimate is sent. **Said, not stopped** — the rule this engine follows everywhere else. | generic — every run, every job | `build_stamp.behind_origin_warning` and `print_build_stamp`; the costing loop in `estimator.estimate_document` | `this commit` | `tests/test_a_run_that_is_behind_says_so.py` — 12 tests against REAL git repositories (a clone four commits behind its origin, an up-to-date one told nothing, no upstream, no git at all) plus the costing guard, including the controls that the louder banner wins, that nothing half-costed reaches the record, and that an ordinary job is told nothing | implemented — the next run says whether it is on the branch before it says anything about money |
| D-123 | 18 Sep 2026 | The 401912-02 run of 06:53 — `KeyError: 'geometry_rollup'`, exit code 1, nothing filed, twice | every job carrying a part nothing measured | **A BOUGHT-IN WITH NO GEOMETRY KILLED THE WHOLE ESTIMATE.** Every read in `feature_synthesis` was a bare `part["geometry_rollup"]`, which is safe only because the one caller — `_interpret_part` — guarantees the key. The moment a SECOND caller appears, costing a part mid-route, it arrives with a part that has no rollup at all: 401912-02's `25.4mm ADHESIVE MAGNETIC TAPE, L: 450mm`, which is purchased and which the run's own log says was deliberately given no borrowed dimensions. The KeyError took the entire job down — the DXF augment, the SolidWorks extract, the SQL price lookups, sixty-nine seconds of work — and filed nothing, twice. **The second caller is not in this repository**: the runner was on `2314342 +local edits` and `estimator.py` on `main-2026` contains no call to `infer_bend_count` at all. But the fragility it found is ours, it is real, and the fix belongs here rather than in whoever calls it next. | A missing rollup **means** "nothing measured this part", which is an ANSWER the rungs below already handle: no geometry bends, no dashed lines, fall through to the drawing's own text. `_rollup(part)` gives the absence that meaning **once**, instead of every reader having to remember — and tolerates the several falsy shapes an empty rollup actually arrives in (`None`, `[]`, `""`) depending on which reader gave up, none of which should be subscripted. Nine call sites across `infer_hole_count`, `infer_bend_count` and `synthesize_manufacturing_features`. **Hardening a read must not stop it reading**, so a real rollup is still consumed and that is its own test. | generic — every bought-in, every unmeasured part, on every job | `feature_synthesis._rollup` and its nine call sites | `this commit` | 7 more tests in `tests/test_a_fold_says_who_counted_it.py` — the magnetic tape by name through both entry points, each falsy rollup shape parametrised, and the control that a populated rollup is still read | implemented — the 401912-02 rerun should reach a book |
| D-122 | 18 Sep 2026 | James Gray: "delivery and packaging we learn as we go. it's relatively unimportant compared to **boms and routes and pricing accurately of those**" — and his steel ruling applied to labour | every labour line on every job | **EVERY DEPARTMENT RATE IN THE SHOP LIVED IN A GITIGNORED FILE.** `tim_rate_card_ingest` reads the estimate template's own "Labour / Rate / Dept" block — the estimator's card, the one they amend — and wrote it to **`tim_rate_card.json` beside `config.py`**, which `src/.gitignore`'s blanket `*.json` rule has always excluded. config overlaid it at import. So the numbers that price **every route** were outside version control; two machines could hold different cards and produce different money from one commit with nothing on either book to compare; and it was **silent** — `TIM_RATE_CARD_LOADED` was assigned and **read by nothing**, so a run with a card and a run without looked identical on the page. This container has no such file, so every figure I have quoted this week is a config default, and James's runner may not be. **And a fourth fault, worse than the others: the overlay wrote INSIDE its loop**, so a throw part-way through left the card **half applied** — some departments on this year's numbers and some on last year's, which is the one outcome nobody would choose and the one nothing would have reported. The open **£355.43 (P/C) vs £331.42 (POWDER)** question is the same fault wearing a different hat: `_rate_table_find` has it recorded as waiting on Tim, on a rate worth about sixty per cent of 401912-02's unit cost, and the answer was in the live template all along. | **The template's own block is the rate card**, read directly by `read_template_rate_card` the way `workbook_input_value` reads Estimate!L5 — the header is LOCATED, not assumed at a fixed row, and reading stops where the labour block stops, because the wire price-break rows below it carry a NUMBER in the dept column and would otherwise become hourly rates. **Whole or not at all**: the card is parsed into a dict and applied in one go, never row by row. Implausible rates are refused (£1–£2,000/hr — generous, because £355/hr is a real LINE rate applied with a throughput divisor, so the clamp only catches a misplaced decimal or the wrong column). **The ingested file still applies where the template was silent** — someone who has just ingested a newer sheet is answering later — but it never overrules the sheet this job is costed on, and **where the two disagree the run says so** instead of resolving it quietly. **Every rate can name its source** (`hourly_rate(op)` → value and provenance) and the run banner prints which card priced the job, beside the commit that costed it. The label→operation mapping moves to config and the ingester imports it: one mapping, one home. | generic — every operation, every job, and the mechanism itself | `config.read_template_rate_card` / `_apply_rate_cards` / `hourly_rate` / `HOURLY_RATE_SOURCE` / `RATE_CARD_NOTES` / `ESTIMATE_LABOUR_LABEL_TO_OP` / `INGESTED_RATE_CARD_PATH`; `build_stamp.print_build_stamp`; `tim_rate_card_ingest` | `this commit` | `tests/test_the_rate_card_comes_from_the_sheet.py` — 14 tests: the block read wherever it sits, reading stopping at the price breaks, a nonsense rate refused, an unreadable template yielding nothing rather than something, the file reported when it disagrees and used where the sheet is silent, one shared mapping, and every held rate able to name its source | implemented — the live template decides the powder rate on the next run; Tim confirms the figure it reports rather than being asked to supply one |
| D-121 | 18 Sep 2026 | A second session's report that "a separate labour path can bypass the DXF count" — checked rather than taken, and the bypass found one road over | every powder-coated folded part | **THE FOLD LABOUR OBEYS THE DXF AND THE COATING AREA DID NOT.** The claim as stated is not true: `estimate_process_times` charges `manufacturing_features.bend_count`, which IS the BENDLINES figure once a flat pattern has answered, and a probe of 401912-02's divider — one measured bend against three angle callouts and a rollup of three — books 0.3 min of fold run time for ONE bend. But the instinct behind it was right, and the bypass is in the two POWDER-COATING AREA calculations, which both took `max(mf.bend_count, geometry_rollup.estimated_bend_line_count, fold_count_textual)`. So a drawing note or a dashed-line heuristic could outvote the press brake's own layer about how many bend-edge strips the powder is spread over. **Measured on the same probe: 0.4137 m2 against 0.3566 m2 — sixteen per cent more coated area, on a part where powder is about sixty per cent of the unit cost.** It moves money in the direction nobody audits, because a slightly high coating figure reads as caution rather than as an error. Two functions asked the same question with the same expression and would have drifted apart the moment either was corrected alone. | A measured count controls the coating area, exactly as it already controls the labour: `_bends_for_coating` returns the count **and who supplied it**, and both callers use it. **The `max()` is kept where nothing has looked** — a rollup count and a drawing's callouts are the only evidence there is, and for a consumable the larger is the safer bet — so every unmeasured part keeps the behaviour it had. The count's source is published on the coating detail, because a coated area is one of the few figures an estimator cannot check by eye. Depends on D-120: before `bend_count_source` was written, no caller could tell a measured count from a proxy, so this fix was not expressible. | generic — every powder-coated folded part on every job | `estimator._bends_for_coating`, `_powder_coated_area_m2`, `_powder_consumable_estimate` | `this commit` | 5 more tests in `tests/test_a_fold_says_who_counted_it.py`, including the 0.3566-vs-0.4137 arithmetic, the control that an unmeasured part still takes the larger count, and the control that the fold LABOUR was already right and stays right | implemented — prove on the 401912-02 rerun |
| D-120 | 18 Sep 2026 | James Gray on the 401912-02 book: "Ensure the correct flat DXF supplies the fold evidence/count. **why are we getting this incorrect ever ?**" and "**anything fundamental around bends needs to be resolved**" | every job with a DXF, and every folded part | **WE PUBLISHED OUR OWN GAP AS A FACT ABOUT THE CUSTOMER'S DRAWING.** 401912-02's book carried a Fold row at thirty minutes of press-brake set-up and, a few hundred words away, "layers not provided so bend vs cut assignment unknown". Read together they say the engine charged a fold it could not see. **Neither half was true, and the two faults are independent.** (1) `drawing_job_merge` hands the DXF interpreter `raw.get("layers")`, and the reader's dict **has never had a `layers` key** — nor `entity_counts`, nor `text_entities`, asked for on the same line and absent for the same reason. So the model was asked which layer is the cut profile with no layers, no entity census and no text, it said so, and its answer was quoted back as evidence about the file. That sentence has appeared on **every job ever run, whatever the DXF** — the staged 01M flat on this job has a BENDLINES layer, and so does a fixture in our own test folder. It is not a limitation of the drawing; it is a hand-off nobody had wired. (2) `manufacturing_features.bend_count_source` is **read** in two places — `_model_measured_zero_bends`, which tells a measured zero from an absent one, and the fold's own review line — and was **written in exactly one**, by the SOLIDWORKS connector. `infer_bend_count` knows which rung answered at the moment it chooses and threw that away to return a bare integer. So a count measured off a BENDLINES layer and a count read off a drawing note were **indistinguishable downstream**, and D-118's new review line, meant to separate them, was about to fire on every folded part in the shop. The sixth two-names fault this week, and the first one caught before it shipped. | **The reader publishes what it read.** `extract_dxf_geometry` returns `layers`, `layer_entity_counts`, `entity_counts` and `text_entities` from the pass it was already making over every entity, the merge carries the layers onto the part as `dxf_layers`, and the interpreter is given them. A model asked to judge bend-versus-cut now has the layer names in front of it, and where it still cannot tell, that reads on a drawing rather than on our plumbing. **And the fold names its rung.** Every return in `infer_bend_count` stamps one: `dxf_bendlines_layer` (the press brake's own truth), `dxf_flat_pattern` (a measured zero — a cut-only export sets no bend layer), `drawing_text`, `inferred_dashed_lines`, `inferred_mirrored_return_flange`, `geometry_rollup`, and `no_bend_evidence` — because a zero with nothing behind it and a measured zero mean opposite things and must not share a word. **Precedence is settled once.** The stamp goes on the PART, not on `manufacturing_features`: that dict may already carry the SOLIDWORKS plate gate's measured zero, which this function is not entitled to overrule, and which it REPLACES wholesale a few lines later. `synthesize_manufacturing_features` resolves the two — a DXF reading outranks the model, as the count already does; anything short of a DXF leaves the model's word standing — and writes the answer to both places so they cannot drift apart. Idempotent, because `_interpret_part` is not guaranteed to run once and a stamp that degrades on a second pass is how a measured fold quietly becomes an inferred one. **No price moves.** | generic — every DXF and every folded part on every job | `dxf_reader.extract_dxf_geometry` (the layer census); `drawing_job_merge.apply_dxf_geometry_to_part` (`dxf_layers`); `feature_synthesis.infer_bend_count` / `synthesize_manufacturing_features`; `estimator._MEASURED_BEND_SOURCES` | `this commit` | `tests/test_a_fold_says_who_counted_it.py` — 15 tests: each rung named, the real fixture's BENDLINES layer read end to end through the merge, the model's own word surviving a dict it does not own, a DXF still outranking it, the two keys agreeing, and the controls that a second pass does not degrade the answer and that no inferred label is mistaken for a measured one | implemented — prove on the 401912-02 rerun |
| D-111 | 17 Sep 2026 | James Gray: "**config needs to be in config files. not json files lying around and being copied manually around.**" | every job with a ruling on it | A job's rulings lived in `<drawing>_confirmed.json`, written into the repository and then **copied by hand to the job folder on the share** before a run. Three faults, all of which bit. (1) **Invisible to git** — the blanket `*.json` rule kept every one out, so 7332-01's rulings are recorded in this register as delivered and were never in a commit; nobody outside the container that wrote them could have them. (2) **A run with no file applied no rulings and said nothing** — an answered question and an unasked one looked identical. (3) **Two machines could hold different answers with nothing to compare.** A decision nobody can diff is not a record of anything. And a fourth, found while moving it: `load_corrections` REQUIRED a `parts` block and threw away any file without one — whole, silently, reporting "nothing to apply" about a file that had plenty. **11908-21's own answers file is exactly that shape**, because Tony had no dimension to correct, so the delivery exclusion and the five metres of edging would have done nothing on the very next run. | **`config.JOB_DECISIONS`** is the home for what a person decided about a job, beside `SHOP_STATED` which is how SDI works. Keyed by drawing number, matched on the job code so an entry for `11908-21` governs it and its GA and no other job, and validated through the SAME `_read_decisions` an answers file goes through — a malformed entry is reported, never silently ignored. Howard's and Tony's rulings are moved into it and the loose files are deleted. **Prices are still refused**: a figure typed here would read on a sheet exactly like one the engine sourced. A job-folder file is still read, because an estimator at the share may write one, and it is merged ON TOP key by key — the file wins, because someone who has just written one is answering later than the repository, and **every overlap is reported** rather than silently resolved. The `parts` requirement is gone: a file may carry readings, decisions, or both. | generic — every job, and the mechanism itself | `config.JOB_DECISIONS`; `estimator_confirmed.decisions_from_config` / `merge_config_decisions` / `load_corrections`; the confirmed block in `file_scan` | `this commit` | `tests/test_a_ruling_lives_in_config_not_in_a_loose_file.py` — 14 tests: both estimators' rulings parse clean out of config, a pack FILENAME resolves its own job, another job gets nothing, a decisions-only file is no longer thrown away, the file beats config and says so, and no `*_confirmed.json` is left lying in the repository | implemented — nothing has to be copied to the share for a ruling to apply |
| D-110 | 17 Sep 2026 | The 11908-21 book of 17 Sep 15:50, read line by line — Estimate row 99 | every faced-board job | **THE LAMINATE IS CHARGED TWICE ON A PROMOTED BOARD.** Row 99: `Glue  laminating  GLUE  11908-21-01J/-02J/-03J  ·  9 off  ·  40/hr  ·  0.725 h  ·  set-up 30 min  ·  £18.43 a unit` — on a job whose entire material bill reads £3.78. The promotion's own review flag has said "the laminating on the route is IN the sheet price, not a shop operation" since it was written, and it was enforced NOWHERE: the sentence went on the sheet and the glue line went on it too. Both lines are individually correct, which is what makes the double survive a reading — the board really is laminated and the drawing really does say so. Only the conclusion is wrong: the drawing's word is evidence about the MATERIAL, not an instruction to the shop. It would have fired on the very next run the moment D-106 made the promotion reachable. | The route cancels `laminating` (and lamination / laying-up) when and only when `_laminate_in_board` is stamped, recorded under **`operations_ruled_out`** — the name wb_populate and route_compiler actually read, which is what the cancelled tube bend taught (D-102) — plus `removed_operations` and a review flag for the human side. **The evidence is deliberately NOT stripped from `textual_operations`**, unlike the tube-bend gate: `_faced_board_promotion` reads that list to decide the board is faced at all, so stripping it would delete the cancellation's own justification and a second costing pass would un-promote the part back onto raw-core money. Scoped to the promotion and nothing else — a panel the shop genuinely lays up itself carries no stamp, and keeps its operation and its glue time. | generic — every faced/laminated board on every job | `estimator.estimate_process_times`, beside the tube-bend gate | `this commit` | `tests/test_the_laminate_is_paid_for_once.py` — 6 tests; 2 fail on the parent commit. Controls: an unpromoted panel is untouched, the rest of the route is untouched, and the promotion still fires after the cancellation has run | implemented — prove on Tony's 50-off rerun |
| D-109 | 17 Sep 2026 | Found while wiring D-108 — the researched board price would not price anything | every job, every researched line, since rung 4 was built | **RUNG 4 HAS NEVER PRICED A SINGLE LINE.** The producer requires four things before a researched figure may be used — a source, the date it was true, what it is per, and the quantity it was found at — and refuses the price outright if any is missing. That refusal is right and is the reason rung 4 is allowed into a total at all. `web_ai_price_lookup` stamps the date on **every** result it returns, under `price_date`. The adapter between the two read `as_of`. So the date was present, recorded and correct, and was dropped in the six lines between two modules: every researched price the engine has ever found was refused for want of a date it already had. No error, no warning, no log — the line simply read as unpriced, which is exactly what it would read if no price existed. A whole rung of the ladder inert. | The adapter reads `price_date` as well as `as_of`. **The guard is not weakened**: a figure with no date anywhere is still refused, and a test holds that. This is the two-names fault — a fact recorded under one key and read under another — for the fourth time in this register (`removed_operations`/`operations_ruled_out`, `manual_labour_metal`/`brush_before_plate`, `unit_of_measure` at D-108, and now this), and it is the most expensive instance yet because what it disabled was an entire pricing source rather than one line. | generic — every researched price on every job | `estimator._rung4_researcher` | `this commit` | `test_the_lookups_date_reaches_the_producer` and `test_a_figure_with_no_date_is_still_refused` in `tests/test_a_board_is_entitled_to_the_last_rung.py` | implemented — the first run after this will be the first time an evidenced researched price reaches a sheet |
| D-120 | 18 Sep 2026 | James Gray, ruling on 401912-02: "**the spreadsheet rate is the controlling rate.** The engine should read the steel £/tonne input from the workbook, use that same figure in its calculation/report, and let the estimator amend it in the sheet when required. **It should not independently substitute £1.45/kg, £950/t or a config fallback.**" | every steel sheet part on every job | **FOUR RATES FOR ONE MATERIAL, AND ONE BOOK CARRIED TWO AT ONCE.** 401912-02 charged £3.07 — the workbook's own L5 at £900/tonne — and published £3.88 beside it from another path. Three readers took that as the engine and the workbook disagreeing about METHOD; it was a rate written in four places: £900/t on the sheet, £950/t in `WORKBOOK_INPUT_DEFAULTS` (which the engine used), £800/t in `MATERIAL_PRICE_GBP_PER_KG`, and a live £1.45/kg from the price service. The duplication is admitted in config's own comment — *"Blank sheet = £900"* written directly above a default of 950 — and `test_a_rate_lives_in_one_place` froze it as one of six pairs awaiting an estimator's ruling. **This is that ruling.** | `config.workbook_input_value` reads the ESTIMATING TEMPLATE's own cell (`WORKBOOK_SOURCE_MAP` → `Estimate!L5`), cached per run, and the config default becomes what it should always have been: the answer when the template cannot be reached, never a second opinion. **An estimator changes the rate where they already change it — in the sheet — and nobody edits Python.** The steel-sheet route is no longer abandoned for want of a per-kilo rate (the formula does not use one), and where the price service HAS an applied per-kilo rate it is refused with the refusal stated — a number quietly not used is indistinguishable from a number nobody had. The £200–£2,500/tonne clamp stands, and says when it fired. **Said only where there was a real second answer**: the config per-kilo fallback exists for every steel on the books and flagging it would have put a sentence on every steel part in the shop, which is how a warning stops being read. | generic — every steel sheet part on every job | `config.workbook_input_value`; the steel branch of `estimator.estimate_material` | `this commit` | `tests/test_the_sheets_own_cell_is_the_steel_rate.py` — 12 tests: a real template cell is read and named, an unreachable or nonsense cell falls back and says so, a live £1.45/kg does not displace the sheet, and the controls that an ordinary steel part is told nothing, a part with no blank is not forced down the route, and non-steel is untouched | implemented — one rate, from the sheet, stated in the engine and the report |
| D-118 | 18 Sep 2026 | James Gray on the 401912-02 book: "**One authoritative material basis and source must flow through both**" and "Ensure the correct flat DXF supplies the fold evidence/count. **why are we getting this incorrect ever?**" | every job — every material line, every folded part | **TWO NUMBERS, AND NEITHER SAID WHAT IT WAS.** 401912-02's divider reads "£3.07 · engine £3.88 — not charged". Three readers of that book, including me, could not settle what the difference was. James settled it: **£3.88 = 2.575 kg blank x £1.45/kg x 1.04 waste** — `mass_times_price_per_kg` on a LIVE per-kilo rate from the price service — while £3.07 is the workbook's own sheet formula at the £900/tonne in its L5 cell. So the two differ in METHOD *and* in RATE, and **four steel-rate paths are live at once**: £800/t (`MATERIAL_PRICE_GBP_PER_KG`), £900/t (the sheet's own cell, which is where it should be set), £950/t (`WORKBOOK_INPUT_DEFAULTS`, used by the engine's sheet formula) and the live £1.45/kg. Two of them are already on `test_a_rate_lives_in_one_place`'s frozen duplicate list; the live per-kilo path is a fourth. An afternoon went on looking for a costing bug that is, at root, one current price source nobody has resolved. **AND ONE FOLD, WITH NOTHING SAYING WHO COUNTED IT.** We are not getting the fold wrong, usually — but the sheet cannot say so. When nothing that can SEE the part has counted the bends, the count falls to the SolidWorks model or a drawing note; that fallback is correct and necessary, because plenty of parts have their folds only in a callout. What was missing is the sentence naming WHICH. 401912-02 charged thirty minutes of press-brake set-up on a count nothing had verified, and said "layers not provided" four hundred words away from it. **The correct flat DXF WAS staged and did supply the blank** — AI Provenance names `401912-02-01M_2mm_MS_revB.DXF (flat)` — so the open question is whether the export carries bend lines or whether the layers did not reach the route reader. Those need opposite fixes. | **Both bases are named beside both numbers.** `_material_basis_phrase` renders the engine's own `cost_method` and the facts beside it — "the blank's share of a sheet, 15 per 2500 x 1250, 2.575 kg blank" against "the part's stated weight (1.7 kg) x a rate per kilogram" — so a reader settles in seconds whether the disagreement is about method (a defect) or about a rate (a ruling somebody owes). The line says so explicitly: *where the two bases are the same, the difference is a rate written in two places, not a costing fault.* **And the fold row names its source.** `bend_count_source` has been on the record all along — `_model_measured_zero_bends` reads it to tell a measured zero from an absent one — and had simply never been said out loud on the line that spends the money. A measured count says what measured it; an unmeasured one says so. **The message says only what is known**: not "the export has no BENDLINES" — a claim about a file this run may never have parsed — but "THIS RUN DID NOT RECEIVE USABLE DXF LAYER DATA", naming both causes, because an export written without bend lines and a hand-off that dropped them send the reader to different departments. Where layers WERE read and none named bend lines, it says that instead. **Neither change moves a price.** | generic — every material line and every folded part on every job | `estimation_report._material_basis_phrase` and the not-charged clause; the folding block in `estimator.estimate_process_times` | `this commit` | `tests/test_a_number_says_where_it_came_from.py` — 12 tests, including that a nested basis and a weight basis cannot read alike, that a DXF WITH layers gets a different ask, and the controls that a part with no fold is told nothing and that neither label moves a time |
| D-119 | 18 Sep 2026 | A fresh container, and a test that had been green only by accident | the test suite itself | `test_the_example_answers_file_carries_howards_two_rulings_and_no_price` read `docs/7332-01_confirmed.example.json` — a file the blanket `*.json` rule meant git has never carried. It passed on the one machine that happened to hold it and failed on every other, including any fresh checkout. **That is D-111 again, in a test rather than a ruling**: a file nobody can diff is not evidence, and a test that depends on one is asserting about local state. | It reads `config.JOB_DECISIONS["7332-01"]`, which is committed, diffable, and the place the rulings actually live since D-111. | generic — the suite | `tests/test_the_canonical_route_keeps_stated_rows_apart.py` | `this commit` | the suite passes from a clean checkout, which is the whole point |
| D-117 | 17 Sep 2026 | "running through the portal" — and four 11908-21 books at D6 = 1 on a job queued for 50 | every portal run on every job | **THE ORDER QUANTITY NEVER REACHED THE ENGINE, AND NOTHING SAID SO.** 11908-21 was queued for 50 off and produced FOUR consecutive books at one off: set-ups amortised over one unit, every labour line divided by one, a unit cost comparable with nothing. The page said 50, the claim said 50, the workbook said 1. **An absent flag leaves no trace** — a cell reading 1 is identical whether the engine was told one or told nothing — so there was no symptom to chase, only a number that looked slightly high, four times, while we hunted defects in the costing. This is the third instance of one shape in this file: `--llm-only` (a full four-reader estimate wearing an LLM-only label) and `--quantity-breaks` ("the runner read None every time and the flag never once reached the engine") came before it. The difference is the cost: a missing break makes a run INCOMPLETE — variant workbooks that are simply not there — while a missing QUANTITY makes it **wrong, and wrong in a way that reads as ordinary**, because set-up over the wrong batch touches every line on the sheet at once. | The service already checks that the instruction it gave was carried out, against the runner's own command echo, which is the ground truth about what the engine was told. It checked `--llm-only`, `--fresh-read` and `--quantity-breaks` and not the number that matters most. It now checks `--order-qty` too, and separately catches a quantity that DISAGREES with the one asked for — told 3 when the page asked 50 is not a smaller version of being told nothing. It names what goes wrong rather than only that something did, and names the usual cause, because the remedy follows from it. **Said, not stopped**: the run may still be wanted and killing it would lose the hour; a guard that refuses is a guard people switch off. Silent at one off, which is the ordinary case. | generic — every run queued from the portal | `estimate_routes._check_the_engine_was_told` | `this commit` | `tests/test_the_quantity_the_page_asked_for_reaches_the_engine.py` — 10 tests, including the controls that the right quantity passes in silence, that a one-off is never warned about, that it is said once rather than per log line, and that prose on the log is not mistaken for the command echo | implemented |
| D-116 | 17 Sep 2026 | James Gray, ahead of the 401912-02 run (Tesco, Metal Divider — Tall): "1.7 kg is the drawing figure... **If the engine prints a different steel mass, flag it against 1.7 kg — do not silently overwrite the title block.**" | every part carrying both a stated weight and a measured blank | **TWO NUMBERS FOR THE MASS OF ONE PART, AND THE SHEET NAMED NEITHER.** The plausibility gate fires only outside half-to-three-times and says nothing inside that band. 401912-02 sits squarely inside it: a 460 x 356.6 blank of 2 mm CR4 is 2.58 kg and the title block says 1.7 kg — a ratio of 0.66. No flag, no choice recorded, and the material silently costed from the lighter figure. **Both numbers are usually right and they are not the same quantity**: a stated weight is the FINISHED part, a blank is what is BOUGHT. On this divider the difference is the triangular window — 0.88 kg of steel that is paid for and thrown away — so costing the net weight under-buys the material by exactly the size of the hole. On a part with no cut-outs the two should agree, and a gap means one of them is wrong. | **A MEASURED BLANK BUYS THE STEEL; THE WEIGHT CHECKS IT** — his own rule for this run, "steel cost from nested blank area, with 1.7 kg only a sanity check", and the transaction it describes: a laser part is nested on a sheet and paid for as its share of that sheet, cut-out and all. The window is scrap that was bought. Costing the finished weight is buying back the hole. **Scoped to a MEASURED flat pattern**: where the blank came from a DXF it is the stronger fact and the weight is the check; where there is no DXF the blank is PDF vision, often garbled, the printed weight is stronger, and that behaviour is untouched. Either way the disagreement is REPORTED, with both figures, which one costed the line, and what the difference means — "the line is light by 0.88 kg" rather than "they differ". A stated weight HEAVIER than its own blank gets a different sentence, because a flat part cannot be, and that is an error rather than a cut-out. **The title block is never overwritten** — it is a fact about the part, not about the price — and where the two agree nothing is arbitrated and nothing moves. Tolerance is a config lever (10% default) so a flag that fires on every part cannot become one that is read on none. | generic — every stated weight on every job | `estimator.estimate_material`, after the plausibility gate | `this commit` | `tests/test_the_title_block_and_the_blank_both_get_a_hearing.py` — 12 tests, including the controls that agreement says nothing, that a part with no blank to check against says nothing, and that a part with no measured flat pattern still costs from its printed weight | implemented |
| D-115 | 17 Sep 2026 | The 11908-21 book of 17 Sep 17:18 — build `9399edc`, the first book where Tony's rates could reach the page | every job carrying a rate a department stated | **ONE BOOK, TWO OF HIS RATES AND TWO CORPUS MEDIANS, WITH NOTHING SAYING WHICH.** Bench came through at Tony's measured 2/hr and Machines at his 12/hr — and CNC at 22.5/hr against his 12, and Packing at 75/hr against his 20. The cause is the derived-throughput gate treating a STATED rate as a DEFAULT: the derived figure wins unless it is more than five times away. Bench and Machines happened to fall outside that guard and took his numbers; CNC and Packing fell inside it and overwrote him. The rule was already written in this same file for stated TIMES — *"corpus medians are evidence about jobs in general. A stated time is evidence about this one, and it wins."* A stated THROUGHPUT is the same fact in the other unit and was getting the opposite treatment. **Separately: no Edge Banding row appeared.** The confirmed metres were stamped on the ASSEMBLY PARENT, whose fabrication route is suppressed by design — so the tape was bought and the operation was dropped without a word, which is the silent no-op the whole confirmed-answers mechanism exists to prevent. | A rate the department stated is **used as the rate**, and where the job's own hours derive something different **both numbers go on the row**, because an estimator seeing 12/hr where the arithmetic implies 22.5 needs to know the engine chose and why. The ceiling and floor guards are untouched for every unstated row: they exist to catch a garbage derivation, not to arbitrate between a median and a measurement. The bander is routed on the **biggest faced panel** — a real fabricated part with a live route — and the length is divided by that part's own quantity, because the row multiplies by it: five metres a TRAY stays five metres a tray however many of that panel go into one. A job with no fabricated panel to carry it says so. | generic — every stated rate, every confirmed banded length | `wb_populate` derived-throughput gate and `_STATED_THROUGHPUT_ROWS`; the confirmed block in `file_scan` | `this commit` | 4 more tests in `tests/test_the_book_is_written_from_the_costed_record.py`, including the control that every unstated row keeps its ceiling and floor | implemented |
| D-114 | 17 Sep 2026 | The 11908-21 book of 17 Sep 16:39 — build `3629222`, restarted runner, every fix in the process | 11908-21, and every faced-board job | **THE ENGINE GOT IT RIGHT AND THE WORKBOOK WROTE SOMETHING ELSE.** The saved record was correct throughout: board promoted to MFMDF, laminate ruled out of the route with the reason recorded on all three trays, edging carrying Tony's confirmed 5 m at a researched rate, delivery his deliberate exclusion. The book carried almost none of it — `Glue laminating 9 off 40/hr set-up 30 = £18.43` written after the ruling was recorded; Bench 30/hr against his measured 2/hr and Packing 75/hr against his 20/hr; every labour row reading "9mm MDF" on money that was MFMDF's; and the tape bought with nobody routed to apply it. **Four hand-offs, each failing the same way: a fact is established in one place and the place that spends the money asks a different question.** | **THE RULING** — the canonical grader never asked whether an operation was ruled out. The legacy grader beside it always has, in its own words "the last gate before a labour row, and the one that spends money". The decision graph is COMPILED before the ruling exists, so compiling is too early and rendering is the only moment that can catch it; D-102 one road over. A ruling on one participant still does not cancel a row doing the work for others. **THE SCOPE** — `_faced_board_job` searched `bom_parts`, and a board panel is never in `bom_parts`: the router sends it to `board_parts`, which is what the Other Sheet block is for. The one list that could not hold a faced board was the only list consulted, so the scoped pilot has never applied to anything. **THE NAME** — a row now carries `costing_material_family`, so a promoted board reads MFMDF instead of MDF; three readers of that book concluded the engine was costing raw board when it was not, and the argument cost more than the defect. **THE WORK** — a confirmed banded length is a ROUTE fact as well as a material one: it stamps the metres on the part, routes `edge_banding`, and the labour rule times the BANDED EDGE instead of the perimeter it had been using — the default D-104 outlawed for the material and left standing in the rule beside it. Where nothing establishes a length the perimeter is still used, flagged as the ceiling it is. | generic — every faced-board job, every ruled-out operation on the canonical road, every edge-banded part | `wb_populate.canonical_labour_groups` (ruling gate, costed family), `_faced_board_job`; `estimator.estimate_process_times` (edge-banding time); the confirmed block in `file_scan` | `this commit` | `tests/test_the_book_is_written_from_the_costed_record.py` — 11 tests: a ruled-out op gets no row, a ruling on one part does not cancel a shared row, the row names the family that was costed, 5 m times the bander above the perimeter, and the controls that an unruled op is untouched and a plain board still reads as itself | implemented — prove on the 50-off rerun |
| D-113 | 17 Sep 2026 | James Gray: "**it was ran after all the latest git pulls**" — and he was right, and so was the book | every run on every long-lived runner | **A PULL INTO A RUNNING RUNNER CHANGES EVERY FILE AND NOTHING THE PROCESS IS RUNNING.** The 16:25 11908-21 book came back with the old glue charge, the old joinery rates, no edging line and delivery still open, against a checkout at `3629222`. Both halves of the contradiction were true: the checkout WAS current and the run was NOT made by it. The estimating process was already up; it had imported the engine hours earlier, and a pull changes files on a disk, not modules in a live interpreter. **Two full rounds of assessment went on reading a book that could not have contained the fixes, hunting defects in code that was never executed.** `build_stamp` held the answer the whole time — `Build: 9299fa5 +local edits` in the Provenance header against a checkout at 3629222 — and was only asked AFTERWARDS. That is the module doing exactly what it was written for, one round trip too late. | The run states it BEFORE it costs anything, on two independent tells, either of which is enough. **SOURCE NEWER THAN THE PROCESS**: a `.py` on the import path with an mtime later than the moment this process imported the engine — the pull-into-a-live-runner case exactly, needing no git at all, and reported with HOW LONG AGO, because "somebody is editing" and "you pulled after this started" are different situations with different remedies. **HEAD IS NOT WHAT WE STAMPED**: `git rev-parse` now against the commit this process recorded, naming BOTH commits, because "you are stale" without the two numbers is not something anyone can check. Printed as a banner above the run rather than a line that scrolls past, and **carried onto the book**, because the console scrolls and the book is what gets forwarded. It does NOT stop the run — the numbers may still be wanted, and a warning that refuses is a warning people switch off — and it stays silent on a healthy run, which is the half that keeps it readable on the day it fires. | generic — every run, every runner | `build_stamp.source_changed_since_import` / `stale_process_warning` / `print_build_stamp`; the Provenance header in `estimation_report` | `this commit` | `tests/test_a_stale_runner_says_so_before_it_costs_anything.py` — 11 tests: a pull an hour after import is detected and timed, a moved HEAD is caught on its own and names both commits, the banner reaches console and book, and the controls that a fresh process says nothing and that the warning never stops a run | implemented — the 16:25 book is not a defect report, it is a stale-process report |
| D-112 | 17 Sep 2026 | Asked before Tony's 50-off run: "**will it price the MDF ?**" — checking the answer end to end rather than asserting it | every board job, and every caller of the researched rung | Two faults found by following the chain the answer depends on. (1) **THREE PARTS OFF ONE SHEET WERE RESEARCHED THREE TIMES.** 11908-21 cuts its base, side and divider from the same 9mm board. Each part asks rung 4 independently, so one board would produce three calls and — far worse than the waste — **three answers**. A sheet showing one material at three different £/m² is not a rounding difference an estimator waves through; it is visibly incoherent, and it would be this engine's own doing. (2) **THE POLICY SWITCH WAS A LIE FROM ONE CALLER.** `FALLBACK_PRICING_POLICY.enable_web_ai_fallback` is how the office stops a run reaching outside — for a machine that must not, or while a provider misbehaves. The bought-in chain honoured it upstream; the board path reaches the researcher directly and ignored it. A switch one caller obeys is worse than no switch, because it is believed. | (1) The researched rate is cached on the **family and the gauge** — what the rate is a property of — for the life of the run, and the MONEY is recomputed from it for each blank's own area, so one board carries one rate and every part still carries its own cost. **A miss is cached too**: a board nothing can price must not be researched again for every part cut from it. (2) The switch is checked in `_rung4_researcher`, where both callers arrive, so it means what it says from either. | generic — every board on every job; the switch, for every researched line | `estimator._researched_board_rate_m2`, `_rung4_researcher`, `_RESEARCHED_BOARD_RATE_CACHE` | `this commit` | 3 more tests in `tests/test_a_board_is_entitled_to_the_last_rung.py`: three parts off one sheet research once and still cost differently, an unpriceable board is asked about once, and the lookup does not run with the fallback switched off | implemented |
| D-108 | 17 Sep 2026 | James Gray: "**it's lame not to price the MDF and Tony is sarcastic and will laugh about it.**" and "**SDI Live will have prices all over the place in it also. SDI does it all the time.**" | 11908-21, and every board on every job | Two faults, both leaving a real material with a real measured area reading as unpriced while SDI's own money sat on file or a current rate was one lookup away. (1) **The catalogue was asked by one name.** `_sheet_catalogue_token` takes the longest word in the material's own name, so a faced board is looked up as "MFMDF" and nothing else. A purchasing system is written by buyers over years and the same board arrives under whatever the supplier's invoice called it — "Melamine Faced MDF" on one row, "MFMDF" on the next. One spelling asked, the other ignored. (2) **Rung 4 was wired into one chain.** The researched-price producer was built for BOUGHT-IN lines and reachable only from the bought-in chain. A board is a material, so once rung 1 missed it fell off the bottom of the ladder with nothing below it. | (1) `_sheet_catalogue_probes` returns every name the catalogue might hold the material under, **most specific first, and the first that answers wins — never pooled**, because two spellings can be two different boards. Each synonym carries a family guard word: "MELAMINE" alone matches melamine-faced chipboard and melamine-faced MDF equally, and one rate answering two questions is the fault this codebase keeps finding. Vocabulary lives in `config.BOARD_CATALOGUE_SYNONYMS` — names SDI's own catalogue uses, and adding one cannot put a price anywhere. (2) **Rung 4 is not a property of being a bought-in; it is the last rung, and every line is entitled to it.** A promoted board with no catalogue rate is researched PER SQUARE METRE — the only basis that survives a change of stock size, since a £/sheet figure is worthless without the sheet it was for and the two travel apart. One researcher serves both callers, because two copies of a price source is how two answers to one question start. Evidenced or nothing; labelled rung 4 everywhere; the contamination guard unchanged; and a sheet price answered "per each" is refused by D-108's unit check rather than multiplied by the area. | generic — every board on every job, and every material that reaches the bottom of the ladder | `estimator._sheet_catalogue_probes`, `_researched_board_rate_m2`, `_rung4_researcher`; `config.BOARD_CATALOGUE_SYNONYMS` | `this commit` | `tests/test_a_board_is_entitled_to_the_last_rung.py` — 14 tests: 0.1521 m² at a researched £24.50/m² prices the tray at £3.88 a part, a per-each sheet price is refused, research that answers nothing leaves the line open and says so, and the controls that plain MDF gains no synonyms and the melamine probe cannot answer for chipboard | implemented — prove on Tony's 50-off rerun |
| D-107 | 17 Sep 2026 | James Gray: "**I don't understand why we can't price edging**... confirmed banded metres x current £/metre. we can get a current £/metre from an LLM or from SDI Live and what does confirmed banded metres mean? The length?... **so, we should be able to price in this case.**" | 11908-21, and every length-bought or mass-bought line on every job | He is right, and the refusal was too wide. D-104/D-105 established that the METRES are the drawing's to answer and never the perimeter — correct, and on 11908-21 it left the line dead, because those DXFs carry no layer data and no note names an edge. **Tony had already said 5 m, and there was nowhere to put it.** A stated physical extent of the product is the same class of fact as "the tube bend is not required": it is a LENGTH, not a price, and D-078 has nothing to say about it. Separately, the rate half had a fault nobody had hit yet: the research brief asks for a price "PER EACH" on every bought-in line, and the producer then multiplies the answer by the line's QUANTITY — which for edging is metres. A researcher asked for the price of "one ABS edging" prices a REEL, and £210 a reel × 5 puts £1,050 of tape on a tray. Every field in the evidence block is present and true; only the unit disagrees. | **EDGING = CONFIRMED BANDED METRES × CURRENT £/METRE**, and the two halves keep their owners. (1) The answers file takes `banded_metres`, in either form estimators state it — a number meaning metres per FINISHED UNIT (which REPLACES what the parts measured, because it already is their sum), or `{part: metres}` meaning metres per ONE of each part named. It ranks above every measurement in `edge_banding`, because those infer the design intent and this states it; a price in that key is refused, and zero is not a confirmation. (2) The research brief asks in the LINE'S OWN UNIT — `unit_of_measure` now reaches it, having been recorded under one name and read under another — and warns against pricing a reel, roll, pack or coil. (3) **The producer REFUSES a figure returned in the wrong unit rather than converting it**: dividing a reel price by a reel length nobody stated would be inventing the quantity this engine exists not to invent. The refusal names both units, which is enough for a person to ask the right question. | generic — any line bought by the metre, the kilogram or the square metre, on any job | `estimator_confirmed.banded_metres`; `edge_banding` rung 0; `indicative_price.research_brief` / `resolve_indicative` unit check; `web_ai_price_lookup._build_spec_block`; the edging mint in `estimator.estimate_document` | `this commit` | `tests/test_confirmed_metres_times_a_current_rate.py` — 21 tests, behavioural: a confirmed extent outranks a measured layer, £1.85/m × 5 m prices the line at £9.25, a £210 reel answered per each is refused and NOT converted, and the controls that an each-line is unaffected and the contamination guard still bars an estimator's sheet | implemented — and 11908-21's figure IS filled in: James settled the basis ("Tony's 5.0m per finished tray as a job-specific physical measurement... equivalent to Tony confirming, 'this tray requires 5 metres of 23 x 1mm ABS edging'"), so `banded_length_m: 5.0` is in the answers file with his name and date. His historic per-metre rate is deliberately not |
| D-106 | 17 Sep 2026 | James Gray on the 11908-21 book of 15:18: "Laminated MDF is still costed as raw MDF: 3050 x 1525 at £43.12, not the faced-board family and its 3080 x 1220 stock size. That repeats Tony's 'wrong sheet size / wrong board price' finding... Bench Work is at 30/hr, not the scoped 2/hr; Packing Joinery is 75/hr, not his 20/hr. **One main technical root: the `MDF+ LAM` evidence is not promoting the live parts into the faced-board family.**" | 11908-21, and every board job SDI holds a core rate for | **THE PROMOTION WAS UNREACHABLE ON EVERY REAL JOB, AND ELEVEN TESTS SAID IT WORKED.** `_faced_board_promotion` is correct and was proven correct — in a test, where there is no database. The live run asks the UDEF catalogue for a £/m² on the material AS READ, finds plain 9mm MDF, prices the blank by area and RETURNS, four hundred lines above the faced-board block. The block's own rule — "once promoted the part may NOT fall back to the bare core's money" — was enforced against the £/kg path and against nothing else. £9.27/m² across a 3050 x 1525 is £43.12: a real, current, checkable rate, for a board this job does not buy. And because the promotion also stamps `_laminate_in_board`, the whole faced-board pilot went with it: the scoped joinery rates Tony's review supplied never saw a faced-board job, so the sheet ran on the department guesses he had just corrected. **One unreachable branch, four of his findings.** | The promotion is asked ONCE, in front of every pricing path, and the CATALOGUE IS ASKED ABOUT THE BOARD ON THE PURCHASE ORDER. Promoted with a live faced-family rate: priced from it — rung 1, current, checkable, and nested on the faced family's own stock sizes (3080 x 1220, not plain MDF's 3050 x 1525). Promoted with none: it falls through to the faced block and reaches the sheet as a VISIBLE UNPRICED LINE — **a promoted board never borrows the core's rate**, by an explicit guard on the branch rather than by luck of ordering. Not promoted: unchanged, and a control test holds that half still. The sheet-yield block and the stated-weight branch now READ the answer instead of asking again, so the evidence flag lands once. The returned record carries `costing_material_family`, which is what wb_populate's scoped joinery rates read. | generic — every laminated board on every job | `estimator.estimate_material`: the promotion hoisted above `_resolve_board_sheet_rate_gbp_per_m2`; the live-UDEF branch nests and reports on the costing family | `this commit` | `tests/test_the_catalogue_must_not_overtake_the_faced_board.py` — 9 tests driving `estimate_material` with a stand-in catalogue that answers for the core, for the faced family, or for both; **8 of the 9 fail on the parent commit**, which is the proof the old suite could not give. Control: plain MDF with no laminate evidence still prices from the live catalogue | implemented — prove on Tony's 50-off rerun |
| D-105 | 18 Sep 2026 | James Gray: "**have we applied edging though?**" | 11908-21, and every faced-board job | D-104 measured the banded metres correctly and then raised them as an ESTIMATOR-INPUT ASK. That is not what Tony asked for and it is not what an estimate is. His words were "no ABS edging Allowed" — allowed meaning COSTED. Edging is material bought by the metre, and James's own acceptance criterion said so: "ABS edging as a **distinct length-priced line**". Half a fix, and the half that does not reach the money. | The edging is MINTED AS A BILL-OF-MATERIALS LINE beside the packaging, delivery and plating lines, so it flows through the canonical graph and onto the sheet exactly as they do — a code (EDGE23X1ABS), a supplier identity, and a QUANTITY IN METRES. The metres are D-104's: only the edges the drawing marks, never the perimeter. **No marked edges, no line** — an edging line on a part nobody bands is the perimeter mistake with a price on it. **The rate is deliberately NOT set at the mint site**, and a test enforces that: it flows through the same chain as any bought-in — SDI Live, supplier catalogue, current quote, then a researched figure with its evidence — so a rate the office already holds wins, and where nothing answers the line reads as unpriced with its metres visible. Setting a rate here is how a manual-sheet number gets in through the back door. A failure to build the line is printed, because a missing edging line looks exactly like a part that is not banded. | generic — every faced-board job | `estimator.estimate_document`, the edging mint | — | 7 more tests in `tests/test_only_the_edges_the_drawing_marks_are_banded.py`, including one that FAILS if the mint site ever sets a price field | implemented |
| D-104 | 18 Sep 2026 | James Gray: "The engine should derive banded metres from the drawing/DXF, not ask Tony to type a length already shown by the design... **We must not use the whole visible perimeter by default: Tony's own 5 m shows that only selected exposed edges are banded.** Geometry can measure an edge precisely, but cannot know it needs ABS unless the drawing marks it." | 11908-21, and every faced-board job | Tony Ford: "Not all materials calculated **no ABS edging Allowed**". The engine measured 2x(L+W) of every faced part and offered it as "the drawn edge metreage" — on a 390x390 tray at 2 off that is 3.12 m a unit before anything else is counted, and Tony's WHOLE JOB is 5 m. **The perimeter is always computable and almost never the answer**, which is the most dangerous combination a default can have: it looks like a measurement. | TWO QUESTIONS, KEPT APART, because they are different in kind. HOW LONG IS THIS EDGE is geometry — to the millimetre, needing nobody's opinion. DOES THIS EDGE TAKE ABS is a DESIGN DECISION that nothing in the geometry records: a tray with four sides may band one, two or all four, and the only place it is written down is the drawing. So the banded length is read from the drawing in three ways, in order of trust: (1) a DXF LAYER whose name means edging — the drawing office drew exactly the banded edges, so their measured length IS the answer, and `dxf_probe` now reports `length_mm_by_layer` because a total cannot answer "how much of this is banded"; (2) a NOTE naming the edges — "ALL ROUND" is a STATEMENT and does mean the perimeter, which is not the same as assuming it in silence; "two long edges", "front edge" name a subset and the subset is measured off the blank; (3) banding stated with the extent unresolved — RAISED, not measured. **No evidence, no length.** The perimeter is still reported beside it as a visible CEILING and is never offered as the answer, and the refusal NAMES WHAT IT LOOKED FOR so an estimator can tell "this part is not banded" from "we could not find the note". | generic — every faced-board part on every job | `src/edge_banding.py`; `dxf_probe.length_mm_by_layer`; the edging input in `wb_populate` | — | `tests/test_only_the_edges_the_drawing_marks_are_banded.py` — 16 tests, including the control that a bare "EDGE OF PLINTH" is a location and not an instruction, and a guard that the 2x(L+W) default cannot return | implemented — the RATE is a separate question and remains SDI Live / supplier / evidenced research; the edging spec identity (EDGE23X1ABS) was already held |
| D-103 | 18 Sep 2026 | Tony Ford, 11908-21 at 50 off: "Wrong sheet size for Laminated board. Wrong price for board." James: "We will learn Tony's methods and timings from his sheet, but not transplant his prices." | 11908-21, and every board job | `BOARD_SHEET_PRICE_GBP` convicted itself in its own comment: the 9mm point read "£172.00 a 3080x1220 sheet ... **stated by Tony Ford in his own estimate** for 0359967 at the 50-off rate". A number off an estimator's sheet, which is what D-078 forbids and D-095 withdrew from the commodity table — still live, and still reaching board money. The Dibond points were worse: "PROVISIONAL ... mid trade", nobody's figure at all. | Every price withdrawn. **The material families and the SHEET SIZES stay**, and that answers Tony's OTHER point: a sheet size is a specification — what the supplier sells — not a price, and £172 buys a 3080x1220 while the stocked list also offers a 2800x2070 that yields more parts. Divide the money by the wrong sheet's yield and every part on the job is understated. The size has to survive for the yield to be right whatever the price turns out to be. `_board_sheet_rate` skips a point with no money, so a board falls to the rungs that can answer and lands in the engine's existing `faced_board_unpriced` / `no_price` state — **named, not a silent zero, and not reverted to plain-MDF kilo money**, which is the protection the laminate tests exist for. The supplier quantity breaks his sheet carries (1/50/100/250/500) are a METHOD worth having, recorded here; they are not five more prices to type in. | generic — every board job | `config.BOARD_SHEET_PRICE_GBP`, `estimator._board_sheet_rate` | — | Five tests moved to the new truth. The interpolation arithmetic is still covered, now against points injected in the test rather than against frozen data — the algorithm was never the problem | implemented |
| D-102 | 18 Sep 2026 | James Gray: "Load Howard's tube-bend and shared-nest rulings into the actual live job run. Correct 004's laser calculation and shared set-up allocation." | 7332-01 | Two rulings Howard made on 15 Sep had never reached a run: the tube bend was charged on three consecutive books, and 003/004 each carried a full 10-minute set-up — 20 minutes where the shop runs one program. The mechanisms for both were built (D-097, D-098) and the job had no answers file to carry them. Separately, 004's laser rate came out at 3,340/hr against Howard's 900: the estimators' calculator times the CUT, and on a 15.88 mm square cap the cut path is a few centimetres, so the formula had the part loaded, pierced, cut and taken off in 1.08 seconds. | (1) `docs/answers/7332-01_confirmed.json` — the rulings in the form the runner reads, with the destination path on the file itself, validated through `estimator_confirmed._read_decisions` with NO complaints. The shared-nest ruling then satisfies the existing one-set-up allocator, which was already correct and only ever needed `laser_nest_proven`. A comment key beginning `_` is no longer reported as a decision that did nothing — the example file teaches writing `_why_...` beside a ruling, and an estimator reading three complaints about their own notes learns to stop writing them. (2) A FLOOR UNDER THE SECONDS, not a cap on the rate: `LASER_MIN_SECONDS_PER_PART = 4.0`, applied as `MAX(V,4)` INSIDE the template's own formula so the calculator is still visibly doing the work and still tracks any change the estimators make to their cutting speeds. Four seconds is Howard's 900/hr read back — a THROUGHPUT, which the pricing policy allows us to log, not a price — and a scoped pilot from one line until the laser department confirms it. What it represents is re-derivable: a part still has to be picked up, placed, pierced and taken off, and none of that is in a cut-length calculation. 003 at 15.25 s a part is untouched, which is the point of a floor. | generic — every small laser part on every job; the rulings are scoped to 7332-01 | `config.LASER_MIN_SECONDS_PER_PART`, the laser formula writer, `estimator_confirmed`, `docs/answers/7332-01_confirmed.json` | — | 7 tests in `tests/test_the_canonical_route_keeps_stated_rows_apart.py`, including the control that the strap is unaffected and that the answers file parses clean | implemented — the answers file has to be COPIED to the job folder on the share before the run |
| D-101 | 18 Sep 2026 | James Gray: "we don't need disclaimers.. the estimator will make any necessary changes before it gets sent out.. **this obsession needs to stop. I keep insisting that this is not necessary but it keeps slipping back in**" | all jobs | He is right, and the record shows it is the THIRD time. `client_quote_html` already carries his removal of a red "LLM-ONLY MEASUREMENT RUN — NOT A QUOTATION" banner, with his reason on the file: "The estimator takes responsibility. remove this sort of alarming disclaimer." D-096 then had the quote REFUSE TO RENDER over an open line, and D-100 wrote a `_quote_NOT-RELEASABLE.html` file in its place. I quoted his own words about the banner in the comment above the code that did it. | Every refusal removed. The quote renders; the report leads with its own content; nothing declines to produce an artefact over an unpriced line. What is kept is the half that was always useful: `release_gate` still lists WHICH lines nothing answered and what would close them, and `line_is_answered` still holds rung 4 to its evidence — that is information an estimator acts on, not a verdict on their judgement. A document that tells a professional not to trust it is not a control; it is declining to do the work and hoping the reader supplies the judgement anyway. **A test now fails if `NOT RELEASABLE` or `NotReleasable` reappears in either renderer**, because being told three times and writing it a fourth is the failure mode this needs protecting from, not the code. | generic | `client_quote_html` (refusal and exception removed), `estimate_explained` (banner removed), `release_gate` (framing rewritten to a list of facts) | — | `test_no_deliverable_refuses_to_render_over_an_open_line` | implemented |
| D-100 | 18 Sep 2026 | The 7332-01 six-off run of 17 Sep 14:06 — and its CLIENT QUOTE | 7332-01 | **THE QUOTE WENT OUT.** £127.86 a unit, £767.17 an order, no block, no caveat — with plating, plater freight, packaging and delivery all at £0.00. Four required costs silently excluded from the one document that leaves the building, on a page a customer would read as a price. It also listed "Tube bending and forming" under What's included, on a leg Howard ruled has no bend. D-096's release gate had already decided this estimate was not releasable and the REPORT consulted it; the QUOTE never asked. **A gate the most dangerous renderer does not consult is not a gate.** Separately: the felt pad came back at £0.22 from the pre-existing web/AI rung labelled "AI market indication (RS Components +2 more)" — something like a source and nothing else: no date, no unit basis, no quantity basis. It reached the total. | The quote asks the gate FIRST, before the page is laid out, and RAISES rather than rendering: a quotation that is wrong about the money has no safe rendering, and a banner at the top of an otherwise normal-looking page is an invitation to scroll past — this file already records a previous banner removed for making the page unreadable. The caller writes the refusal to `<job>_quote_NOT-RELEASABLE.html`, because a run that merely failed to produce a quote looks from a folder listing exactly like one that has not finished, and last run's quote for the same job is still sitting there ready to be attached by mistake. The estimate itself is untouched: still on the workbook, still explained in the report, simply not yet a quote. The unevidenced AI figure is caught by the gate already — `line_is_answered` refuses it for the three missing fields — so the pad blocks rather than totalling. | generic — every job's quote | `client_quote_html.NotReleasable`, `build_quote_html`, `generate_quote_files` | — | 6 more tests in `tests/test_a_required_line_with_no_price_stops_the_estimate.py`, including the control that a releasable estimate still quotes, and the 14:06 pad asserted as NOT answered | implemented — **what the run proved and did not.** PROVED: brushing 1.5/hr, pack-out 15/hr, final pack 7.5/hr, the generic PACM row no longer borrowing a stated time, H67 at 1.0 mm, D6 at 6. NOT PROVED: the tube bend is still charged (the answers-file ruling is not on the runner), and no line yet carries an evidenced rung-4 price |
| D-099 | 18 Sep 2026 | James Gray: "implement the central rung-4 producer so it can return an evidenced LLM-indicative price for a required line... for bought-in components such as the felt pads; packing and delivery; specialist bought-in processes such as plating and plater freight. For each it must retain: source/evidence, date, unit, quantity basis, calculation, and the 'LLM indicative — review required' status. It must not read Howard's £250 or £120/£20 figures, nor any config literal." | all jobs | D-095 withdrew the typed prices and D-096 stopped the engine publishing around the resulting blanks. Neither PRICED anything, so the honest next run would have been a page of refusals. The missing half is the rung that can actually answer. | **THE DIFFERENCE IS NOT THE LABEL.** The withdrawn config entry called itself INDICATIVE in those words, so a longer disclaimer would rebuild exactly what was deleted. What makes rung 4 admissible is that it CAN BE CHECKED: source, the date it was true, what it is per, the quantity it was found at, and the arithmetic from that figure to the money on the line — so an estimator can disagree with ONE STEP rather than with the whole number. Miss any one and there is no price: not a smaller one, not a zero, and the line says which source would have answered it. THREE CLASSES, because they are researched differently: a bought-in component is a trade unit price times the drawing's quantity; a commercial line is priced FOR THE ORDER and divided by it, so it moves with the quantity instead of freezing at the one it was found at; a subcontract process is a rate against the part's OWN measured property — mass, coated area — with any batch minimum spread over the order, never a lump sum for "a job like this", which cannot be re-derived. **CONTAMINATION IS BARRED AS AN INPUT, NOT ONLY AS AN OUTPUT:** Howard's £250 does not become a price by being handed to a model and returned as its answer — that is laundering, and it produces a figure that LOOKS researched and is not. A poisoned brief never reaches the researcher, and the test proves the researcher was not called. | generic — every unpriced required line on every job | `src/indicative_price.py`; wired into `estimator._resolve_part_system_cost` at the seam the withdrawn commodity table used to occupy, consulted LAST so a real catalogue/UDEF/history rate still wins; the engine's own `web_ai_price_lookup` is injected behind the producer's seam | — | `tests/test_a_researched_price_is_one_that_can_be_checked.py` — 25 tests, most of them REFUSALS, because the refusals are what stop this becoming the config table again with a longer label; plus controls that a clean input is not barred and that a real rate still outranks it | implemented — **the six-off rerun is now decisive either way**: every required line carries an evidenced price and the gate passes, or the workbook blocks naming the precise missing source |
| D-096 | 18 Sep 2026 | James Gray: "the estimate must be blocked as incomplete, not totalled with a zero... It must never show a normal-looking £108.89 unit price that quietly excludes four required costs"; "an LLM indicative price may contribute to the estimate total... If that evidence cannot be produced, the line makes the estimate incomplete and blocks release. It never becomes £0" | all jobs | Withdrawing the typed commodity prices (D-095) was right and ON ITS OWN MADE IT WORSE: the felt pad went from a wrong price to NO price, and no price reaches the sheet as a blank the total steps over. A zero is the one answer that is never true — the pad costs something, the plating costs something, the freight costs something. An estimate that adds up as though they cost nothing does not look incomplete; it looks FINISHED. | A RELEASE GATE, kept deliberately separate from the sufficiency verdict because they answer different questions: sufficiency is about CONFIDENCE and its honest answer can be "priced, and thin in these places"; release is about COMPLETENESS and its only honest answers are yes and no. A thin estimate is publishable with its doubts named; an incomplete one is not publishable at any confidence, because the number it shows is not the price of the thing. A zero is valid ONLY where somebody decided it — free issue, not required, estimator-excluded, customer-supplied; anything else is an unanswered question and it blocks. **RUNG 4 IS ADMISSIBLE WITH ITS EVIDENCE AND NOT WITHOUT IT:** source, date, unit basis, quantity basis. That is the whole difference between a researched price and the literals just deleted — those were labelled INDICATIVE too, and neither a typed figure nor a researched one can be told apart on a sheet unless the researched one names what it can be checked against. Each missing field is reported in words, because "not evidenced" is not something an estimator can act on. The gate FAILS CLOSED: if it cannot run, the estimate is not releasable, because an estimate nobody checked is not an estimate that passed. The report leads with the verdict, above the total. | generic — every job | `src/release_gate.py`; `estimator._assess_estimate_data_sufficiency`; `estimate_explained` | — | `tests/test_a_required_line_with_no_price_stops_the_estimate.py` — 18 tests named for James's four items: a felt pad, pallet, clip and screw each either obtain an allowed price or block release visibly, with controls that a ruled zero passes and an invented reason does not | implemented — the resolver that FILLS rung 4 is the pricing path's job and is unchanged here; this is the gate that refuses to publish without it |
| D-097 | 18 Sep 2026 | James Gray: "For 7332-01-003 and -004, share one 10-minute laser set-up. Howard's explicit statement is the proof for this job... For every other job, shared set-up requires either a proven combined nest or an estimator-confirmed nesting group. Same gauge alone is never enough." | 7332-01 | D-074 made shared set-up conditional on a PROVEN nest, and the proof is arithmetic over `parts_per_sheet`, which the nester did not produce for 003/004 — so each kept a full 10-minute set-up and the job booked 20 minutes where the shop runs one program. The gate was right to refuse; what was missing was the other kind of proof. | Two proofs, and the second is the stronger: a person who knows the job. `estimator_decisions.nesting_groups` — {group name: [part numbers]} in the job's own answers file, so it governs one drawing and never travels. Members are named EXPLICITLY, because same gauge alone is never enough and a ruling about 003 and 004 says nothing about a third component that shares their gauge. A group of one is rejected: it shares nothing. The confirmed group is consulted only where the arithmetic has not already decided — it is the second proof, not a louder one. | generic (the mechanism); this job's ruling is scoped to 7332-01 | `wb_populate._confirmed_nesting_group`, `estimator_confirmed._DECISION_KEYS`, `docs/7332-01_confirmed.example.json` | — | 8 tests in `tests/test_the_canonical_route_keeps_stated_rows_apart.py`, including the control that no answers file means no shared set-up | implemented |
| D-098 | 18 Sep 2026 | James Gray: record Howard's tube-bend ruling; and the six-off book's three unlabelled PACM rows | 7332-01 | THREE FAULTS IN WHAT AN ESTIMATOR READS. (1) `operations_off` matched the operation by exact spelling, and the engine emits `tubebend` on the route while the example answers file spells it `tube_bending` — so an estimator copying that file would have ruled the operation off, seen no error, and had no effect. The strongest evidence there is, discarded over an underscore. (2) The example answers file itself carried `plating_gbp_per_unit: 250.0` — Howard's own figure off his own sheet, the exact thing D-078 forbids, in the document the next person copies. (3) Three "Assemble/pack (Metal)" rows of which ONE was labelled, so the 7.5/hr final pack could not be told from the generic row except by arithmetic. | (1) Matched on the letters, so any spelling of the same word answers to the ruling. (2) The price key is gone from the example and the file now says WHY in its own text. (3) Each pack row says which it is — "1 of 2 — PACK TO PLATER", "2 of 2 — FINAL PACK AFTER PLATING", or "general assembly and pack — NOT a plating pack stage". And the labour row's gauge asks the recorded substitution, so 008 no longer reads 1.0 mm on the material row and 0.9 mm on its laser row — one gauge on the whole sheet. | generic | `estimator` operations_off matching, `wb_populate.labour_row_description` and the canonical grouper's thickness read, `docs/7332-01_confirmed.example.json` | — | tests updated in place; the example file is asserted against | implemented — verify on the six-off rerun |
| D-095 | 18 Sep 2026 | James Gray on the six-off book: "P/P felt pads priced from config... Remove the literal entirely. Retain the BOM identity and quantity, but hold it as awaiting a current SDI Live/supplier source"; "the £0.20 felt-pad rate is still contrary to the agreed policy, so this workbook should not go out" | 7332-01, and every job | `STANDARD_COMMODITY_PRICE_GBP` held five figures reaching estimates as money — a pallet at £12.00, a perforated-panel clip at £1.20, the felt pad at £0.20, a wood screw at 3p, an M4 at 8p. The table's own `source` fields convict it: "derived from a retail pack", "market rate ... no supplier quote on file", "provisional set when 11762-17 surfaced the line". Every one is a number somebody worked out once and typed into the source. Being labelled INDICATIVE on the sheet does not make it a price; it makes it a guess wearing a disclaimer. | **EVERY PRICE IN THE TABLE WITHDRAWN.** Not only the felt pad — the policy is not about one line. The keys, labels and sources STAY, because they are the line's identity: what the item is, how it is recognised in a description, and what was once believed about it. That costs nothing and it is what lets the next rung search for the right thing. Only the money goes. `standard_commodity_price` already skips an entry priced at or below zero, so the line falls through to the rungs that can answer, with NO code change: current SDI Live/UDEF, supplier catalogue, an identified current quote, then a researched indicative figure — dated, labelled, evidence kept. | generic — every job | `config.STANDARD_COMMODITY_PRICE_GBP` | — | `tests/test_a_commodity_price_typed_into_config_is_not_a_price.py`. **Five test files were deleted**: they proved the revoked policy (that these figures reach the sheet) and were good tests of it. They are in the history; the new file names them | implemented — **and it opens the other half.** James, same day: "we should have a price for everything.. why would a price be 0 when we have so many layers including llm". Withdrawing the literal must not leave a zero. The waterfall he set — SDI Live, supplier catalogue/API, current quote, LLM-supported indicative with evidence, else BLOCK THE ESTIMATE AS INCOMPLETE — is NOT yet implemented, and until it is, a withdrawn line reads as an owned gap rather than a researched price |
| D-093 | 18 Sep 2026 | The 7332-01 six-off book of 17 Sep 12:47 and its report — the first real proof of D-088/D-090/D-091/D-092 | 7332-01 | **THE ROWS SPLIT. TWO OF THE THREE THEN TOOK THE WRONG TIME, and the book named both faults in its own rate-basis column.** Rows 109 and 110 are right — 0.65h is 15 min set-up plus 6 units at 4 min, 1.05h is 15 min plus 6 at 8 min, both `stated_shop_time`. Either side of them: (1) **BRUSHING TOOK THE MEDIAN.** Row 105 reads basis `historical`, 0.33h = 15 min set-up + 6 units at 79/hr, £1.69 where Howard's 40 minutes makes about £125 at six off. The claim index was built with a `break` after the FIRST marker a part matched, and 7332-01-101 is welded, brushed AND plated — so it registered the weld pair and stopped; the brushing row's claim covered `welding` and `dress_welds` and did not cover brushing. (2) **THE GENERIC ASSEMBLY ROW TOOK A STATED TIME NOBODY STATED FOR IT.** Row 108 is the whole job's pack (007, 008, 101, felt pad) and came out at 1.05h, identical to the final pack: it found no `assembly` hours, fell through to the department-alias fallback — "any operation on this part billing to the same bench" — and PACM is the bench for the generic pack, the pack out and the pack back alike. One stated figure, charged twice, on a row nobody had stated anything about. | (1) The index no longer stops at the first claim. There was never a reason to: a part has as many stated times as the shop has given it. (2) The alias fallback is barred in BOTH directions for a stated operation — it can neither be the target that borrows nor the source that lends. It keeps doing the job it was built for, which is one operation under two names (`assembly` on the route, `handling` in the costing, one PACM row). A stated time belongs to its own operation and to no other. | generic — every part carrying more than one stated claim, and every department where stated and generic work share a bench | `wb_populate` claim index and the stated-hours lookup | — | `tests/test_a_stated_shop_time_gets_its_own_row.py`, five more tests quoting the book's own rows | implemented — **needs the next six-off run to prove.** Expected: row 105 basis `stated_shop_time` at 1.5/hr, row 108 back on the department's own figure, 109 and 110 unchanged |
| D-094 | 18 Sep 2026 | Same book: row 104 Tubebend, 7332-01-002, still charged at £6.29 | 7332-01 | Howard ruled it on 15 Sep — "Tube Bending Op. - Not Required" — and D-085 made the gate's ruling reach the route. It is STILL on the book, and that is not a regression: the report shows the route recorded `tubebend required — read off the drawing's own text` at rank 70, so the gate took its KEEP-AND-FLAG branch, which is deliberate. "Removing charged work because one estimator disagreed with one drawing is how a rule stops describing anything." The drawing says bend; the engine charges it and asks. **What was missing is the route for Howard's ruling to be applied to THIS job** — and it exists: `estimator_decisions.operations_off` in the job's answers file, which no 7332-01 answers file carries. | No change to the gate, which is behaving as designed. But `operations_off` carried the SAME two-names fault D-085 found on the gate: it recorded `removed_operations` and not `operations_ruled_out`, so an estimator's own ruling — the strongest evidence there is — would have been stripped from the costed record and rebuilt by the route under cutover. Now recorded under the name the route reads, with the reason naming the answers file. **ACTION, and it is not ours:** 7332-01's answers file needs `operations_off: {"7332-01-002": ["tubebend"]}` for Howard's ruling to bite. | generic — any operation an estimator takes off any job | `estimator._apply_estimator_operations_off` | — | `test_an_estimators_own_operations_off_ruling_reaches_the_route` | implemented; the ruling itself is outstanding on the job's answers file |
| D-092 | 18 Sep 2026 | James Gray, review of b4bf5af: "The canonical test uses one target. The saved job has plater_pack and plater_final_pack each on both 008 and 101. Therefore the grouped rows will carry quantity 2... The route must first state whether the 4- and 8-minute actions are once per finished stand or once per plated component." | 7332-01 | Splitting the rows was necessary and not sufficient. **7332-01 has TWO plated parts**, 008 and 101, so the estimator mints each pack stage on both and the canonical grouper ADDS their quantities: the row books two occasions of a figure that was given as one, and the throughput reads 30/hr where the stated 4 minutes makes 15. The one-target test could not have seen it. Underneath sits a divergence between the two graders that nobody had noticed: the legacy loop forces qty 1 for the pack DEPARTMENTS (`_PACK_OPS`), the canonical grouper takes the accumulated group quantity — same job, same rule, two answers, and the canonical one is the road this job travels. The legacy answer was also right by luck rather than by reason: it is keyed on the department, so it covers the two pack stages and is silent about brushing. | The basis is DECLARED, per operation, in `config.STATED_TIME_OPERATIONS` — now a mapping of operation to `PER_FINISHED_UNIT` or `PER_COMPONENT` rather than a bare tuple. All three are per finished unit, and the figures say so themselves: Howard was asked outright whether the 40 minutes was per stand or per consignment and answered "40 Minutes was given by production for one unit"; the pack times came the same way, against the engine's own per-unit line. A stand is packed for the platers once, whatever it has inside it. Both graders record the basis on the group, the emit loop reads it and books ONE occasion however many components participate, and the run NAMES the participating parts — because a quantity chosen silently cannot be overruled, and if the shop says the pack really is per component, one word in config changes it. | generic — both labour paths, every job with a stated per-unit figure on more than one component | `config.STATED_TIME_OPERATIONS` (now op -> basis), `PER_FINISHED_UNIT`; `wb_populate.stated_time_is_once_per_finished_unit`, both groupers, the emit loop's quantity choice | — | `tests/test_the_canonical_route_keeps_stated_rows_apart.py` — 16 tests, including the two-plated-part shape with the CONTROL that the group really does accumulate qty 2 and is overridden deliberately, so the test cannot pass by the group having accumulated 1 | implemented — **the six-off rerun is the proof and has not run.** No cell values are predicted here |
| D-091 | 18 Sep 2026 | James Gray, review of 5ceb04b: "it only replaces grouping in the legacy labour loop. The 7332 record is on canonical-route cutover... canonical_labour_groups() does not call labour_group_key() or check STATED_TIME_OPERATIONS. So the claimed 'now applies to all stated rows' is not true for the 7332 rerun" | 7332-01 | **THE FIX WAS REAL, TESTED, AND ON THE WRONG ROAD.** The workbook has TWO labour graders. D-090 put the stated-time identity rule into the legacy loop; 7332-01 does not use it — its summary carries a canonical route shadow with 31 decisions and `CANONICAL_ROUTE_WORKBOOK_CUTOVER` defaults True, so the live path is `canonical_labour_groups`, which asked neither `labour_group_key` nor `STATED_TIME_OPERATIONS`. Its generic key is (department, material, thickness, sequence), which puts the pack OUT and the pack BACK on one row and pools brushing with any manual-metal work at the same gauge — and the fallback beneath it takes a decision with NO sequence and DELIBERATELY joins it to an existing group of the same department, which for a stated rule is the merge made on purpose. I then predicted I105/I109/I110 off the path the job does not take. The prediction was withdrawn; nothing had been proven for the rerun. | The two graders build different key SHAPES and legitimately cannot share a key function. What must not differ is WHICH operations refuse to share a row, so that question now lives in ONE predicate, `is_stated_time_operation`, and both graders ask it — the legacy loop through `labour_group_key`, the canonical grouper as the first branch of its own chain, above every department key, keyed `("canonical-stated", wb_op, operation)`. One stated rule is one row for the job on either path; the row still carries the department's title and bills at the department's rate. | generic — both labour paths, every job | `wb_populate.is_stated_time_operation`, `labour_group_key`, `canonical_labour_groups` | — | `tests/test_the_canonical_route_keeps_stated_rows_apart.py` — 9 tests driving `canonical_labour_groups` itself with real decisions: three stated rules give three rows, a sequence-less stated decision does not join a department group, and the control that unstated work still shares one | implemented — **the six-off rerun is still the proof and has not run.** What these tests establish is the GROUPING under cutover; the rates that grouping then permits (1.5, 15, 7.5/hr) are asserted from config arithmetic, not from a populated workbook, because the template lives on a UNC path no test box here can reach |
| D-090 | 18 Sep 2026 | James Gray, review of 57db153: "The grouping logic then keys one-row-per-job work solely by that displayed title... the two pack stages can still collapse into one row before the stated-time logic runs"; "#REF! is suppressed, not mapped correctly... It risks converting an explicitly unresolved plating cost into a blank/zero contribution" | 7332-01 | **TWO DEFECTS IN THE FIX ITSELF, both caught before a rerun burned them onto a book.** (1) D-088 gave each stated rule its own OPERATION and stopped there. The workbook groups labour by the DISPLAYED DEPARTMENT TITLE, so the pack out and the pack back — both "Assemble/pack (Metal)" — still merged into one row BEFORE the stated-time lookup ran, and brushing still pooled with deburr and bench work at the same material and gauge. The stated figure was not overridden by a median; it was never asked for. The previous round's tests could not have caught this: they proved names, rates and aliases, not rows. (2) The `#REF!` was SUPPRESSED, not mapped. The final hygiene scrub exists to catch what nobody can enumerate; leaning on it for a line whose intent IS known hid a broken reference instead of deciding what the line should say — and would have turned an explicitly unresolved plating cost into a blank contributing zero with nothing to explain itself. | (1) The grouping key keeps the ENGINE OPERATION for anything in the new `config.STATED_TIME_OPERATIONS`, while the row still shows the department's title and bills at the department's rate. Strictly NARROWER than the grouping it replaces: a stated operation can only be split out of a group it would have shared, never merged into one it would not, so no other row changes. The branch is now a named function, `wb_populate.labour_group_key`, because the first cut of its test rebuilt the rule and asserted against the rebuild — a mirror that passes whether or not the engine agrees with it, which is two copies of one fact and the same fault in another guise. (2) The decision moved to the BOM block, where the intent is known. A row the engine has claimed gets exactly one of three things in its price cell: a number, a formula THIS RUN wrote, or nothing — and a line with nothing says **AWAITING A CURRENT PRICE** in its description and is colour-marked as an input, because a blank with nothing said IS the silent zero. Ours is told from the template's by SHAPE (`_ours_by_shape`: our break formula wraps its LOOKUP in an IF asking whether the break row was filled; the template's is a bare LOOKUP), so a bare template LOOKUP is caught **whether or not it says #REF! today** — one that happens to evaluate is the same fault with a luckier reference. The address is logged either way: a broken template formula is not our bug and wants repairing at source. The final scrub stays as the last line of defence for every other cell on every sheet, including template rows the engine never claimed. | generic — every job's labour block and every job's BOM price column | `config.STATED_TIME_OPERATIONS`; `wb_populate.labour_group_key`, `_ours_by_shape`, the BOM price sweep | — | `tests/test_a_stated_row_and_a_price_cell_survive_the_template.py` — 16 tests driving the emitter's own key function and a real openpyxl sheet, with controls that unstated work still shares a row, that a formula this run wrote is left alone, and that a template row the engine never claimed is not touched | implemented — the six-off rerun is the proof, and it has not run |
| D-089 | 18 Sep 2026 | James Gray, 7332-01 six-off cells: "Estimate!H67 is 0.9 and M67 costs from it. Write the recorded 1.0 mm production substitution into the sheet-material output before the workbook is populated." | 7332-01 | Howard ruled it on 15 Sep — "0.9mm Steel Production use 1mm in Lieu" — it shipped as D-068, and `apply_production_substitutions` recorded it on the part and pushed 1.0 through `source_precedence` at rank 95, above every reading. **The sheet still showed 0.9, with nothing on the row to say why not.** The same wrong-record shape as the tube-bend ruling, one field over: the substitution is recorded on the RAW part, the Sheet Steel block writes its gauge from the COSTED part estimate, and `production_substitution` was never copied across the costing boundary. Its only route to the sheet was whichever normalised thickness happened to survive onto the costed record, with the material estimate's own thickness sitting behind it as a fallback. A fact that is right, recorded and unreachable is a fact that did nothing. | Two halves, because one without the other changes nothing. The recorded ruling — and the drawn gauge beside it — travel with the costed record. The Sheet Steel writer then asks THAT rather than re-deriving a gauge and hoping: a confirmed substitution replaces whatever reached the costed record, and the run names the part, both gauges, the rule id and who ruled it, because a gauge that silently differs from the drawing is worse than the bug it fixes. Only CONFIRMED rules arrive — an unconfirmed one returns early and costs as drawn — so a cell that changes here is always one a person has ruled on, and an estimator-confirmed gauge still outranks the rule and stands the substitution down. | generic — any substitution rule, any job, any gauge | `estimator.estimate_part` output record; `wb_populate` Sheet Steel writer | — | `tests/test_what_production_buys_is_what_the_sheet_costs.py` — drives `apply_production_substitutions`, with controls for a gauge with no rule and for an estimator-confirmed gauge, and ties H67 to the block's own column map | implemented — verify on the six-off rerun |
| D-088 | 18 Sep 2026 | James Gray, 7332-01 six-off cells: "I105 is 79/hr... in this template that means a run rate of 1.5/hr"; "I109 is 15/hr = 4 min/unit; I110 is 30/hr = 2 min/unit. Map the stated pack-to-plater/final-pack times rather than allowing generic or size-banded rates to win." | 7332-01 | **The three rates name the fault exactly.** 79/hr is the corpus median for "Manual labour (Metal)", 23 lines of history. 30/hr is 2 minutes a unit against a stated 8. 15/hr is 4 minutes and is CORRECT. So two stated times were replaced and one survived — and the one that survived, `plater_pack`, is the only one with an OPERATION OF ITS OWN. The workbook emits one row per department, and both failing rules were writing under a department's name: brushing wrote `manual_labour_metal`, shared with deburr, bench work and insert labour; the final pack wrote `handling`, which every other part on the job also writes. Five branches of the emit loop can each discard a computed time and four have been caught doing it, one run apiece. **This is not a fifth branch.** It is the layer above: a stated time that shares a key with a department median has already lost before the emit loop sees it. | Each stated rule gets its own operation, its own workbook row, its own department alias and its own rate line — which is what `plater_pack` already had, and precisely why it worked. `brush_before_plate` (MANM, £31.18, the same bench and rate as manual labour metal) and `plater_final_pack` (PACM, £28.56) join it. A rate line is not optional: `estimate_part` SKIPS any operation whose rate lookup misses, writing no hours at all, which is how the tube-bender once worked for free. A plated part's generic `handling` allowance comes off when it takes the two stated packs, because Howard's two occasions ARE the handling on a plated part and leaving it would charge a third pack nobody does. The stated-time markers keep vouching for the OLD names too, so re-opening a book produced before the split does not read its pack time as a department median. Expected on the rerun: 1.5/hr, 15/hr, 7.5/hr. | generic — every plated part and every brushed weldment, on any job | `config.HOURLY_RATES_GBP`, `BRUSH_BEFORE_PLATE`, `estimator` plating block, `wb_populate.OP_NAME_MAP` + `_STATED_SHOP_TIME_MARKERS`, `department_codes` | — | `tests/test_a_stated_shop_time_gets_its_own_row.py`; nine existing tests updated to the new operation names, all driving the engine | implemented — verify on the six-off rerun. NOT asserted: what produced the 2 minutes on I110. The department's generic allowance is 0.8 min, not 2, so the obvious answer is the wrong one; it is banded or derived, and naming it without the run's own record would be a guess wearing a fact's clothes |
| D-087 | 17 Sep 2026 | James Gray, 7332-01 six-off rerun list: "Remove the visible replacement characters from workbook/report text." | 7332-01 | There is NO U+FFFD anywhere in this source tree — a byte-level scan of the whole repository finds it only inside an old PDF. The substitution happens at RUN TIME, downstream of us, and there is more than one way in: a drawing note decoded from cp1252 as UTF-8, a supplier description carried through a console that is not UTF-8, a string read back out of the estimators' own .xls. `test_a_workbook_uses_glyphs_excel_has` already held FIVE NAMED MODULES to the rule by reading their source, which catches a developer pasting an emoji into a heading and cannot catch any of the above — a grep for the character will never find it, because the mangling is at render time. | Asked once, at the boundary, on the VALUE rather than on the source. `workbook_text_hygiene.scrub_workbook` walks every string cell of the populated book immediately before `wb.save`, which is the only point every workbook passes through whatever wrote the cell. TWO CLASSES REMOVED AND ONLY TWO: U+FFFD, which is already damage and whose original character cannot be recovered, so printing the box tells the estimator nothing except that we mishandled it; and astral pictographs above U+FFFF, which are valid UTF-8 that Calibri has no glyph for — boxes on the machines that matter, `?` through a non-UTF-8 console. BMP symbols are deliberately untouched: £, —, ×, ·, Ø, ⚠ and ° all draw in the workbook's own fonts, and stripping them would damage real content to fix a problem they do not have. Nothing is transliterated into words — a dropped pictograph was decoration beside text that already said the thing. The run logs what it repaired and on which sheet. | generic — every job, every sheet, every writer | `src/workbook_hygiene.py` (`scrub_workbook` before `wb.save`; `scrub_report_text` before the report is written) | — | `tests/test_no_deliverable_carries_a_character_or_a_ref_error.py` — 19 tests, including a real openpyxl workbook scrubbed and read back, and guards that each scrub runs BEFORE its write rather than merely existing | **extended 18 Sep** — (a) the REPORT is covered too, at its own write boundary, which is the other half of "workbook/report text"; a browser draws U+FFFD as a black diamond, and the report is built from the same drawing notes and supplier descriptions as the book. scrub_report_text rather than repair, because the whole document is one string and collapsing runs of spaces would reflow the markup. (b) A #REF! formula is blanked and its cell ADDRESS named on the log every run: it comes from the blank template, not from us, and a run that quietly cleaned it would hide a fault that wants fixing at source. (c) James: "the new module claims its source uses codepoint escapes only, but it contains literal replacement/variation characters." Correct, and the module's own test could not see it — U+FFFD and the variation selectors are in the BMP, and the guard only looked above U+FFFF. The characters are now built with chr(0xNNNN) and the test checks the whole byte range, so the documentation is verified rather than asserted |
| D-086 | 17 Sep 2026 | James Gray, 7332-01 six-off rerun list: "Correct report wording so an estimate with awaiting-price lines never says 'priced in full.'" | 7332-01 | The Explanation's provisional paragraph opened "**This job is priced in full**" and closed "Every line is costed and the unit cost is a real figure". It was written about a DIFFERENT gap — geometry read off a view rather than measured — and asserted the money was complete so the reader would worry about the right thing. Then D-078 made a deliberately unpriced line NORMAL: plating, freight, packaging and delivery stay visibly awaiting a current price rather than carrying a figure off an estimator's sheet. On 7332-01 that is four lines, and the report still told Howard the job was priced in full. **The engine was contradicting its own pricing policy inside its own report**, which is worse than either answer alone. | The claim is now conditional on the thing it claims. Lines awaiting a rate are counted off `bom` — THE SAME LIST THE PRICE TABLE UNDER IT WALKS, using the same `_price_source` that writes each line's verdict, so the sentence and the rows cannot disagree; the first cut counted `material_rows`, which is the per-block reconciliation summary whose rows carry no code, and returned zero silently. With lines outstanding it reads "priced except for N line(s) still awaiting a current price", and the closing sentence says the unit cost is real but **NOT the finished price** — the awaiting lines carry no money at all, so the total can only go up. With nothing outstanding the original wording stands, because when the money is complete saying so is the useful answer. | all jobs — fires on the shape of the estimate, not on this one | `estimate_explained.build`, the data-sufficiency paragraph | — | Four tests in `tests/test_the_explanation_adds_up_to_the_sheet.py`, including the control that a fully priced job still says so | implemented |
| D-085 | 17 Sep 2026 | James Gray, 7332-01 six-off rerun list: "Suppress the erroneous Tubebend operation on leg 002." Howard Thurley had already ruled it: "Line 103 - Tube Bending Op. - Not Required" | 7332-01 | D-069 put the gate in and the leg STILL arrived carrying Tubebend — TBEN at its own hourly rate with a 45-minute set-up, on a leg that is sawn. The gate was not wrong; it was writing to the wrong name. Suppression cleaned the COSTED record (`ops`, `textual_operations`, `inferred_operations`, both timing dicts) and recorded the removal in `removed_operations`. The sheet does not read that record: `wb_populate.route_operations_by_part` rebuilds the operation list from `summary["parts"]` and `route_compiler` builds its claims from the same place, and BOTH honour a ruling under one name only — `operations_ruled_out`. **Two names for one fact**, which is the exact fault this register exists to catch, and here it had produced a real charge on a real book. | The gate records its ruling under the name the readers actually read, with the reason attached so the cancellation can be explained rather than merely obeyed. `removed_operations` stays as the human-facing list it always was; `operations_ruled_out` is the machine-facing one, and the route compiler cancels the claim at source rather than the sheet filtering it afterwards. Fires on the shape of the evidence — a square or rectangular section whose only bend evidence is an angle callout, or a tube nothing states a bend for — so it holds for any leg on any job, not for 7332-01-002. | generic (any section stock) | `estimator.estimate_part`, the tube-bend gate | — | `tests/test_a_ruling_the_engine_made_reaches_the_sheet.py` — drives `route_operations_by_part` on a part built in the test, with the control that the route KEEPS the operation when no ruling is recorded | implemented |
| D-084 | 17 Sep 2026 | James Gray, Sage X3 migration plan: "all data by 30 Oct 26", ERP go-live Mon 4 Jan 2027 | all | The engine's material and supplier prices are asked of SDI Live (Access Supply Chain). Access is being retired: all data lands in Sage X3 by Fri 30 Oct 2026 and the ERP goes live on Mon 4 Jan 2027. A price source with a retirement date is a change to where every priced line comes from, so it belongs here rather than only in the migration plan — the estimators read this register to know what answered a price. | Recorded as a dated change of price source, not yet made. From ERP go-live the material and supplier price lookups read X3 in place of SDI Live, with dual-running validation across the changeover: the same job priced against both, and any line that moves explained before the old source is dropped. Nothing about the pricing standard relaxes — D-078 holds across the move, so a figure carried over from Access must still be a current lookup, a supplier catalogue row or an identified quote in X3, and a line with no source in X3 says it is awaiting a price and names its owner exactly as it does today. The engine is NOT repointed early: the data has to land and be signed off (FD on the trial balance, stock value, AR, AP and the order totals) before any price is read from it. Two seams already named on the migration page stay open until then — the product-category confirmation, which decides how catalogue rows are found, and the 20,724 items carrying a blank description, which is what a description-based lookup fails on. | generic — every priced line on every job | not yet implemented; the seam is `price_provenance` / the UDEF lookup path, and the plan is `#aisvc-x3` on the portal and section 06 of the programme status report | — | Dual-running validation across the changeover is the proof and is not yet possible; recorded now so the date is visible to the estimators rather than only to the migration | proposed — dated, not implemented |
| D-083 | 17 Sep 2026 | James Gray: "we have the following jobs that we have completed together with their estimating manual runs... we should do a stylish parity in a management KPI style of reporting"; "we keep doing this every week" | 7332-01, 12349-02, 10975-02, 11908-21 | Parity had never been computed for the parallel run: both existing harnesses need the engine's own summary.json, which lives wherever the engine ran, so the reports could only carry the estimators' verdicts in words. What actually travels between machines is the WORKBOOK. | Parity is computed from the two workbooks alone, using `parity_check.read_house_workbook` on BOTH sides — the manual sheet and the engine's are the same house template, so the one block reader serves both and each side is reconciled to its own stated totals first. THREE THINGS IT REFUSES TO GET WRONG. (1) **A variance across two quantities is arithmetic about nothing**: 11908-21 ran at 1 and Tony's sheet is at 50, 10975-02 ran at 1 and Howard's Rev B sheet is at 10 — set-up amortises over a different number on each side. Both are compared at the manual's quantity, read from the engine's own Quantity Breaks tab; where no such column exists the pair is reported NOT COMPARABLE with the reason and excluded from the averages. (2) **The same article, not just the same job number** — 10975-02 has an L-stand Rev A at 1 off and the A4 table-top holder Rev B at 10; pairing the table-top run with the L-stand sheet gives a material variance near 800%. (3) **No manual figure is written into the output**, per D-078: parity is published as the engine's own number and a variance with a direction, which measures us without retaining them and cannot be used as a price. RESULT, four pairs: material +0.4% to -66.4%, subtotal -24.8 / -22.9 / +12.9 / -10.8%, mean absolute 17.9%, engine under on three of four with a named cause on each. | all jobs where a manual sheet exists | `tools/parity_workbooks.py`, on top of `parity_check.read_house_workbook` | — | Four real pairs computed and published in the estimating status report; all eight books reconciled to their own totals | implemented |
| D-082 | 16 Sep 2026 | Tim Wilkes, 11350-02 review: "Where does engine get rate for pemming (Manual labour) from." | 11350-02 | The MANM row booked 0.70 batch hours at £31.18/hr and the sheet said nothing about where either figure came from, so a rate the estimator himself supplied read as one the engine invented. | Answered, and the derivation recorded so it can be checked rather than believed. TWO figures, two sources. The RATE is MANM's own department rate from the labour-rates table, not a per-job figure. The TIME is 15 seconds per pressed fastener, which is Tim's own 12120 sheet read back: 4 clinch nuts a unit at 60 units/hr and 2 PEM studs a unit at 120 units/hr are both 240 inserts an hour, so both state 15 s an insert. On 11350-02: 4 studs x 27 off = 108 inserts x 15 s = 0.45 h, + the standard 15-minute MANM set-up = 0.70 h, x £31.18 = £21.83 a batch = £0.81 a unit — the row exactly. A throughput rate, so loggable under the pricing policy, but a SCOPED PILOT from one sheet until a second job or the department confirms it. | 11350-02 + any job with pressed fasteners | `config.MANM_INSERT_SECONDS_EACH` (15.0), `BOOK_MANM_INSERT_LABOUR`, `HOURLY_RATES_GBP["manual_labour_metal"]` | — | The arithmetic above reproduces the booked row from the two constants; put to Tim on 16 Sep to confirm the 15 s still holds | implemented, awaiting Tim's confirmation |
| D-081 | 16 Sep 2026 | Tim Wilkes, 11350-02 review: "Powder - Where is Cost coming from for RAL 9003 (Could we put Powder ref on drawing)" | 11350-02 | The powder line showed a quantity of 0.03364 and "source not named in the workbook", which invites the reading that a colour was priced. No colour is priced. | Answered: the engine prices powder BY WEIGHT and never by RAL. Coated area x `POWDER_KG_PER_M2` x the per-kg rate — on 11350-02, 0.1682 m2 x 0.20 = 0.03364 kg x £4.00 = £0.14. £4.00/kg is the configured standard-powder rate recorded as confirmed by estimating (Tim, POWDER5 on job 1282); metallic/textured/wrinkle specials carry their own higher rate, and a standard RAL such as 9003 takes the standard one. TWO THINGS OPEN, both Tim's to rule: whether £4.00 still holds, and the kg/m2 — we use 0.20, derived at ~50% transfer efficiency, where the workbook template's own Powder Qty Calculator uses 0.1667 (6 m2/kg, 100% transfer). TIM'S PROPOSAL ACCEPTED IN PRINCIPLE: a powder reference on the drawing lets the line price from a catalogue SKU instead of a per-kg average, which is the same move already made for the powder book. | all powder-coated jobs | `config.POWDER_COSTING_POLICY`, `POWDER_COST_PER_KG`, `POWDER_KG_PER_M2`, `POWDER_MIN_KG_PER_PIECE` | — | The arithmetic above reproduces the booked line from the two constants | answered; two figures awaiting Tim, powder-ref-on-drawing raised with Design |
| D-080 | 16 Sep 2026 | Tim Wilkes, 11350-02 review: "Ticket Strip- Can it not take previous cost off system or isn't drawing giving correct information" | 11350-02 | DBR60 is a real code and came back unpriced with "NOT PRICED — needs a rate", which does not say whether the lookup happened, so the estimator cannot tell whose gap it is. | An unpriced line carrying a REAL code now says that the code was put to the purchasing catalogue and the price history and that neither holds a price for it, and that this is a gap on our side rather than a fault in the drawing. The underlying cause is the standing open item, not a new one: no supplier price list has been loaded into `AIEstimating.BoughtInCatalogue`, and the source filter deliberately refuses web-indicative, RAG-workbook and our-own-estimate rows, so a line with a perfect code still finds an empty shelf. Elite, Eagle and Thermaset remain the action. | all jobs — fires on the shape of the line, not on this job | `estimate_explained._price_source`, real-code branch | — | `tests/test_a_code_we_invented_is_not_a_code_tim_can_look_up.py` | implemented |
| D-079 | 16 Sep 2026 | Tim Wilkes, 11350-02 review: "Fixings- M4 Wing Nut/M4 x 8mm Pems - can it not take of system or internet for cost or is spec missing on drawing" | 11350-02 | Neither. The pack gives no part number for these, so the reconciler mints a placeholder from the description — BI-NUT, BI-PEMSTUD — and that placeholder then sits in the code column of every document looking exactly like a part number. The line read "NOT PRICED — needs a rate", so the estimator was asked to price something whose code he could not find, with no way to learn that the engine had written it. | A minted code is named as ours on the line: the code was synthesised so the line could be carried and counted, nothing can be looked up against it, the description was matched against the catalogue instead and no priced row carried its words and sizes, and the drawing's spec is not the problem. The answers differ from D-080's on purpose — this one is closed by a code on the pack or a hand price, that one by loading the catalogue. New predicate `is_engine_minted_code`, the counterpart of `is_category_not_a_code`: the drawing printing a CLASS where a code belongs, against US printing a placeholder where the drawing printed nothing. Deliberately narrow — BI- followed by letters only, so a real code carrying digits is never called an invention. | all jobs — fires on the shape of the code | `part_identity.is_engine_minted_code` (beside `synthesise_bought_in_code`, which mints them — a recogniser anywhere else becomes a second private copy of that vocabulary, which `test_one_hardware_vocabulary_serves_every_reader` refuses; it is a SHAPE test, not a second list, so a new minted code is recognised the moment it is added), `estimate_explained._price_source` | — | `tests/test_a_code_we_invented_is_not_a_code_tim_can_look_up.py` (17 executed cases, synthetic rows with no job number) | implemented |
| D-078 | 16 Sep 2026 | James Gray: "A number copied from an estimator's sheet is not a price source, even as a 'reference'. It must not be retained in the current register, documentation, reports, prompts, tests, or audit payloads." "We can't take a number off a sheet." | all | D-077 demoted the freight figure to an audit record; the tape roll price and the edging rate still sat in `ESTIMATOR_STATED_PRICES` and the register; the plater figure sat in the audit record. Every one was a number off a manual estimate — attributed, dated, and still a copy that cannot be re-derived from anything current | **Removed entirely, everywhere a run can reach**: the register ships empty (prices AND audit record), `ESTIMATOR_STATED_PRICES` is empty by rule, every amount is redacted from source comments, docs, the status report and tests. What carries forward is the METHOD, number-free: TAPE113C's roll length and cut-length arithmetic; EDGE23X1ABS's specification and measured-length method (`FACED_BOARD_EDGING_SPEC`, Ostermann, credited to Tony); Harrods 01 as decorative plating needing a fresh quote; a plated route's freight line priced only from a current transport source. Where no current source answers, each line is WITHHELD / AWAITING PRICE naming the owner. Mechanism tests run on SYNTHETIC figures the test injects; the shipped state is asserted empty | generic — the standard for every price entry | `data/price_register.json`, `config`, `stated_prices`, tests, docs | `this commit` | `test_a_price_is_data_not_a_literal` (register empty; no key resolves; audit invisible), `test_a_price_in_source_control_cannot_go_stale_visibly` and `test_a_length_off_a_roll…` (mechanism on synthetic figures; shipped state withheld), `test_the_joinery_floor…` (edging rate held nowhere, spec kept) | implemented — supersedes D-073 and D-077. OPEN under the same rule (James to rule): `BOARD_SHEET_PRICE_GBP` (sheet prices taken from Tony's priced jobs), the £/kg zinc card and `SECTION_STOCK_PRICE_GBP_PER_KG` are also figures with no live source — migrating them leaves board and section lines awaiting price until SDI Live answers |
| D-077 | 16 Sep 2026 | James Gray: "never convert a manual-estimate figure into 'confirmed' merely by moving it from config to the register"; "unless there is a re-useable / understandable calculation we can't use it" | 7332-01 | D-073 fixed the cross-job inheritance and then repeated the fault it fixed, one layer down: the £— sat in `prices` as chargeable, `status: confirmed`, sourced only as "SDI transport via Howard" — which is Howard's estimate review ("**For Ref.** [figures redacted] Pallet Network, per-order and per-unit"), relayed, with no quote document and no transport-system lookup held | The standard, as set: a current SDI transport lookup or a current, identified transport quote → price it; otherwise the PLATERFREIGHT line stays AWAITING a current quote. The £— moves to `historical_audit_record` — `source_type: manual_estimate_reference`, source text quoting his "For Ref." line verbatim, the £—-a-unit-at-6-off arithmetic kept so the number stays understandable — where `lookup()` cannot see it. **It prices nobody, 7332-01 included**: the rerun's freight line reads awaiting a current quote, owner SDI transport, exactly as instructed. A current quote enters `prices` as a NEW entry when one exists | generic (the standard applies to every register entry) | `data/price_register.json` + the tests re-pointed at the standard | `this commit` | `test_a_named_plate_spec_is_a_quoted_price` — 7332-01 itself cannot charge it; the resolver cannot see the audit record; the "For Ref." source and the arithmetic are asserted present | implemented — supersedes D-073's chargeable entry. OPEN QUESTION for James: TAPE113C (£—) and EDGE23X1ABS (£—/m) are also estimator-stated; they answer only after SDI Live and are labelled, but the same standard may want them demoted too — his ruling |
| D-076a | 16 Sep 2026 | James Gray: "drawing search intelligence menu items are all messed up now" | site | Verified by rendering the pages: the Operate rail stacked "SDI Drawing Search Intelligence Guide" (wrapped to two lines) directly above "SDI Drawing Search Intelligence", with a third same-named entry already in AI Services — four near-identical lines reading as duplication. (The git diffstat's truncated test filename was column-width truncation, not corruption; the file is intact and passing) | The launcher takes the site's own external-link convention — "SDI Drawing Search Intelligence ↗", matching the footer's "wearesdi.com ↗" — on all three sidebars, so guide, launcher and service write-up read as three different things at a glance. Position and target unchanged | site | the three HTML pages + the site test pins the arrowed label | `this commit` | `test_a_page_only_uses_components_this_site_has` + rendered screenshots before/after | implemented |
| D-076 | 16 Sep 2026 | James Gray: "integrate this link http://LC-328802:5000/ … as a new menu item below #fixture-guide called SDI Drawing Search Intelligence" | site | The Drawing Search GUIDE is on the portal; the APPLICATION itself (Muhammad's search, in UAT on the host at :5000) was reachable from nowhere on the site | A new Operate entry, **SDI Drawing Search Intelligence**, directly below its guide, on all three navigations (the portal sidebar and both copied sidebars — one codebase serves 8071, 8072 and the server), plus a header link on the /estimating page itself. A plain external href opening its own tab — the portal nav's click handler only acts on `data-view` entries, so nothing is hijacked | site (all deployments of app.py) | `sdi-intelligence-portal.html`, `sdi-estimating-intelligence.html`, `sdi-estimating-guide.html` | `this commit` | `test_a_page_only_uses_components_this_site_has` — the app entry exists below its guide on all three, opens its own tab, and is not a portal view | implemented |
| D-075 | 16 Sep 2026 | James Gray, rerun-blocker review: "the current config says 4 and 8 minutes are per consignment, while the code charges them per unit — and may apply PACM setup twice" | 7332-01 | Two records of one fact disagreed: `SHOP_STATED` provenance labelled the plater pack times minutes/CONSIGNMENT while the engine books them per UNIT. And the D-072 split means each pack row takes the template's own PACM set-up — two set-ups the sheet was not saying out loud | Reconciled per UNIT on the evidence: the 14:28 book showed the combined line as 12 minutes A UNIT (£6.90 at 6 off); Howard reviewed that line and disputed only the presentation ("Two separate Operations this job"), never the per-unit figure, and his 4/8 was given against the engine's per-unit 2-minute allowance on the same line. Provenance now reads minutes/unit with that evidence. The double set-up is stated ON the split's own flag — two set-ups for two occasions (packing out, packing back), with the way to halve it ("if the bench regards these as one set-up, say so and the second comes off") — a question genuinely the bench's, raised where a person can rule | scoped (7332 pilot times); the reconciliation rule generic | `config` (provenance) + `estimator` (flag) | `this commit` | `test_a_named_plate_spec_is_a_quoted_price` — the line carries the per-unit wording and the two-set-up statement | implemented — Howard rules on one-or-two set-ups from the rerun book |
| D-074 | 16 Sep 2026 | James Gray, rerun-blocker review: "Laser setup sharing must use a proven nesting result, not merely same material and gauge" | 7332-01 | D-066's allocation shared one set-up across rows of the same laser, material and gauge — which says the parts COULD share a program, not that they DO. Howard's constraint is conditional: "if multiple components used but don't exceed a single sheet" | **The nest is proven, not presumed.** `canonical_labour_groups` proves each nest from the engine's OWN nesting results (the template's K38 formula per part, in `stock_estimate`): every member on the same stock sheet, and Σ((qty/unit × order qty) ÷ parts-per-sheet) ≤ 1.0. The verdict and its arithmetic are stamped on every member (`laser_nest_proven`, `laser_nest_evidence`) and serialized on the route record. `allocate_laser_nest_setup` allocates ONLY a proven nest; an unproven one — a member nothing nested, mixed stock sheets, an order past one sheet — keeps a FULL set-up on every row with the reason flagged: overstated and said so, never reduced on a presumption | generic (laser) | `wb_populate` (`_prove_laser_nests` + allocation gate) | `this commit` | `test_five_components_never_create_five_laser_setups` — executed: the proven nest records "1.00 of one sheet"; 12-off (2.00 sheets) is NOT allocated and the template cells are untouched; a part nothing nested and mixed stock sheets each leave the nest unproven | implemented — supersedes D-066's material+gauge presumption |
| D-073 | 16 Sep 2026 | James Gray's non-negotiable pricing policy; Howard's "[figures redacted] Pallet Network, per-order and per-unit" was 7332-01's own quote | 7332-01 | The plater freight lived in config (`SHOP_STATED` → `PLATING_LOGISTICS`) and was charged on EVERY plated job — a job-specific transport quote become a shop constant: the £— fault in a smaller coat | **The route inherits; the money does not.** The £—/order moves to `data/price_register.json` as `PLATER_FREIGHT`, scope `job_only: 7332-01`, source SDI transport via Howard. `estimator.plater_freight_for_job(job_codes)` asks the register with the job's own codes; both charging sites (the plating-line note and the standalone PLATERFREIGHT line) price only when the register answers for THIS job. Any other plated job still gets the freight LINE — raised UNPRICED, `plater_freight_awaiting_quote`, naming what is missing and who owns it (SDI transport). No env override remains: changing the figure is a register diff, reviewable | generic rule (plated route needs a current transport quote); the figure job-scoped to 7332-01 | `data/price_register.json` + `config` (figure removed) + `estimator` | `this commit` | `test_a_named_plate_spec_is_a_quoted_price` + `test_work_the_drawing_never_mentions_is_still_work` + `test_an_answer_needs_somewhere_to_live` — executed: 7332-01 resolves £—; another job gets None and the unpriced branch exists; config carries no freight figure | implemented — the 7332-01 rerun should show freight priced from ITS quote; any other job's book shows the awaiting-quote line |
| D-072 | 16 Sep 2026 | Howard Thurley, 7332-01 reply ("Two separate Operations this job, items need to be packed to send to platers before" the final pack) | 7332-01 | His 4 + 8 minutes were booked as ONE 12-minute `handling` figure on one row — the right money and the wrong record: neither 4 nor 8 could be checked against it, and the route never said the part leaves the building in the middle | The pack to the plater is its own operation: `plater_pack`, same PACM bench and rate-table row title, its own row keyed as its own decision on the canonical route (verified surviving the route compiler as `required`). `handling` keeps the final assembly & pack at 8. The plater row's description says which pack it is ("pack to plater — the part goes out for plating and is packed again") so two Assemble/pack rows never read as a double-charge; the stated-time marker vouches for the new op; `HOURLY_RATES_GBP` carries it at the PACM rate | generic (any outsourced-plate route) | `estimator` (pack split) + `wb_populate` (op map, row description, stated marker) + `department_codes` + `config` (rate) | `this commit` | `test_the_shop_knows_things_the_drawing_does_not_say` (two ops booked 4/8, recorded for the compiler; unplated part packs once; maps executed) + the 7332 money tests updated to the split with the total held at 12 | implemented — verify the two rows on the 7332-01 rerun |
| D-071 | 16 Sep 2026 | Howard Thurley ("M&S dress all seen welds; TTI none"), 15 Sep 2026 | 7332-01 | Whether a weldment's DRES line exists was decided by shop default alone (dress after every structural weld). Howard's reply states it is the CUSTOMER'S standard — a fact about the customer, not about any one job | His words kept as a rule, never a price: `config.CUSTOMER_FINISH_STANDARDS` carries per-customer `dress_visible_welds` with who stated it and when, alias spellings included. `estimate_document` resolves the standard from the SAME customer name the workbook header prints (`job_customer` — the £— register's lesson) and stamps it on every part; the DRES chain then suppresses only the engine's own INFERENCE for a no-dressing customer, saying whose rule that is. A drawing that states dressing is never overruled; a customer not listed keeps the shop default; an M&S weldment is dressed with the standard credited on the line | generic mechanism; two customer entries (M&S, TTI), each his statement verbatim | `config` + `estimator` (`customer_finish_standard`, DRES chain, `estimate_document` stamp) | `this commit` | `test_the_shop_knows_things_the_drawing_does_not_say` — executed: TTI weldment books no DRES naming his rule; M&S books it crediting the standard; unknown customer unchanged; a stated dressing outranks the customer default; the name resolves however the folder spelled it | implemented |
| D-070 | 16 Sep 2026 | Howard Thurley, 7332-01 reply ("Subjective – depends on component, peel may be incorporated into individual operations. Nothing fixed for this.") | 7332-01 | The acrylic route booked "peel the protective film" as a Manual Labour (Acrylic) line on EVERY acrylic part — the engine's standard allowance, read off nothing. Asked directly whether his sheet carries it, the department's answer is that no fixed allowance exists | A default the department itself calls not-fixed is the engine inventing a standing charge. The peel/handling allowance is booked ONLY where the drawing's own text states the work (peel / protective film / masking / demask), flagged as drawing-stated with a confirm; otherwise **the line does not exist and the record says why in his words**, with the way to reverse it (state the operation with a time) — an absence a person can act on, not a gap | generic (acrylic route) | `estimator` (acrylic route, peel gate) | `this commit` | `test_the_shop_knows_things_the_drawing_does_not_say` — executed through `estimate_part`: a plain acrylic part books no MANA and the flag carries his words; a drawing stating the peel is charged for it | implemented — verify on the 7332-01 rerun |
| D-069 | 16 Sep 2026 | Howard Thurley, 7332-01 reply ("Tube Bending Op. – Not Required") | 7332-01 | The earlier gate removed a tube bend only where NOTHING stated one, and treated an angle callout as a measurement. 7332-01-002 is the counter-example: a 15.88 square leg whose 45° callouts describe the MITRE SAW CUT, and the op stayed charged-with-a-question at £32.84/hr + 45 min set-up | The section's own shape speaks: the tube-bender wraps ROUND/OVAL tube around a former to a RADIUS. Measured bends (a DXF bend line, the model's own count) are never second-guessed; a round section with a radius callout is bent without a question; a **square/rect section whose only evidence is angle callouts loses the op**, citing his ruling and naming what would bring it back (a radius or a bend line); a bend word on a round/unknown section stays charged and asks, naming exactly what is missing. `bends` (which folds words and angles into one figure) is explicitly NOT a measurement here | generic (tube route) | `estimator` (tube-bend gate) | `this commit` | `test_the_shop_knows_things_the_drawing_does_not_say` — executed: the mitred square leg comes off citing his words; round+radius bends silently; an angle on round asks for the radius; measurements never questioned | implemented — verify on the 7332-01 rerun |
| D-068 | 16 Sep 2026 | Howard Thurley, 7332-01 reply (15 Sep 2026) | 7332-01 | "0.9mm Steel Production use 1mm in Lieu" — raised as TBC on 9 Sep (the engine flagged and costed as drawn), CONFIRMED in his 15 Sep reply. Costing a gauge the buyer cannot order under-charges every 0.9 mm steel job by the difference | A **production-substitution rule as data**: `config.PRODUCTION_MATERIAL_SUBSTITUTIONS` carries the rule with who stated it, when, on which job, and its status. Confirmed → the substitute gauge is COSTED (applied before the mass, laser speed and gauge column read the thickness), the drawn figure kept on the part and named in the flag with how to override. Estimator-confirmed thickness (rank 100 — the answers file simply states thickness_mm) outranks the rule, which stands down saying so. A rule not yet confirmed only flags and costs as drawn — the TBC behaviour is the mechanism's own unconfirmed state, not deleted | scoped production-material rule: MILD STEEL and ZINTEC only. Stainless deliberately excluded — 0.9 mm stainless is a real buy, and widening a production fact past the person who stated it is the scoped-pilot fault | `config` (the rule as data) + `estimator.apply_production_substitutions` | `this commit` | `test_the_shop_knows_things_the_drawing_does_not_say` — all three states executed: confirmed costs 1.0 keeping drawn 0.9; a person's confirmation stands it down; an unconfirmed rule flags only; stainless untouched; end-to-end through `estimate_part` | implemented — verify on the 7332-01 rerun |
| D-067 | 16 Sep 2026 | Reviewer critique of D-064, then the engine's own behaviour | 11908-21 | Two faults, and the second was found only because the first was fixed properly. (1) D-064's bench line applied **Tony's 2-parts/hour to ANY board assembly** — plywood, timber, plain MDF — simply because it had children. His measurement was one faced-board tray: a scoped pilot becoming a shop constant, inside the change made to fix a different fault. (2) Rewriting the tests to run `estimate_part()` END TO END (the first cut re-created the rule's predicate from source text, a test that cannot fail when the engine does) exposed the deeper one: the canonical material collapse turns "MFMDF" into "MDF" for block routing and **discards the facing** — so on the real path Tony's rate never applied to ANYTHING, and a title block stating MFMDF outright needed the laminate proven again from a finish note or file name before the sheet price could reach it: plain-core money on a faced panel, the 4× under-charge 11908-21 shipped | The rule is generic; the rate is not. Every multi-part board assembly still gets a bench line — that follows from the BOM's own structure — but only **faced/laminated board (MFMDF/MFC), the family Tony measured**, takes his 30 min; everything else takes the house allowance and the flag says his pilot does NOT govern it. And the collapse keeps the facing: `_stated_faced_family` is recorded beside the canonical name, is promotion evidence on its own (a drawing that NAMES the faced family needs no second proof), and is what the bench scope reads. Plain MDF with no evidence still stays plain MDF | rule generic (board assemblies); rate scoped pilot (faced board, 11908-21) | `estimator` (bench scope + `_stated_faced_family` + promotion evidence) | `this commit` | `test_a_board_drawing_that_says_rebate_gets_the_spindle` — **executing `estimate_part()`, not reading its source**: MFMDF at 30 min with the pilot named; plywood/timber/MDF at the house allowance saying so; the facing surviving the collapse; plain MDF unpromoted | implemented — supersedes D-064's scope column |
| D-066 | 16 Sep 2026 | Howard Thurley's own 7332-01 sheet (15 Sep 2026) | 7332-01 | **His sheet carries FIVE separate LASM rows** — 0.2667, 0.1344, 0.0967, 0.1846, 0.1982 h — which is his "labour rates should be separate line each component as reflect the different time per component", proven in his own file. The engine keys laser rows by (operation, material, gauge), so 003 and 004 share one row at a combined 441/hr. He also states the constraint that goes with it: "if multiple components used but don't exceed a single sheet, laser rate would be reduced to not exceed the given set up time" | **NOT YET IMPLEMENTED, and deliberately not half-implemented.** Splitting the key to one row per component is a small change; on its own it would multiply the department's set-up by the number of rows — five 10-minute set-ups where the shop has one — which is precisely the error he warned about, and worse than the combined row it replaces. The two halves must ship together: key per part, then allocate ONE department set-up across the rows that nest on a single sheet. The set-up reaches the row through the TEMPLATE's own lookup, not through anything the engine writes, so the second half means writing the setup column — the workbook's own authority under the operating rules — and that is a change to make carefully rather than at the end of a session | generic (laser; the nesting constraint is per gauge) | `wb_populate` — `canonical_labour_groups` keys a laser decision per component and stamps its `laser_nest_id` (op·material·gauge); `allocate_laser_nest_setup` wraps the template's OWN set-up lookup as (lookup)/N across the nest, so the sum is exactly one allowance BY CONSTRUCTION and moves if the estimators change their table. Touched only after checking, per row, that the row's hours formula (J) really reads its set-up cell (L); where the template is not that shape NOTHING is written and the sheet says the set-up is overstated. The nest, its size and each row's share are serialized on `workbook_labour` rows — the allocation is row data, auditable off the record | — | `test_five_components_never_create_five_laser_setups` — **both halves executed, not read as text**: five components, five rows, ONE allowance shared; a different gauge is a different nest; a lone row keeps the template's own set-up; an unrecognised template is untouched and says so | **implemented — the two halves shipped together. Verify on the 7332-01 rerun** |
| D-065 | 16 Sep 2026 | Howard Thurley's own 7332-01 sheet (15 Sep 2026) | 7332-01 | D-063 separated set-up from run time on Tony's joinery sheet, and the proof was five whole numbers against the engine's own set-up table. It was recorded as a joinery finding | **Confirmed on a second sheet, and a metal one.** Howard's 7332-01 at quantity 6 decomposes the same way against the same table, and reproduces three figures he had stated in plain English weeks earlier: WELD 3.50 h − 30 m = **30.000 min/unit** ("0.5 hr"), DRES 2.50 h − 30 m = **20.000 min/unit** ("20 min"), MANM 4.25 h − 15 m = **40.000 min/unit** ("40 minutes" brushing). Two estimators, two product families, two jobs, one convention — so the SET-UP MODEL is shop-wide. Deliberately does not widen the joinery RATES: what is confirmed is how these sheets are built, not one tray's speeds | shop-wide (the set-up model). The joinery rates remain a scoped pilot | `config.SHOP_STATED` (recorded beside the rates) | `this commit` | `test_the_joinery_floor_is_measured_not_borrowed` (Howard's three stated figures reproduced; the note checked for not overclaiming) | implemented |
| D-064 | 16 Sep 2026 | Tony Ford (11908-21) | 11908-21 | "No bench work time." BENC is **25.5 of his 42 hours** — the single biggest operation on the job — and the engine charged none of it. Not a wrong rate: the operation never existed, for one reason. Every non-welded assembly mints ONE `assembly` event, and `OP_NAME_MAP_JOINERY` sends "assembly" to **Packing Joinery** — so a tray's assembly event became its PACKING row, and the fitting that produced the tray was charged as boxing it. One event doing two jobs and landing on the wrong one | A board assembly is **fitted before it is packed**, so it gets its own `bench_work` line alongside the pack event. The evidence is the BOM's own structure — a parent with children has to be put together before it can be packed — which is the same standard the assembly event itself is minted on, not a new inference. Minutes come from the register's RUN rate (2/hr = 30 min a tray), not the generic 2-minute bench default, so the engine and the workbook agree instead of relying on the throughput guard; the department's set-up is charged once per order and is deliberately NOT booked per part. **The arithmetic closes from both ends**: 30 min × 50 + the 30-minute set-up = 25.5 hours, which is the figure on his Labour tab | scoped pilot — board assemblies (MDF/MFMDF/MFC/ply/timber). NOT metal, NOT acrylic — an acrylic display really is assembled and packed in one PACP pass, which is what Howard's "Apply Tape, Bag, Bulk Pack" describes | `estimator` (joinery route rule, beside the acrylic one) | `this commit` | `test_a_board_drawing_that_says_rebate_gets_the_spindle` (his 25.5 hours reproduced; a board LEAF and every metal/acrylic assembly untouched; the set-up not booked per part) | **reopened → superseded by D-067**: the scope column here was wrong twice over — the rate reached ply/timber it was never measured on, and on the real path the facing collapse meant it reached nothing at all. The rule (a board assembly is fitted before it is packed) stands |
| D-063 | 16 Sep 2026 | Tony Ford (11908-21) / traced in session | 11908-21 | "CNC setup not amortised". D-045 shipped his hours-per-order figures WHOLE (11.32, 10.71, 10.71, 1.96, 18.18), which buried the set-up inside the per-part rate — and the workbook then added its own set-up row on top, so a faced-board job paid for its set-up twice and its quantity breaks had nothing left to amortise. His finding, arriving from the other side. Setup was recorded as UNKNOWN because one job looked like one equation in two unknowns | **The evidence separates them.** Subtracting the set-up this engine ALREADY holds for each department (`OPERATION_SETUP_MIN`, read off the Estimate template's own rate rows) leaves whole-number run rates in **all five** joinery departments: CNCJ 12/hr, EDGE 12/hr, MC J 12/hr, BENC 2/hr, PACJ 20/hr. One integer would be a coincidence; five, against a table written long before his sheet was read, is his sheet and this engine using one convention — which makes the split EVIDENCE rather than a curve fit. The register holds RUN rates; set-up stays in the one table and is charged once per order, as his sheet does. `joinery_setup_min_per_department: None` is superseded — a stale UNKNOWN reads as an open question | scoped pilot — faced/laminated board (MFMDF, MFC) | `config.SHOP_STATED` + `_joinery_derivation` (set-up passed at the call site, re-derived from the table by the test) | `this commit` | `test_the_joinery_floor_is_measured_not_borrowed` (every rate re-derived from `OPERATION_SETUP_MIN`; all five land on integers; every rate faster than the hours-whole figure it replaced) | implemented — verify on the 11908-21 rerun |
| D-062 | 16 Sep 2026 | Tony Ford (11908-21) | 11908-21 | "No machining saw/spindle" — MC J has been on the Estimate template's rate card all along at £28.735/hr with a 30-minute set-up, and the department vocabulary knew its spellings (morticing, tenoning, spindle moulding, planing). What was missing was anything that MINTED the operation, so 4.67 of his 42 hours had nowhere in the engine to land. Not a wrong rate — an absent line | `machining_joinery` is minted **only on the drawing's own evidence**: a rebate, groove, housing, mortice, tenon, moulding, mitre or profile in the part's own finish/notes/description, or a REBATE/GROOVE layer in its DXF (SDI's SolidWorks export carries one). A board part with none of that gets no such row — the engine does not know that every panel is sawn, and assuming it would put half an hour of set-up plus run time on every joinery job in the shop. The row is named, sequenced between the router and the edge bander, given a floor so it cannot blank the labour total, and takes Tony's measured figure from the register on faced-board jobs | generic (board, on drawing evidence) | `document_builder` (mint), `department_codes` (alias), `wb_populate` (row name, floor, sequence, overlay) | `this commit` | `test_a_board_drawing_that_says_rebate_gets_the_spindle` (the words that mean spindle work; a plain panel gets nothing; rate, setup, floor and sequence all present) | implemented — verify on the 11908-21 rerun |
| D-061 | 16 Sep 2026 | James Gray | all jobs | Three corrections landed on one figure in a day — revoke the customer inheritance (D-053), stop it being a chargeable constant (D-056), scope it to its own job (D-060) — and each was right and none was enough, because each still let £— reach a line. On reading the comparator implementation: "we don't know about the 250 as our estimating process is as independent as it can be… it should not appear in a new estimate at all — not as a charge, fallback, comparator, workbook note or suggested value… it must be impossible for an estimator run to read, display or charge it" | **Historic estimates teach; they do not supply prices** — added to Part 1. A figure from an earlier job may not appear in a new estimate in any form, and where retained for audit it lives OUTSIDE the pricing resolver. The £— moves from the register's `prices` to `historical_audit_record`, which `load()` does not read and `lookup()` cannot return; the comparator branch is deleted; the specs-on-file listing no longer offers earlier quotes as candidates. **What 7332-01 taught is kept and is not a number**: Harrods 01 is decorative, so the per-kilo zinc card cannot price it (the original sixteen-to-one defect, untouched), and a fresh job-specific quote is required. The line says what is missing and who is being asked | generic safeguard | `data/price_register.json` (audit section), `estimator.plating_unit_price`, specs-on-file listing | `this commit` | `test_a_price_is_data_not_a_literal`, `test_a_decision_made_once_is_made_once`, `test_a_named_plate_spec_is_a_quoted_price` (no figure reaches any line, under any job) | implemented |
| D-060 | 16 Sep 2026 | James Gray — architecture decision | all jobs | Prices were numeric literals in `config.py` — the tape's roll at £—, Tony's edging at £—/m, a plater's £—. Each was attributed and dated, which made them honest and did not make them right: changing a rate was a CODE change, nothing carried an EXPIRY so age was indistinguishable from agreement, and a job's quote looked identical in the file to a shop's standing rate — which is how £— for one stand became chargeable on every job naming the same finish | **One versioned price register, `data/price_register.json`**, carrying `price_key`, label, amount, unit, currency, source type and reference, source date, review date, scope, status and supersession. `src/price_register.py` reads it and computes two things a caller must not work out for itself: `chargeable` (status firm AND in scope) and `out_of_review`. `stated_prices` reads the register BEFORE config, so the waterfall is unchanged and entries migrate without breaking. An incomplete entry is NOT APPLIED and named; a missing register costs the run nothing and says so. **Scope carries the restriction, status carries the firmness** — so ONE `HARRODS01` entry is 7332-01's confirmed quote on 7332-01 and a labelled comparator everywhere else, instead of two copies of a number in two places. Tracked in git on purpose (`.gitignore` exception) so a rate change is reviewable in a diff without a code edit | generic architecture | `data/price_register.json`, `src/price_register.py`, `stated_prices._cfg`, `estimator.plating_unit_price` + specs-on-file listing, `README` (SDI_REGISTER_TODAY) | `this commit` | `test_a_price_is_data_not_a_literal` (14 tests: completeness refused, missing register survivable, scope both ways, review date honoured, the engine reads it) | implemented — first three prices migrated; `BOARD_SHEET_PRICE_GBP`, the £2.50/kg zinc card and `COMMERCIAL_LINE_GBP_PER_ORDER` still to move |
| D-059 | 16 Sep 2026 | James Gray | all jobs | "NOT PRICED" was becoming the final outcome rather than a temporary one, and a £0 plating line understates a decorative-brass unit by most of its value. The operating rules now say the engine must attempt to price every required item, and that "not priced" is permitted only after the documented hierarchy is exhausted — naming the missing information and the requested owner | Three Part 1 sections added as the operating contract: **Mandatory rule for changes** (no commit is a completed learning change until the register is updated), **Price and method separation**, and **No silent gaps**. The plating path's last rung now names what is missing and who is being asked. Rung 4 (a labelled market estimate) is **declared as not connected** rather than pretended: the market path prices parts off a drawing and nothing asks it what a plater charges for a finish | generic | `docs/CHANGE_REGISTER.md` Part 1, `estimator.plating_unit_price` (rung 5 wording) | `this commit` | `test_a_named_plate_spec_is_a_quoted_price` | implemented — rung 4 outstanding |
| D-058 | 16 Sep 2026 | James Gray — architecture decision | all jobs | Estimator feedback has shown that knowledge currently spans SDI Live, spreadsheets, source files, answers files and code. Without clear ownership, a number can become an unexplained constant or duplicate calculation. | **Configuration and pricing-logic boundary defined.** Shared, reusable, evidenced facts belong in named configuration entries; pricing calculations belong in central engine modules; workbook formulas remain the workbook's authority. Every shared entry needs source, date, owner, scope and review status. | generic architecture rule; job answers remain job-scoped | policy recorded in Part 1; existing config entries need audit and classification | — | verify through the SDI Estimating Intelligence architecture page and future estimate provenance | **implementation/audit required** |
| D-057 | 16 Sep 2026 | James Gray — pricing-policy decision | all jobs | The Harrods 01 £— figure exposed a design risk: it was a valid supplier figure for 7332-01, but source code could charge it on a future job merely because the finish name matched. That copies an old estimate rather than learning from it. | **Price-source hierarchy added to the operating guide.** SDI Live/UDEF and supplier data are preferred; a current job quote may price that job; historic sheets can be used as dated, identified comparators/inferences; and LLM market research remains a labelled last-resort price. The price line must state which source won. A previous job's supplier figure may identify a likely process or provide a transparent historical comparator, but must not become an unlabelled automatic price on a new job | generic safeguard; job quotes remain job-scoped | policy recorded in Part 1; D-056 removed the chargeable constant | `this commit` | `test_a_named_plate_spec_is_a_quoted_price`, `test_a_decision_made_once_is_made_once` | **implemented — awaiting live proof.** Clauses 1–3: `gbp_per_unit` is gone from `NAMED_PLATE_SPECS.HARRODS01` entirely (D-056), SDI Live is asked first, and this job's answers file outranks everything below it. Clause 4: with no live price the line now PRICES from the last quote as a labelled historical comparator — £—, 7332-01, 9 Sep 2026, "NOT a current price", why no better source won, and what to do — carried into the sheet's own description column, because a comparator that reads "plating" by the time an estimator sees it is an unlabelled standing rate whatever the note said upstream. A first cut returned NOT PRICED and was wrong: a £0 plating line understates a decorative-brass unit by most of its value |
| D-056 | 16 Sep 2026 | James Gray | 7332-01 | **£— was a numeric literal in source and the engine charged from it.** Revoking the customer-keyed inheritance (D-053) did not stop that, and marking the entry `priced_per_job` did not either — those were labels, and `plating_unit_price` never saw a job to check them against, so any drawing naming Harrods01 still inherited 7332-01's quote. James: "the engine must learn methods, conditions, and evidence, not copy a manual estimate's numbers into the next estimate… prices should live in a versioned, attributable price register or live system connector — not as numeric literals in Python configuration. Code should contain the pricing MECHANISM; data should contain approved rates, dates, scope, source, and expiry" | The entry keeps the KNOWLEDGE and loses the rate. What it teaches: "Harrods 01" is a **decorative** requirement, so the £/kg zinc card must not price it (the original defect, sixteen to one); and it is **quoted per job**. `gbp_per_unit` is gone from the entry entirely, so nothing can charge from it. The figure is asked of SDI's own sources at run time like the tape's roll price; where they cannot answer the line is NOT PRICED and asks for the plater's current figure, showing the last quote (£—, 7332-01, 9 Sep 2026) as dated context marked "for reference, not applied". A job-specific quote belongs in that job's answers file, which already has a path for it. **Not even 7332-01 prices from source** | generic | `config.NAMED_PLATE_SPECS` (rate removed, method kept), `estimator.plating_unit_price` (live lookup → ask), specs-on-file listing | `this commit` | `test_a_named_plate_spec_is_a_quoted_price` (a live price prices it; nothing in the sources means an ask; the entry carries no chargeable rate), `test_a_decision_made_once_is_made_once`, `test_the_card_only_prices_the_plating_it_is_a_card_for` (the zinc card is still stopped dead) | implemented |
| D-055 | 16 Sep 2026 | James Gray's reviewer (13:47 assessment) | 0355255 | The Rev box was blank beside a filename that says REV B. The pattern required "_rev" immediately followed by the letter and searched only the PDF title or the caller's stem — both real packs defeat it: "10975_REV B" has a space, "_Rev_A_" an underscore, and neither is the stem it was given | The separator may be space, dot, dash or underscore; the drawings' own names are searched as well as the title and the stem; and the token must END there, so "REVERSE PANEL" is not Rev E. Both real packs now read (10975-02 Rev B, 11908-21 Rev A) | generic | `client_quote_html._drawing_identity` (revision) | `this commit` | `test_the_pack_names_the_unit` | implemented — verify on the rerun |
| D-054 | 16 Sep 2026 | Traced in session (13:47 book, four runs) | 0355255 | The tape blocked as `10975EPDMCLOSEDCELL` through FOUR builds. Every fix chased where a WITHHELD FLAG lived — set it on the record (D-041), record the names it answers to (D-044), close those names over the job (D-050) — and each failed identically, because a flag written in one place has to survive to another and on this job it does not | **The flag was never the evidence.** The sheet is: this line is priced by the roll-goods length arithmetic off SDI Live, £0.28, reproducible between runs. A line whose APPLIED price came from a reproducible source cannot also be a line costed by an AI market estimate — the money that reached the total came from the catalogue and the market figure beside it is a rival that lost. True whatever any flag says, and true under every spelling, because the aliases come from the job's own records. Narrow: only a reproducible APPLIED price on the SAME line clears it, so 11350's £86.04 with nothing behind it still blocks | generic | `invariants.check_prices_are_reproducible` (alias groups + reproducibly-priced set) | `this commit` | `test_a_displaced_price_stays_displaced_under_every_name` (the live shape with NO flag anywhere; a catalogue price on a different line clears nothing) | implemented — verify on the rerun |
| D-053 | 16 Sep 2026 | Howard Thurley (reply, 16 Sep 2026) | 7332-01 | **A rule we shipped was wrong and he said so.** We told him Brass Harrods 01 at £— was "recorded as a standing decision, so any future Harrods job whose drawing only says Harrods01 prices at £—". He replied: "Plating would be as drawing specific, £— is from supplier per unit and is independent of any other job. Plating jobs priced independently." | The customer-keyed entry is REVOKED and `INHERITED_ESTIMATOR_DECISIONS` is empty; `test_the_customer_name_alone_buys_nothing`, which that entry reversed on purpose, stands again — it was right the first time. Reading the spec off the drawing is unaffected; the figure beside it is marked `priced_per_job` with the last quote's job and date, shown as evidence and asking for this job's plater price. The MECHANISM stays, because a genuine standing rule (his own "M&S dress all seen welds, TTI none") still belongs in it — what it must not hold is a quoted price wearing a rule's clothes | generic | `config.INHERITED_ESTIMATOR_DECISIONS` (emptied), `NAMED_PLATE_SPECS` | `this commit` | `test_a_decision_made_once_is_made_once` (rewritten to pin the revocation and keep the machinery tests) | implemented |
| D-052 | 16 Sep 2026 | James Gray's reviewer (P2 on D-049) | 7332-01 | The 40-minute brushing figure now applies to every plated weldment, but Howard gave it "based on experience and for similar size units, time would vary per unit / size" — a calibrated judgement being applied as a constant of the shop | An INDICATIVE STATED METHOD, and the line says so: the flag names the basis, the part it was calibrated on, and this weldment's OWN measured size, so the person who can judge whether the figure travels is given what they need. **Deliberately no numeric band** — nobody has told us what size 7332-01-101 is, so any threshold would be this engine's guess wearing Howard's name, which is the fault the register exists to stop. The caveat travels with the line instead | generic (plated weldments), indicative | `config.BRUSH_BEFORE_PLATE` (`basis`, `calibrated_on`), `estimator` (flag) | `this commit` | `test_work_the_drawing_never_mentions_is_still_work` | implemented — Howard to confirm the size range |
| D-051 | 16 Sep 2026 | James Gray's reviewer (P1, tested against the saved 10975 JSON) | 0355255 | The filename-title fallback from D-047 **found nothing on the real record**: `_source_drawing_names` guessed at plausible key names, and `file_scan` writes two specific ones — `primary_pdf` and `job_source_pdfs` — that were not among them. Discovered names `[]`, resolved description `10975-02-GA`. The fix was written, tested and inert | The lookup reads the fields the scan ACTUALLY writes, primary first, at the top level and under `estimate_summary`, handling both `{name, path}` dicts and UNC paths; the speculative keys stay below them because they cost nothing. The regression test is built on the shape the scan writes rather than a shape that would make the code pass | generic | `client_quote_html._source_drawing_names` | `this commit` | `test_the_pack_names_the_unit` (real record: job stem in, "A4 Table Top Graphic Holder" out; UNC paths; either field alone) | implemented — verify on the rerun |
| D-050 | 16 Sep 2026 | Traced in session (12:56 book) | 0355255 | Why D-041 and D-044 took three builds and still blocked: `mark_withheld` records the spellings the record carried WHEN ITS PRICE WAS DISPLACED, and on this job that moment is too early. The roll-goods pricer displaces the tape's market answer while the record knows itself only as "10975"; the compiler attaches the merged spelling `10975EPDMCLOSEDCELL` afterwards. Every build that believed it had fixed this went on blocking under a name the withholding never saw | The names are CLOSED OVER THE JOB at check time, where the whole identity graph is visible, instead of captured at the moment of withholding: any record sharing a name with a displaced line contributes its own names, and the closure chains (A merged from B, B from C). Not a general amnesty — the price fingerprint from D-044 still has to agree before anything is skipped, so a second line under a closed-in name stating its own figure still blocks | generic | `invariants.check_prices_are_reproducible` (identity closure) | `this commit` | `test_a_displaced_price_stays_displaced_under_every_name` (the live shape: alias attached AFTER withholding; closure is not an amnesty; the fingerprint still gates) | implemented — verify on the rerun |
| D-049 | 16 Sep 2026 | Howard Thurley (reply to our questions, 16 Sep 2026) | 7332-01 | We asked outright: "is the 40 minutes per stand, or once for the whole consignment? At 6 off that is the difference between about £3.50 and £21 a unit." He answered per unit — and the register held it as per CONSIGNMENT, so at six off the operation was understated six-fold | `brush_before_plate_min` is minutes **per unit**, and `BRUSH_BEFORE_PLATE` carries ONE key (`minutes_per_unit`) rather than an alias — two keys holding one figure is two places to edit and two chances to disagree. His two caveats are in the provenance: it is an experienced judgement, not a measurement, and it is scaled to similar-sized units, so a much larger or smaller stand should be asked about again | generic (plated weldments) | `config.SHOP_STATED` + provenance, `config.BRUSH_BEFORE_PLATE`, `estimator` | `this commit` | `test_work_the_drawing_never_mentions_is_still_work`, `test_nobody_divides_by_hand_twice` | implemented — verify on the 7332-01 rerun |
| D-048 | 16 Sep 2026 | James Gray's reviewer (register audit) | — | The register recorded several rules as complete that the day's live workbooks disproved, and D-045 contradicted itself outright — "one job is one job" in its evidence, "generic (joinery)" in its scope. A register read as a list of completions is the document somebody reaches for when deciding whether a number can be trusted | Split into two layers: **Part 1, the current operating guide** — only rules proven on a live book, each carrying rule type, applicability AND exclusions, source with stated-or-derived, quantity/setup basis, the book that proved it, the commit SHA and status; **Part 2, the change ledger** — every finding, attempt and correction including the failures. New status **reopened** for a rule a live book disproves. "this commit" replaced by real SHAs on the rows that had them. A rate from a single estimate is a `scoped pilot`, never `generic`, until a second job or the department confirms | documentation | `docs/CHANGE_REGISTER.md` | `this commit` | the audit itself: D-015, D-024, D-030, D-040, D-041, D-042, D-044 reopened against live books | implemented |
| D-047 | 16 Sep 2026 | James Gray's reviewer (12:56 assessment) / traced in session | 0355255 | Three fixes shipped in `93bc6a7` did not reach the 12:56 book. The Description box came out EMPTY — the code guard fired and the filename fallback never ran, because `_drawing_identity` is handed the JOB stem ("10975-02", which cleans to nothing) and never the drawing's own filename, which is where the product name lives. The answers file was not accepted and nothing in the log said what had been searched | (a) The resolver reads the pack's own drawing filenames off the scan record, longest first, so "0355255 - A4 Table Top Graphic Holder - 10975_REV B.pdf" can name the unit when no record in the job can; the cleaning is one function (`_title_from_a_filename`) instead of an inline block, and it refuses a code-shaped result. (b) A folder that holds something answers-shaped and yields nothing now says so: which folders were searched, under what drawing number, and what answers-like files it saw — no file at all stays silent, because that is the ordinary case | generic | `client_quote_html._source_drawing_names` / `_title_from_a_filename`, `file_scan` (confirmations else-branch) | `this commit` | `test_an_estimators_no_is_an_answer` (live shape: job stem in, product name out) | implemented — verify on the rerun |
| D-046 | 16 Sep 2026 | Tony Ford (his own 11908-21 sheet, 3 Sep 2026) | 11908-21 | "Not all materials calculated no ABS edging Allowed" — the pack names no edging spec anywhere (his own line quotes the Egger reference from the spec book, not from the drawing), so the engine could not mint the material without inventing it, and the line was silently absent | His spec and rate go into `ESTIMATOR_STATED_PRICES` — 23 × 1 mm ABS to match Egger W1001 ST9, Ostermann, £—/m — asked of the system first and the register second, like the tape, so a line priced from it says whose figure it is and when. **The rate generalises; the metreage does not.** He bands 5 m a unit against a much larger drawn perimeter, because only the VISIBLE edges are banded — which edges those are is a judgement about the product, not a number on the drawing, and a rule that banded every drawn edge would overcharge every joinery job by the difference (on his own tray, £5.18 against £1.82). So the engine measures what is drawn, holds the rate, states the upper bound, and asks for the banded metres | generic (faced board) | `config.ESTIMATOR_STATED_PRICES` + `FACED_BOARD_EDGING_CODE`, `wb_populate` edging ask | `9f853e4` | `test_the_joinery_floor_is_measured_not_borrowed` (his £1.82 reproduced from the register: 5 m × £— × 1.04) | implemented — Tony to confirm the rate still stands |
| D-045 | 16 Sep 2026 | Tony Ford (his own 11908-21 sheet, 3 Sep 2026) | 11908-21 | "No edge banding, no machining saw/spindle, no bench work time, CNC setup not amortised" — every joinery throughput the engine held was a GUESS borrowed from a neighbouring department, because no joinery job had ever been measured. His Labour tab is the first measurement and the guesses were badly out: CNCJ 11.32/hr against a guess of 30 (2.6× fast), EDGE 10.71 against 30 (2.8×), BENC **1.96 against 79 (40.3×)**, PACJ 18.18 against 99 (5.4×), and MC J 10.71 with no row in the engine at all. Bench work is 25.5 of his 42 hours, run forty times too fast — most of the gap to his £57.09 | The joinery departments are measured from his sheet and held in `SHOP_STATED` with per-figure provenance; the workbook's throughput table reads them from the register, so the literal is a record of the old guess and changing it changes nothing. **Recorded as DERIVED, not stated**: his sheet gives HOURS PER ORDER at a quantity of 50 and the engine needs parts/hour, so each figure is his hours ÷ his quantity — a per-order element inside those hours (a setup) would inflate the per-part figure, and one job is one job. Both caveats travel in the figure's own evidence string. MC J is banked though nothing emits it yet, so that operation arrives with a measurement rather than another guess | **scoped pilot — faced/laminated board (MFMDF, MFC) only** | `config.SHOP_STATED` + `SHOP_STATED_PROVENANCE` (`_joinery_derivation`), `wb_populate._THROUGHPUT_DEFAULTS` overlay | `4091060` | `test_the_joinery_floor_is_measured_not_borrowed` (arithmetic reproduced from his sheet; every figure attributed and marked derived; the 40× bench correction pinned) | **rescoped after review** — shipped first as a universal joinery floor, which repeated the setup-amortisation defect Tony himself reported. Now gated to the family it was measured on, with setup recorded as UNKNOWN (`joinery_setup_min_per_department: None`) rather than silently treated as zero. Awaiting Tony on two questions: is the bench figure right, and how much of each department's hours is set-up once rather than per tray? |
| D-044 | 16 Sep 2026 | James Gray's reviewer (P1 on D-041) | — | The displaced-price suppression was keyed by part CODE. One code can appear on two separately costed lines, so displacing one would have hidden a live AI price on the other — quietly defeating the guard that exists to stop three different totals on identical inputs | The record carries both halves of the identity: the names the line answers to AND a fingerprint of each displaced price (its source and its figure). A stamp is skipped only where both agree, and the comparison is deliberately asymmetric — a judged stamp stating a figure the displaced price does not is REPORTED, never suppressed, because a figure we cannot check off is a figure we report. The fingerprint reads the fields these blocks actually carry (`source`/`source_type` from an LLM stamp as well as `source_name`/`source_class` from the resolver), since an empty fingerprint would match nothing on exactly the stamps this is for | generic | `price_provenance.stamp_fingerprint`, `fingerprints_match`, `mark_withheld`, `invariants.check_prices_are_reproducible` | `0b5cd85` | `test_a_displaced_price_stays_displaced_under_every_name` (same code on a second line still blocks; older records without fingerprints keep working) | **REOPENED with D-041** — the fingerprint gate is sound and tested, but it cannot fire on a name the record never captured. Same root cause, same fix. |
| D-043 | 16 Sep 2026 | James Gray's reviewer (P1 on D-040) | — | `estimator_dimensions.json` carries no job in its name and was returned on sight, so in a folder holding two jobs one file governed both — the cross-governance the named lookups had just been tightened to prevent, left open by the one pattern with no name to check | The unnamed file earns the job on one of two proofs and nothing else: the folder holds a single job, so there is nothing to confuse it with; or the file NAMES its job inside (`drawing_number`/`job`/…) and that job is this one. Otherwise it is refused out loud, naming the remedy | generic | `estimator_confirmed._unnamed_file_is_for_this_job` | `0b5cd85` | `test_a_confirmed_decision_stays_closed` (single-job folder trusted; several jobs refused; a file naming its own job accepted for that job only) | implemented |
| D-042 | 16 Sep 2026 | James Gray's reviewer (12:16 assessment) | 0355255 | Three defects in the deliverables, none of them money: the AI Explanation tab listed PACKAGING twice with contradictory verdicts ("an SDI house rate marked INDICATIVE" and, three rows later, "£0.00 — the line is costing nothing") and then summarised the job as "2 line(s) carry no price at all — PACKAGING, DELIVERY" on a sheet where packaging is priced; the Description box read "10975-02-GA", a code under a label an estimator asked for so he could tell what a sheet was FOR; and the provenance banner carried five emoji, reported as "replacement characters in headings and currency labels" | (a) The sheet prices packaging with a FORMULA so it reprices when D6 changes, and a formula cell reads back as no number — the record, not the read-back, is the authority on whether a line is priced, so a line the record has classified comes off the unpriced list in both writers. (b) A title that is only a code is refused at the one resolver, on SHAPE not string equality (the old guard compared against the drawing number and "10975-02-GA" ≠ "10975-02"), which lets the pack's own filename name the unit; filing debris is trimmed from both ends of that name. (c) The banner says its counts in words — Calibri has no glyph for an astral-plane codepoint, and a status a reader cannot see is not a status | generic | `estimate_explained` (×2 writers), `client_quote_html._reads_as_a_code` + stem trim, `wb_populate` (Description guard), `estimation_report` (banner) | `0b5cd85` | `test_one_line_gets_one_verdict`, `test_an_estimators_no_is_an_answer` (code-shaped titles, both real packs) | **REOPENED (12:56 book)** — the packaging duplicate and the emoji are gone and stay fixed, but the Description box came out EMPTY rather than named: the code guard fired correctly and the filename fallback never ran, because the resolver is handed the JOB stem ("10975-02") and never the drawing's own filename. Fixed since; awaiting a book to prove it. |
| D-041 | 16 Sep 2026 | Traced in session (12:16 book) | 0355255 | One report said both "No line rests on an AI market indication" and, four paragraphs later, "2 priced line(s) that reached the total were costed by an AI market estimate: 10975EPDMCLOSEDCELL" — BLOCKING. Both were the same tape. It is priced by the roll-goods length arithmetic off SDI Live (the £0.28 on the sheet); the market answer that displaced was marked withheld on the part record, but the compiler had merged this line from a second spelling and the same displaced price is stamped again under THAT name, where it went on reading as money in the total | Withholding is a property of the LINE, not of the object somebody happened to hold: `mark_withheld` records every spelling the displaced line answers to (its own code, matched/canonical codes, folded identities, merged aliases) and the reproducibility check honours them. Deliberately not "every alias of every part" — only a line whose price was explicitly displaced contributes, so a market price that genuinely reached the total is still caught under whichever name it is stamped with | generic | `price_provenance.mark_withheld`, `invariants.check_prices_are_reproducible` | `79854f0` | `test_a_displaced_price_stays_displaced_under_every_name` (incl. the guard still blocking a real applied market price, and one line's displacement not excusing another's) | **REOPENED (12:56 book)** — still blocking as `10975EPDMCLOSEDCELL`. The names are captured at `mark_withheld` time from the record in hand, and that record does not yet carry the merged spelling the compiler attaches later. Needs the identity closure computed where the whole job is visible, not at the moment of withholding. |
| D-040 | 16 Sep 2026 | James Gray's reviewer (11:19 assessment) / traced in session | 0355255 | Three findings on the 11:19 book — order quantity stuck at 1, the delivery exclusion never landing, and a gauge Howard had confirmed still on the outstanding list — turned out to be ONE cause: the answers file was never found. The finder only ever tried the DRAWING NUMBER, and this pack's drawing number is the customer's (0355255) while the office names the file after SDI's job number (10975-02). Neither is wrong; they are two names for one job | A job answers to the names on its own pack. The finder tries, in order: the drawing number and its stems; an exact filename against the PDF's name or the folder's; then ONE answers file in the folder whose JOB CODE is this job's, longer or shorter (10975 ↔ 10975-02). The inexact step is narrow by design — the code relation must break at a separator (11908-21 never answers for 11908-22, 1097 never for 10975), a name carrying no job code matches nothing, and two files matching one code are never guessed between: the run says which two and asks for one to be named | generic | `estimator_confirmed.find_corrections_file`, `job_codes`, `_same_job_code` | `e0a343b` | `test_a_confirmed_decision_stays_closed` (the real 0355255/10975-02 filenames; neighbour-number and no-code refusals; the ambiguity announced) | **Implemented, NOT yet verified** — the 12:56 run still costed at quantity 1 with gauge, tape and delivery open, so no answers file was accepted. The lookup itself is tested; what is missing is evidence of where the file was and what the run saw. The run now says so out loud when a folder holds something answers-shaped and nothing is accepted. |
| D-039 | 16 Sep 2026 | James Gray's reviewer (11:19 assessment) | 0355255 | Every deliverable was stamped "+local edits", so no book could claim to be reproducible from its commit — on a checkout whose tracked files were identical to the commit. The stamp asked plain `git status --porcelain`, which lists untracked files as `??`: the drawing pack, the answers file placed beside the drawings, and the workbook the run had just written each made the engine accuse itself of being modified | The reproducibility question is asked of TRACKED files only (`--untracked-files=no`) — the same narrowing `engine_build.py` already carried and this module missed. Loose files are still counted and reported, as loose files, in their own sentence; nothing is hidden, because the source digest hashes every module on the import path whether git has heard of it or not. Job droppings the engine writes beside a pack (CAD exports, `*_report.html`, `*_quote.html`) are now ignored deliberately, named narrowly so a hand-written doc is never swallowed | generic | `build_stamp.build_stamp` / `build_stamp_line`, `.gitignore` | `7965e07` | `test_a_loose_file_is_not_a_modified_engine` (loose files ≠ local edits; a modified tracked file still raises the alarm; the flag pinned against the source) | implemented |
| D-038 | 16 Sep 2026 | Howard Thurley (his own 0355255 sheet, 7 Sep 2026) | 0355255 | Assemble/pack booked at the corpus-derived 99/hr while his sheet books PACP at 30/hr for "Apply Tape, Bag, Bulk Pack" — most of the labour gap at volume (~59p/unit at 10-off) | `SHOP_STATED["acrylic_assemble_pack_parts_per_hour"] = 30` with per-figure provenance; the throughput table reads the register (same overlay as the laser's 95) | generic (acrylic assemble/pack) | `config.SHOP_STATED`, `wb_populate._THROUGHPUT_DEFAULTS` overlay | this commit | `test_nobody_divides_by_hand_twice` (provenance completeness) | implemented — verify on the 10975-02 rerun |
| D-037 | 16 Sep 2026 | Howard Thurley (his own 0355255 sheet) | 0355255 | The method bagged in PACK13 (his email's code — an 18×24 bag at £29.68/1000) while his priced sheet bags in PACK56 (12×18×100G, The Packaging Company, £17.91/1000), which also matches the 12×18 the method describes | The sheet is the estimate he issued: the method's bag is PACK56; the sheet row now names the method's OWN codes so a future change never leaves a stale literal; the PACK13-vs-PACK56 question stays in the covering email for his confirmation | gated-generic (the packing method) | `config.PACKING_METHOD`, `estimator` (dynamic row description) | this commit | `test_packing_is_a_stated_method_priced_live` (pack-of £17.91, codes from config) | implemented — Howard to confirm |
| D-036 | 16 Sep 2026 | James Gray's reviewer (P1) | — | The order-quantity check only flagged, and only when SDI_ORDER_QTY survived the runner — the suspected failure is the runner dropping the request before the env var exists, so the check saw nothing and 1-off books shipped against a 50-off ask | The request is part of the run contract and survives a dropped flag: `order_quantity` in the job's answers file is read at the same point SDI_ORDER_QTY is (the flag still wins when it arrives); and a recorded request the costed record does not match gets the DO NOT SEND treatment — refusal note, quote held, workbook still delivered | generic | `file_scan` (order-qty decision), `main.py` (refusal), `wb_populate` (sheet flag) | this commit | `test_one_assembly_is_packed_once`, refusal path shares `do_not_send_note` | implemented |
| D-035 | 16 Sep 2026 | James Gray's reviewer (P1) | — | The root-collapse spelling rule stripped ANY trailing alphabetic word: ABC-LEFT merged into ABC-RIGHT and a genuine assembly was deleted because its children were a subset | Only NAMED sheet-role tokens strip (`part_code_conventions.ASSEMBLY_ROLE_TOKENS`: GA, ASSY, ASSEMBLY, ARR, ARRANGEMENT, GEN), applied in all three places the loose strip existed: the root collapse, the pack-event fold, and the answers-file name stems (one ABC_confirmed.json must not govern both hands) | generic | `part_code_conventions.strip_assembly_role`, `route_compiler` (×2), `estimator_confirmed.drawing_stems` | this commit | `test_one_assembly_is_packed_once` (ABC-LEFT/RIGHT both survive, shared panel counts 2) | implemented |
| D-034 | 16 Sep 2026 | James Gray's reviewer (09:57 assessment) | 11908-21 | Two runs went out at 1-off against a --order-qty 50 command and nothing said the request was lost; and the quote's title read "QTY FILL ANY OPEN GAPS ON CORNERS WITH MATCHING WAX" — a drawing note printed as the product name | wb_populate asserts the written D6 against SDI_ORDER_QTY and flags a mismatch loudly ("the sheet is NOT the quantity asked for"); the quote's title source rejects instruction-shaped text (imperative-led: FILL/CHECK/DO NOT/…) and falls to the top assembly's own description | generic | `wb_populate` (assertion), `client_quote_html._reads_as_an_instruction` | this commit | `test_an_estimators_no_is_an_answer` | implemented |
| D-033 | 16 Sep 2026 | Tony Ford | 11908-21 | "Delivery is not required" — and the sheet still asked "NOT YET PRICED: enter the per-unit figure" on every run | `commercial_excluded` in the answers file's estimator_decisions (e.g. ["DELIVERY"]): the line stays on the sheet at a deliberate £0 naming whose call it was, classes nil-by-design everywhere, and comes off the outstanding list; unknown codes are reported, never swallowed | generic (per job, by decision) | `estimator_confirmed._read_decisions`, `estimator` (placeholder mint), `costed_facts._price_origin`, `estimator_inputs` | this commit | `test_an_estimators_no_is_an_answer` | implemented — needs Tony's file entry for 11908-21 |
| D-032 | 16 Sep 2026 | James Gray's reviewer (09:57 assessment) | 11908-21 | Every drawing quantity costed DOUBLE (base 1→2, sides/dividers 4→8, "no reason recorded"): both spellings of the root — "11908-21" and "11908-21 GA" — cascaded one unit each into the same children. The colourway collapse missed by one bumpon: Jaccard 3/4 = 0.75 against its 0.8 bar | Same-stem roots also fold on CONTAINMENT where the two names are one assembly's spellings (a trailing purely-alphabetic token is a sheet role); genuinely different arrangements have disjoint children and fold on neither test; GA2's digit still never folds | generic | `route_compiler` (root collapse cluster rule) | this commit | `test_one_assembly_is_packed_once` (quantities 1/4/4 asserted on the graph) | implemented |
| D-031 | 16 Sep 2026 | James Gray (09:57 assessment) | 11908-21 | The faced-board promotion (D-027) never fired on the live run: the trays carry a modelled weight, and the stated-weight × £/kg branch returned plain-MDF money before the promotion point was ever reached — the weight was right and the price basis still wrong | A promoted laminated board declines stated-weight PRICING (the weight stays on the record) and prices by the purchased sheet; and where a price point states the sheet it was paid for (£172 buys a 3080×1220), the yield is computed on THAT sheet, never on a stocked size the money did not buy | generic | `estimator` (stated-weight gate, `_board_priced_sheet_mm`, board branch), `config.BOARD_SHEET_PRICE_GBP` dict form | this commit | `test_a_laminated_core_is_bought_pre_faced` (modelled-weight case; plain board keeps its weight path) | implemented |
| D-030 | 16 Sep 2026 | James Gray (19:02 review, F6) | 10975-02 | The blank check reported a phantom 792×760.3 DXF flat — 377% of the model — twice: the pack's only DXF is the GA sheet exported whole, and the reader called it a flat pattern, so its border/view extents reached the record as measured geometry | The reader applies the decisive rule at the source: a DXF carrying DIMENSION entities is a drawing, never a flat (`flat_pattern_detected: False`); and `merge_dxf_into_scan_json` consults the claim instead of ignoring it, refusing the geometry on the record rather than silently | generic | `dxf_reader` (`_is_flat_pattern`, merge guard) | this commit | `test_a_drawing_sheet_is_not_a_flat_pattern` (incl. the real 0355255 GA DXF verified False in session) | **CLOSED by D-152** — the third path was not a DXF path at all. 792 is the PAPER GRAPHIC on page 2 and 760.3 the acrylic L-stand on page 1; the pair was assembled field by field by `source_precedence` and labelled *DXF flat* by its reader's one caller. The fixes recorded in this row are correct and are now asserted against the real files. |
| D-029 | 16 Sep 2026 | Tony Ford (0359967 review) | 11908-21 | "No rebate charge allowed" — the sheet went out with rebate 0 and /0.93 while the office books M&S at 1.8% over /0.92 | Per-customer commercial terms (`CUSTOMER_COMMERCIAL_TERMS`, the office's own table off Tony's Labour tab: Tesco 2.7/0.93, TTI 6.6/0.93, M&S 1.8/0.92, Boots 2.7/0.93); wb_populate writes the rebate cell (never over a typed figure) and corrects the absorption divisor in the sheet's own formula; the JSON equivalent reads the same table | generic (per customer) | `config.customer_commercial_terms`, `wb_populate._apply_customer_terms`, `estimator._build_workbook_equivalent_pricing` | this commit | `test_the_customers_own_terms_price_the_sheet` (incl. Tony's £57.09 arithmetic reproduced) | implemented |
| D-028 | 16 Sep 2026 | Tony Ford / engine | 11908-21 | Two Packing Joinery rows for one tray — the same shipping assembly minted a pack event under "11908-21" AND "11908-21 GA" | Root assemblies whose names differ only by a trailing purely-alphabetic sheet-role token fold to one; the fuller parts list keeps the event; a digit in the tail (GA2) is a different stand and never folds | generic | `route_compiler` (assembly-event mint) | this commit | `test_one_assembly_is_packed_once` | implemented |
| D-027 | 16 Sep 2026 | Tony Ford (0359967 review) | 11908-21 | "Wrong sheet size for Laminated board / Wrong price for board / no ABS edging" — 9mm laminated MDF costed as plain MDF (£43 a sheet vs the £172 laminated board bought), laminate flagged "supplied free", edging silently absent | A laminated core is bought pre-faced: promotion to MFMDF/MFC on the drawing's own evidence (laminating op, finish, DXF name "MDF+ LAM"), priced by the purchased sheet, never falling back to core £/kg; 3080×1220 Lawcris sheet added to stock sizes; 9mm point £172 recorded in `BOARD_SHEET_PRICE_GBP` dated and credited (his breaks 173/172/168/165/163 noted, vector mechanism pending); laminate finish reads "charged in the board"; unstated edging becomes ONE ask carrying the measured edge metreage | generic | `estimator._faced_board_promotion` + board branch, `config` (sheets, price point), `invariants._laminate_is_in_the_board`, `wb_populate` (edging ask) | this commit | `test_a_laminated_core_is_bought_pre_faced` | implemented — confirm £172 and PACK/edging specs with Tony |
| D-026 | 15 Sep 2026 | James Gray | 11908-21 | Job email arrived with no attachments; the run costed nothing and the covering note still went out as "PROVISIONAL. not reported/unit at 1 off" — unreadable from a broken estimate | An empty estimate (no BOM, material, labour or unit figure) sends a short note whose subject is "NOTHING WAS ESTIMATED — the pack arrived empty", naming the cause and the remedy; a real book with unreadable money keeps the DO NOT SEND path | generic | `estimate_explained.covering_email` | 18c0467 | `test_an_empty_pack_is_not_a_provisional_estimate` | implemented |
| D-025 | 15 Sep 2026 | James Gray (19:02 review, P1) | — | If the break-table fill fails or writes 0 rows, the sweep still measures the empty table and prints four confidently flat columns — the 19:02 book's shape, reachable by a second route | The table's health is judged before the sweep runs; an empty table puts one warning in the run log AND in red on the Quantity Breaks tab itself ("do not quote quantity pricing from this sheet"), same object both places so the wordings cannot drift. The companion P1: the ordering test proved text order only — now the mechanism is exercised on real cells (the 19:02 packing step 1.92/0.219/0.0674/0.05236/0.04669 written and read back; the quantity vector non-descending and padded) | generic | `main.py` (guard before `_sweep`), `quantity_breaks_tab.write_quantity_breaks_tab(warning=…)` | this commit | `test_one_workbook_carries_every_quantity` (cell-level tests + warning on/off) | implemented |
| D-024 | 15 Sep 2026 | James Gray (19:02 review) / Howard Thurley's answers | 0355255 | The confirmations file did nothing three ways: `10975-02_confirmed.json` not a recognised name ({drawing} is the GA sheet name), `10975` matched no part (the tape's record is the SW component name "10975 EPDM Closed Cell Tape^…"), and an omitted basis (defaults to "read", rank 72) never closes a gauge the DXF holds at 95 | Filenames: trailing purely-alphabetic sheet-role tokens are stripped to stems ("10975-02-GA" also answers to "10975-02"; numeric segments never stripped). Matching: a part answers to its part_number, the ^config-stripped name, the leading code of a "CODE Description" component name (digits-led, ≥4, never a category word), and every folded duplicate identity. Closure: a person AGREEING with the kept value closes the gauge decision whatever rank their reading carries (the agreement is on the record via `support_for`); a person naming a different figure closes nothing. Agreements are visible: counted in the report, flagged on the part, said in the run log | generic | `estimator_confirmed` (`drawing_stems`, `confirmation_match_keys`, `_same_figure`), `costed_facts.thickness_conflict`, `file_scan` log | this commit | `test_a_confirmed_decision_stays_closed` (incl. Howard's exact file end-to-end) | **Implemented, NOT yet verified** — same as D-040: no live book has yet shown the file landing. |
| D-023 | 15 Sep 2026 | James Gray | — | Packing at £0 on every estimate while Howard's method sat in his email | Method (bag per unit, boxes in stated steps) held in `config.PACKING_METHOD` with provenance; consumable prices live from UDEF each run (pack-of-N divided to the price of one); all-or-nothing; no extrapolation past the last stated step; between-break counts priced but labelled INFERRED pending Howard's capacity-rule answer; applicability gated to the stated basis (plastic-family display goods, ≤5 kg/unit — the ceiling is an SDI Intelligence assumption, declared) so a steel or joinery job does not inherit the bags | gated-generic (plastic display goods) | `commercial_lines._method_price`, `material_price_break` break carry-through | 0862530 · 360b11d (pack-of-N) · 06cb558 (applicability gate, INFERRED label) · d6a2c02 (sheet row explains its number) · this commit (partial evidence cannot bypass the gate: an unmeasured fabricated leaf declines the method by name; bought-ins ride along; assembly parents ignored) | `test_packing_is_a_stated_method_priced_live` | implemented |
| D-022 | 15 Sep 2026 | James Gray (review) | — | `SHOP_STATED`'s shared header attributed 0355255's linebend/laser figures to 7332-01 | Per-figure provenance (`SHOP_STATED_PROVENANCE`: who, when, source job, unit, evidence); `shop_stated_source(key)` replaces the shared sentence | generic | `src/config.py` | 0862530 | `test_nobody_divides_by_hand_twice` (two-jobs attribution test) | implemented |
| D-021 | 15 Sep 2026 | Howard Thurley | 0355255 | Yield 24/sheet; exchanging length and width gives 26 | Try both orientations for sheet plastics; write the blank turned so the workbook's own formula agrees; never for directional finishes (`DIRECTIONAL_FINISH_TOKENS`); steel and faced board stay drawn-orientation until the shop rules on grain | generic (sheet plastics) | `estimator.select_sheet_size` + acrylic/HIPS branches | bdb9356 | `test_a_blank_may_be_turned_on_the_sheet` | implemented — verify on next 10975-02 run |
| D-020 | 15 Sep 2026 | Howard Thurley | 0355255 | "AI £48.89, unsure where this has been generated" | It is the live UDEF-derived plain-stock 2 mm acrylic median × 3050×2050 sheet (recomputed £47.21 on 15 Sep); commitment: an exact confirmed SKU (PLAS534 £45.19) will take precedence over the category median (fix F1, proposed) | explanation + F1 proposed | `estimator._resolve_board_sheet_rate_gbp_per_m2` (existing); F1: `MATERIAL_SKU_MAP` (to build) | — | traced in session, arithmetic verified both ways | F1 proposed |
| D-019 | 15 Sep 2026 | Howard Thurley | 0355255 | Tape £13.63 where his arithmetic makes it £0.28 | Roll goods priced by length used; roll length is a packaging fact in config; price asked of the system first (UDEF £—), estimator-stated second; stamp carries the real source | generic (roll goods) | `estimator.roll_goods_material`, `stated_prices.resolve` | 99dba22 · b15833b · 5ee2da9 | `test_a_length_off_a_roll_is_priced_by_its_length`, `test_the_label_follows_the_money`; £0.28 on the 16:38 book | verified |
| D-018 | 15 Sep 2026 | James Gray | — | Supplier column blank for four different kinds of source | Every price names its system (`price_provenance.source_system_label`); nothing renders silently; stated figures classify as config, not catalogue | generic | `price_provenance`, `wb_populate._price_origin` | b15833b | `test_a_price_says_which_system_answered_it`; "SDI Live UDEF" on the 16:38 tape line | verified |
| D-017 | 15 Sep 2026 | Howard Thurley (relayed) | 0355255 | Linebend booked at 30 parts/hr; department states 60 | 0.5 min per bend, stated register, credited to the Acrylic Dept via Howard | generic | `SHOP_STATED["linebend_min_per_bend"]` → `ACRYLIC_OP_DRIVERS` | earlier + bdb9356 (consolidation) | 59.9988/hr on the 16:38 book, row 97 | verified |
| D-016 | 15 Sep 2026 | Howard Thurley + James | 0355255 | Breaks on one sheet; break for 10 missing; 100 not wanted; per-qty workbook copies still filed | One workbook: Material Price Break tab + Quantity Breaks tab; `QUANTITY_VARIANT_WORKBOOKS` derives (copies only where the tab cannot carry the breaks); price row padded to header width (top-quantity £0 bug); template `#REF!`/mis-pointed lookups repaired in the delivered book and named on the log | generic | `material_price_break`, `quantity_sweep`, `quantity_breaks_tab`, `main.py` | d0b64e7…216bd52 | `test_one_workbook_carries_every_quantity`, `test_the_top_quantity_is_not_priced_at_zero`; one book at 16:38 with 10/50/250/1000 | verified |
| D-015 | 12 Sep 2026 | Howard Thurley (via 7332-01) | 7332-01 | Weld 50 min/stand where he states 30; dress likewise | Weld/dress minutes per weldment stated; per-joint rates derived from the measured joint count — nobody divides by hand | generic | `SHOP_STATED`, `WELD_TIME_MODEL` | earlier | `test_nobody_divides_by_hand_twice` | **REOPENED** — Howard's 16 Sep reply says the delivered book still mishandles weld and dress, and separately that the 40-minute brushing figure is PER UNIT, not per consignment as this register recorded it. The brushing basis is corrected; weld/dress joint counting is open. |

*Older decisions predate this register; they are recorded in the commit history and the
test files' docstrings, which is where this register's rows point anyway.*
